<!--
    EsLiveIndicator 컨트롤 기본 템플릿.
    Visual: 단순 원형 점. State에 따라 색·가시성·애니메이션 변경 (EsLiveIndicator.cs).
-->
<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:controls="clr-namespace:Easislides.Wpf.Controls">

    <Style TargetType="{x:Type controls:EsLiveIndicator}">
        <Setter Property="Width" Value="12" />
        <Setter Property="Height" Value="12" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type controls:EsLiveIndicator}">
                    <Ellipse x:Name="PART_Dot"
                             Width="{TemplateBinding Diameter}"
                             Height="{TemplateBinding Diameter}"
                             Fill="{DynamicResource Brush.Live.Active}" />
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
