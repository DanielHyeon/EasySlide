# Logic Map Tools

`extract_function_ast.py` extracts C# method structure with `tree-sitter` and `tree-sitter-c-sharp`.

Install tools outside the repo, either globally or as CI/dev dependencies:

```powershell
pip install tree-sitter tree-sitter-c-sharp
```

Analysis output belongs under `openspec/changes/<change-id>/analysis/`.

