# FrmMain → WPF UI/UX + 기능 포팅 로드맵

> 목표(사용자 /goal): **FrmMain의 UI/UX를 완벽하게 포팅 + 아이콘·디자인을 현대적 WPF 스타일로 업그레이드, 기능까지.**
> 기준 문서: [frmmain-vs-wpf-detailed-gap.md](frmmain-vs-wpf-detailed-gap.md)(전수 갭) · 원칙: 증분마다 TDD(테스트 먼저→실패→구현→통과)→code-reviewer→빌드/심볼확인→커밋/푸시. **dead UI 금지**(백엔드 없는 컨트롤은 비활성+툴팁으로 정직 표기).
> 검증 도구 주: 요청된 `$gstack-qa`·`$gsd-verify-work`(GSD/gstack)는 환경 미설치 → 표준 검증(전수 `dotnet test` + code-reviewer + Release 빌드/심볼)으로 대체하고 그 사실을 매 증분 보고.

## 증분 계획 (의존성 순)

| # | 증분 | 성격 | 갭 근거 | 상태 |
|---|---|---|---|---|
| 1 | **현대적 메뉴바**(파일/편집/보기/출력/도구/도움말, EsIcons+wpfui) — 기존 명령·DI 창에 배선 | UI/UX + 기능(기존 배선) | §2.1 메뉴 52→0 | ✅ 완료(9e0fb74) |
| 1b | **F-키 단축키(F12/F9/F3/F1) + 메뉴 제스처 힌트** — FrmMain 라이브 운영 키 파리티 | UI/UX + 기능 | §2.1, §3.9 | ✅ 완료(c8ff422) |
| 2 | **Region 1/2 이중언어 출력 렌더 파이프라인** — 디코더(이미 region2 파싱)→스냅샷 이중영역→렌더→VM→XAML | 기능(골격) | §3.2, §5 #1 | ⬜ 차기(최대 갭) |
| 3 | **인-셸 per-region 포맷 인스펙터**(영역별 색·정렬·폰트·표시모드 R1/R2/Both·인터레이스) — 2 위에 UI | UI/UX + 기능 | §2.2 | ⬜ |
| 4 | **절 라벨 직접 점프**(1~9·후렴·브릿지 버튼 → 절 인덱스 이동) | UI/UX + 기능 | §3.1 #절점프 | ✅ 완료(119600c) |
| 5 | **코드/악상 표시 + 조옮김**(ShowNotations 정상 배선 → 코드 렌더, Transpose ↑↓, To Capo 0) | 기능 | §3.7(+mis-bind 결함) | ⬜ |
| 6 | **Display Panel**(송출 하단 정보 바: 제목/저작권/항목번호/절·슬라이드/이전·다음) | UI/UX + 기능 | §3.2, §5 #2 | ⬜ |
| 7 | **콘텐츠 브라우징 보강**(이미지 라이브러리·배경 적용, 폴더 정렬/번호, 미디어·PPT 폴더 브라우징) | UI/UX + 기능 | §2.3, §3.3 | ⬜ |
| 8 | **전환 효과 UI**(항목/슬라이드 전환 — 서비스는 이미 존재, 인스펙터 노출) | UI/UX | §3.2 #전환 | ⬜ |
| 9 | **항목 검증·실제 큐 도메인 강화**(SeedPlaceholderQueue 더미 제거, ValidateWorshipListItems 대응) | 기능(견고성) | §5 #6·#7 | ⬜ |
| 10 | **세부 정리**(우클릭 컨텍스트 메뉴, 단축키 F3/F5/F9/F11/F12 매핑, 헤딩 AsRegion·Verse 헤딩) | UI/UX | §2.1, §3.9 | ⬜ |

> 각 증분은 독립 커밋/PR. 1·3·4·6·8은 직접적 UI/UX, 2·5·9는 기능 골격, 7·10은 보강. **2가 3의 선결**(per-region UI는 이중영역 렌더가 있어야 의미). 1은 의존성 없어 먼저 착수.

## 진행 로그
- (2026-06-01) 로드맵 수립.
- (2026-06-01) ✅ 증분 1 — 현대적 메뉴바(6대메뉴, EsIcons+wpfui, 컨텍스트 주입). 919 green, code-reviewer Approve, 커밋 `9e0fb74`.
- (2026-06-01) ✅ 증분 4 — 절 라벨 직접 점프(GetSectionLabels + JumpToLyricsSectionCommand + Preview SectionJumpBar). 934 green, code-reviewer Approve, 커밋 `119600c`.
- (2026-06-01) ✅ 증분 1b — F-키 단축키(F12/F9/F3/F1) + 메뉴 제스처 힌트. 942 green, 커밋 `c8ff422`.
- (2026-06-01) ✅ 증분 (상태바) — 하단 상태바(예배순서 수·절위치·상태·모니터, VM 무변경). 947 green, 커밋 `eb0aab2`.
- (2026-06-01) ✅ 증분 2 슬라이스 1 — **Region 1/2 region-aware 파서**(GetRegionPages, [region 2] 분리, 단일영역 무회귀). 956 green, 커밋 `c08d5ff`.
- (2026-06-01) ✅ 증분 2 슬라이스 2 — **이중언어 라이브 송출 배선**(GoLive 결합 송출 + VM 영역-인식 페이지수). [region 2] 곡이 두 언어를 회중 화면에 동시 송출(동작·가시). 966 green, code-reviewer Approve, 커밋 `57ae22f`.
- (2026-06-01) ✅ 증분 2 슬라이스 3 — **영역별 독립 렌더**(Region1/Region2 별도 본문 + 영역별 색(코드 30), 출력 XAML StackPanel 두 본문). [region 2] 곡이 두 언어를 독립 색으로 동시 송출. 단일 영역 무회귀(외곽선 정렬·높이 상한 code-review 반영). 969 green, 커밋 `191e022`.
- (2026-06-01) ✅ 증분 2 슬라이스 4 — **이중언어 Sequence 지원**("1 C 2 C"가 이중언어에도 적용). 974 green, 커밋 `dd80e16`.
- (2026-06-01) ✅ 증분 2 슬라이스 5 — **Region2 독립 정렬**(코드 32, R1 왼쪽·R2 오른쪽 등). 977 green, 커밋 `668e9aa`.
- (2026-06-01) ✅ 증분 2 슬라이스 6 — **Region2 독립 폰트**(코드 44/48). 980 green, 커밋 `6c672ae`.
- ✅ **이중 언어(Region 1/2) 트랙 영역별 완전 독립 완성**: 파서·송출·영역별 **색·정렬·폰트(이름+크기)**·Sequence. 잔여 minor: 이중언어 절점프 라벨(편의 기능, 추후).
- (2026-06-01) ✅ 증분 5(조옮김) 슬라이스 1 — **ChordTransposer 핵심 로직**(루트·접미사·슬래시·±반음). 1001 green, 커밋 `ad17650`.
- (2026-06-01) ✅ 증분 5(조옮김) 슬라이스 2 — **SongEditor 미리보기 조옮김 UI**(♯+/♭−/원조, 코드만 이동·저장 가사 불변). 1002 green, 커밋 `557ae6a`.
- (2026-06-01) ✅ 증분 6(Display Panel) 슬라이스 1 — **출력 곡 번호 표시**(FrmMain "Show Item Number"). 설정·렌더·데이터·VM·XAML·메뉴 토글 종단(15편집, 기본 off 무회귀). 1021 green, code-reviewer Approve, 커밋 `c37d61e`.
- (2026-06-01) ✅ 증분 6(Display Panel) 슬라이스 2 — **출력 저작권 표시**(FrmMain "Show Copyright Information"). SongSummary.Copyright(GetSongs COPYRIGHT 컬럼) → LiveQueueItem → GoLive → CurrentItemCopyright → ShowsCopyright → OutputWindow 하단 중앙 Border(좌/우 코너 비충돌). 설정·렌더·VM·메뉴 토글 종단(12파일, 기본 off 무회귀). 1025 green, code-reviewer 코너 충돌 MAJOR 수정(우하단→중앙), 커밋 `b0ca00a`.
- (2026-06-01) ✅ 증분 6(Display Panel) 슬라이스 3 — **출력 다음 항목 표시**(Display Panel PrevNext). MainViewModel.ComputeNextTitle(큐 Id 매칭, 마지막=빈문자열) → ResolveLiveProjection.NextTitle → GoLive → CurrentItemNextTitle → ShowsNextItem → OutputWindow 우측 상단 Border("다음 ▶ ..."). 설정·렌더·VM·메뉴 토글 종단(기본 off 무회귀). 1031 green, code-reviewer Approve(0 critical/major), 커밋 `6efcd90`.
- (2026-06-01) ✅ 증분 7(전환 효과) 슬라이스 1 — **출력 페이드 전환 UI**(FrmMain 전환 효과, 페이드만 구현). 설정 UseFadeTransition(기본 true=기존 250ms 보존)·TransitionDurationMs(0~2000) + 메뉴 "전환 효과 ▸ 페이드 사용/빠르게·보통·느리게". **근본 버그 수정**: `EasiSettingKeys.All` 누락으로 곡번호·저작권·다음항목 토글이 라이브 즉시 반영 안 되던 잠복 버그(5키 All+LegacyMap 등록, 회귀 가드 추가). 1037 green, code-reviewer Approve(0 critical), 커밋 `94793e0`.
- (2026-06-01) ✅ 증분 8(이미지) 슬라이스 1 — **출력 전역 배경 이미지**(FrmMain Images 탭 핵심 기능, 배경 적용). 설정 LyricsMonitorBackgroundImagePath(기본 빈값=무회귀) + 렌더 우선순위(곡별 61 우선, 없으면 전역) + 메뉴 "배경 이미지 ▸ 선택.../지우기". 1041 green, code-reviewer Approve(0 critical), 커밋 `edae914`.
- (2026-06-01) ✅ 증분 8(이미지) 슬라이스 2 — **이미지 썸네일 갤러리 브라우저**(FrmMain Images 탭). ImageLibraryService(폴더 열거)·ImageLibraryViewModel(Load·적용·하위폴더 토글)·ImageLibraryWindow(WrapPanel 썸네일·폴더 선택·더블클릭 적용). 메뉴 "배경 이미지 ▸ 갤러리에서 선택...". 1053 green, code-reviewer Approve(0 critical, includeSubfolders 체크박스 연결로 해소), 커밋 `76796d6`. **백로그(P2)**: 대용량 폴더 썸네일 비동기/지연 디코딩(현재 동기).
- (2026-06-01) ✅ 증분 9(PraiseBook) 슬라이스 1 — **찬양집 색인 브라우저**(FrmMain PraiseBook/Listing 초성 그룹핑). PraiseBookIndexService(한글 초성·영문·숫자·기타 그룹화, 가나다 순)·VM·읽기전용 중첩 ItemsControl 창 + 메뉴 "도구 ▸ 찬양집 색인...". 1066 green, code-reviewer Approve(0 critical), 커밋 `96af0d4`.
- (2026-06-01) ✅ 증분 9(PraiseBook) 슬라이스 2 — **명명 찬양집 저장/관리 영속화**(FrmMain PraiseBookDir). StoreFileNaming(경로 안전 공통 헬퍼 추출, WorshipListStore 도 위임)·PraiseBookStore(JSON 저장/불러오기/목록/삭제/이름변경)·VM SaveAs/Open/Delete + 창 콤보·이름 다이얼로그. 다른 찬양집 이름 충돌 시 실수 덮어쓰기 방지. 1082 green, code-reviewer Approve(0 critical), 커밋 `9fb2376`.
- (2026-06-01) ✅ 증분 9(PraiseBook) 슬라이스 3 — **찬양집 색인 HTML 내보내기**(FrmMain GenerateIndexReport). PraiseBookIndexExporter(머리글자 그룹·제목·번호 표 HTML, 특수문자 이스케이프=주입 방지)·VM BuildIndexHtml·창 SaveFileDialog 저장(try/catch 안내). 1088 green, code-reviewer Approve(0 critical), 커밋 `21a5c57`.
- ✅ **절 라벨 직접 점프** — `JumpToLyricsSectionCommand` + SectionJumpBar 로 이미 구현됨(FrmMain PreviewBtnVerse 1~9·c·b 대응). 갭 해소 확인.
- (2026-06-01) ✅ 증분 10(이미지 perf) — **이미지 갤러리 썸네일 비동기 디코딩**. 목록 즉시 표시 + 백그라운드 디코딩(Task.Run·Freeze) + 재진입 취소 가드(AsyncRelayCommand CancellationToken). 대용량 폴더 UI 멈춤(개선사항.md 병목) 해소. 1090 green, code-reviewer MAJOR(재진입 경쟁) 반영, 커밋 `18c54ce`.
- (2026-06-01) ✅ 증분 11(InfoScreen) — **공지 화면(자유 텍스트 출력 송출)**(FrmInfoScreen). MainViewModel.PublishNotice/ClearNotice + NoticeScreen VM/Window + 메뉴 "도구 ▸ 공지 화면". code-reviewer MAJOR 2건 근본수정: ①공지 텍스트가 가사 포맷터를 타 마커 손상되던 문제→Notice kind 면 포맷터 우회(verbatim), ②_liveItemId=null 와일드카드로 공지 중 이동 버튼 활성되던 문제→NoticeLiveId 센티넬. 재검증 에이전트로 종결 확인. 1100 green, 커밋 `156c25b`.
- (2026-06-01) ✅ 증분 12(PowerP) — **PowerPoint 폴더 브라우저**(FrmMain PowerP 탭). FolderFileEnumerator(폴더 열거 공통화, ImageLibraryService 위임)·PowerPointLibraryService(.ppt/.pptx)·VM/Window + 메뉴 "파일 ▸ PowerPoint 폴더 찾아보기". 선택·더블클릭으로 예배 순서 추가. 1109 green, code-reviewer Approve(0 issue), 커밋 `95e2089`.
- (2026-06-01) ✅ 증분 13(전환 모션) — **출력 전환 슬라이드 4방향**(페이드+좌/우/상/하). enum LyricsTransitionKind 설정 전배선 + OutputWindow TranslateTransform 슬라이드(CubicEase)+ClipToBounds + 메뉴 "전환 효과 ▸ 모션". 기본 Fade=무회귀. 1113 green, code-reviewer Approve(0 critical), 커밋 `5df4e5f`. 셰이프/타일 등 나머지 전환은 P2 백로그.
- (2026-06-01) ✅ 증분 14(Recent) — **최근 예배 순서**(FrmMain Recent Edits). RecentWorshipListsService(JSON 영속·최신순·중복제거·최대8) + MainViewModel 통합(Save/Load 시 Record)·동적 서브메뉴 "파일 ▸ 최근 예배 순서"·재오픈 명령. DI 등록. 1123 green, code-reviewer Approve(0 critical), 커밋 `2fac483`.
- (2026-06-01) ✅ 증분 15(InfoScreen 글자) — **공지 화면 글자 크기 선택**(보통40/크게60/아주크게80). 새 plumbing 없이 기존 곡별 폰트 오버라이드(FormatData 47=pt) 재사용 — PublishNotice(text,pt)→OverrideFontSizePx. 프리셋 디코더 범위 회귀 가드. 1127 green, code-reviewer Approve(0 critical), 커밋 `450b0db`.
- (2026-06-01) ✅ 증분 16(세션 메모) — **세션 노트**(FrmMain Session Notes). WorshipSessionNotesService(세션 키별 텍스트 영속, StoreFileNaming 재사용)·MainViewModel.CurrentWorshipListName(메모 키, DI 무변경)·VM/Window + 메뉴 "도구 ▸ 세션 메모". code-reviewer MINOR 2건 반영(IO 실패 거짓성공 방지·공백 판정 일치). 1143 green, 커밋 `c02f744`.
- (2026-06-01) ✅ 증분 17(전환 확장) — **줌/회전/뒤집기 전환**(트랜스폼 기반). LyricsTransitionKind += ZoomIn/ZoomOut/Spin/FlipH/FlipV. OutputWindow OnSceneChanged 를 TransformGroup{Translate,Scale,Rotate}+중앙 원점으로 재작성(Fade/Slide 무회귀). 메뉴 모션 9종. 1144 green, code-reviewer Clean(무회귀 전수 확인), 커밋 `f653ed5`.
- (2026-06-01) ✅ 증분 18(셰이프/마스크 전환) — **원형·사각 리빌 + 와이프 4방향**. 2-레이어 엔진 없이 단일 레이어 WPF Clip 지오메트리 애니메이션(EllipseGeometry 반지름·RectAnimation)으로 구현. enum += RevealCircle/RevealRectangle/Wipe×4, 메뉴 모션 15종. code-reviewer MAJOR(빠른 전환 시 이전 Completed 가 다음 클립 지우던 경쟁) 근본수정(ShouldClearClip 참조 가드 + InternalsVisibleTo 단위 테스트). 1146 green, 커밋 `f472d81`.
- (2026-06-01) ✅ 증분 19(다중 타일 마스크) — **블라인드 가로/세로 + 체커보드**. 단일 레이어 GeometryGroup(여러 RectangleGeometry) 클립 애니메이션 — BuildBlinds(8띠)·BuildCheckerboard(10×6 셀 2단계) + remaining 카운트다운 클립 정리. 빌더 internal static 단위 테스트(개수·커버리지·타이밍). enum += Blinds×2/Checkerboard, 메뉴 모션 18종. 1149 green, code-reviewer Approve(0 critical). 커밋 `ebd8c0a`.
- (2026-06-01) ✅ 증분 20(셰이프 전환 마무리) — **다이아몬드 + 양문 열기/닫기**. Diamond=마름모 PathGeometry 중심 배율 확대(AnimateScaledShapeClip 일반 메커니즘, (w+h)/2+1 오버스캔으로 전체 덮음)·Doors=2분할 클립(AnimateTileClip 재사용). 끝에 화면 전체를 덮어 잔여 마스크 없음. enum += Diamond/Doors×2, 메뉴 모션 21종. Diamond FillContains·Doors 커버리지 단위 테스트. 1152 green, code-reviewer Approve(MAJOR AA 슬라이버 반영). 커밋 `3d0e5e9`.
- (2026-06-01) ✅ 증분 21(별 전환) — **별(Star) 모션**. 별의 안쪽 골 반지름을 모서리 거리+1 로 잡으면 단일 레이어로도 끝에 전체를 덮음(골이 경계 전역 최소점이라 노치 없음 — 해석·수치 증명, code-reviewer 검증). 기존 AnimateScaledShapeClip 재사용. enum += Star, 메뉴 모션 22종. FillContains(모서리·골방위) 단위 테스트. 1153 green, code-reviewer Approve(0 critical). 커밋 `e9d1f2f`.
- (2026-06-01) ✅ 증분 22(2-레이어 엔진 + 십자) — **오목 도형 전환용 2-레이어 출력 엔진**(범위 한정). SceneChanging 이벤트(새 콘텐츠 전 발화)→뷰가 옛 프레임 RTB 스냅샷→PreviousLayer(ContentArea 뒤). 십자(Cross)가 코너에 옛 화면을 남기고 완료 시 새 화면 전환. **기존 22 단일레이어 전환 무회귀**(PreviousLayer 기본 Collapsed, Cross+duration>0 만 스냅샷). code-reviewer MAJOR 2건 근본수정(UpdateLayout 결정적 캡처·영상 송출 시 단일레이어 강등). enum += Cross, 메뉴 모션 23종. 1155 green, 커밋 `efbe6aad`.
- (2026-06-01) ✅ 증분 23(엔진 일반화) — **나비넥타이(BowTie)**. 2-레이어 엔진 위에 오목 도형 추가(좌/우 삼각형, 상/하 중앙 오므라듦). RequiresPreviousLayer 에 BowTie 추가·BuildBowTie·AnimateScaledShapeClip 재사용. 메뉴 모션 24종. FillContains 단위 테스트. 1156 green. 커밋 `1f7648d`.
- (2026-06-01) ✅ 증분 24(하트) — **하트(Heart)**. 2-레이어 엔진 위에 매개변수 곡선 폴리라인(90점)으로 정확한 하트 도형 추가(베지어 추정 없음). 위 노치·코너는 옛 프레임, 완료 시 새 화면. 메뉴 모션 25종. FillContains(본체·코너·노치·닫힘) 단위 테스트. 1157 green. 커밋 `f909666`.
- (2026-06-01) ✅ 증분 25(각도 스윕 메커니즘) — **시계 와이프(Wedge)**. 마지막 전환 메커니즘. 호 각도 애니메이션을 진행도 DP 콜백으로 프레임마다 부채꼴 재생성. 0→360° 스윕(360° 전체 덮음). _wedgeActive+BeginAnimation(null) 이중 가드(빠른 전환·누수 방지, ApplyClipReveal/즉시컷/OnClosed). 메뉴 **모션 26종 — 모든 전환 메커니즘(트랜스폼·단일클립·타일·2레이어 도형·각도스윕) 망라**. BuildWedge FillContains 단위 테스트. 1159 green, code-reviewer Approve(0 critical, MINOR 클록 분리 반영). 커밋 `5a7e85e`. **남은 도형(P2, 장식)**: Spiral(Wedge+회전)·WindMill(다중 부채꼴)·FanUp 등 Wedge 변형.
- (2026-06-01) ✅ 증분 26(각도 스윕 변형) — **나선(Spiral)·바람개비(WindMill)·부채 펼침(FanUp)**. 증분 25 Wedge 엔진을 일반화: BuildWedgeFigure(공통 부채꼴 PathFigure) 추출 → BuildWindMill(4날개 90°간격, FillRule.Nonzero 로 경계선 메움)·BuildFan(하단중앙 꼭짓점 ±halfAngle, 90°→상반평면=전체)·BuildWedgeShape(종류별 디스패처). Spiral 은 클립 스윕 + 콘텐츠 -360° 회전(TransitionStart) 합성. 끝에 전체 덮어 2-레이어 불필요(WindMill/FanUp 전체커버 + 경계선·하단모서리 seam FillContains 단위 테스트로 증명). enum += Spiral/WindMill/FanUp, 메뉴 **모션 29종**. code-reviewer Approve(0 critical/major, MINOR seam 테스트·StopWedge 주석 반영). 1163 green. Release DLL 심볼 검증(BuildWindMill/BuildFan/BuildWedgeShape + enum 3종 메타데이터 확인).
- (2026-06-01) ✅ 증분 27(코드/악상 표시 + mis-bind 근본수정) — **"코드 표시"(Show Notations) 운영 기능**(FrmMain ShowNotations, 가사 위 코드 줄 송출). `LyricsDisplayFormatter.ExpandNotations`(off=원시 그대로=무회귀, on=`가사 » 코드`→`코드\n가사`)·`LiveQueueItem.ShowNotations`·`ComputeBodyText` 전처리·`ResolveLiveProjection` 에서 설정 주입·운영 중 토글 시 라이브 곡 재송출(MainViewModel). **근본 결함 수정**(gap §3.7): 상태 라벨("LIVE")이 `LyricsMonitorShowNotations` 에 mis-bound 돼 기본값에서 LIVE 라벨이 사라지던 버그 — 라벨을 밴드 가시성에 위임하고 죽은 `NotationVisibility` 속성·씬 필드(2개 positional record) 제거. 절 수는 켜고/끄고 무관(경계 패리티 단위 테스트). code-reviewer MAJOR 2건(기본값 on→off 무회귀·재송출이 선택 아닌 라이브 절 사용)+MINOR 1건(마커 줄 '»' 분리) 근본수정 + 재검증 에이전트로 종결. 1178 green. Release DLL 심볼 검증(ExpandNotations·RepublishLiveSongForBodyChange·ShowNotations 존재, NotationVisibility 제거 확인). **남은 한계(정직)**: 코드는 가사와 같은 글꼴 윗줄 송출 — 음절 단위 모노스페이스 정밀 정렬은 후속.
- (2026-06-01) ✅ 증분 28(성경 typed-reference) — **구절 직접 입력 점프**(FrmMain 인라인 성경 BibleUserLookup, gap §4 🔴 해소). `BibleReferenceParser`(순수 파서: "창 1:1"·"창 1:1-5"·"창 1:1-2:3"·"John 3:16"·"1요한 4:7-8"·'.' 구분자·공백 관대, 끝에서 숫자 꼬리를 잡아 숫자로 시작하는 책 이름도 안전; 책 토큰 없음·0·역순 범위 거부)+`ResolveBook`(번호·정확·접두·포함, 공백 무시 "1 John"="1John"). `BibleViewModel.JumpToReference`(파싱→책 해석→LoadBook→절 위치로 본문 오프셋 계산→`BuildSelection` 재사용으로 SelectionChanged·선택 일관). 성경 탭에 입력창+이동 버튼(Enter 지원)·본문 범위 하이라이트·예배순서 추가. 드래그 선택 흐름 무회귀. code-reviewer Approve(0 critical/major, MINOR 3건 — ValidationMessage 선클리어·동률 해소 문서화·숫자 책 토큰 핀 — 반영). 1208 green. Release DLL 심볼 검증.
- (2026-06-01) ✅ 증분 29(예배 순서 검증) — **라이브 송출 전 깨진 PPT·미디어 파일 감지**(FrmMain ValidateWorshipListItems, gap §5 Top10 #6). `LiveItemKindMatcher`(PPT/미디어/장치 별칭 매처 — MainViewModel 중복 어휘 단일화)·`WorshipListValidator`(주입 가능한 fileExists 로 디스크 없이 테스트; PPT 빈 경로=MissingFilePath, 존재 안 하는 파일=FileNotFound, 입력 장치·곡·성경·공지는 제외). MainViewModel `ValidateWorshipListCommand`+`WorshipListProblems`+`HasWorshipListProblems`(StatusText 요약+좌측 패널에 전체 문제 목록 표시). 도구 메뉴+예배순서 패널 검증 버튼. GoLive 무영향(순수 추가). code-reviewer Approve(0 critical/major, MINOR 2건 — 텔레메트리 succeeded 의미 명확화·문제 목록 UI 노출 — 반영). 1219 green. Release DLL 심볼 검증. **남은 차원(정직)**: 곡 DB 존재("삭제된 곡") 검증은 비동기·DB 의존이라 후속.
- (2026-06-01) ✅ 증분 30(라이브 조옮김) — **운영 중 코드 조옮김 ±반음**(FrmMain Transpose Up/Down Semi-Tone·To Capo 0, gap §3.7/§2.2). 기존 3조각 합성: `ChordTransposer`(증분 5)+`ExpandNotations` 코드 렌더(증분 27)+`RepublishLiveSongForBodyChange`(증분 27). ExpandNotations 에 transpose 인자 추가(0=무회귀 비트동일)·`LiveQueueItem.TransposeSemitones`·`ComputeBodyText` 전달·`ResolveLiveProjection` 에서 VM 값 주입. MainViewModel `TransposeLiveUp/Down/ResetCommand`+`LiveTransposeSemitones`(±11 클램프, ±12=옥타브라 원조 동일 회피)+`LiveTransposeLabel`. 변경 시 라이브 곡 재송출(코드만 이동, 가사 불변). **새 곡 송출 시 원조(0) 초기화**(절·슬라이드 이동은 조옮김 유지). 출력 메뉴 "코드 조옮김 ▲/▼/원조". code-reviewer Approve(0 blocking, MAJOR 후보는 자체 검증으로 비결함 강등). 3개 비차단 후속 반영(±11 근거 주석·공지 경로 주석·A~G 시작 단어 한계 테스트 핀). 1229 green. Release DLL 심볼 검증.
- (2026-06-01) ✅ 증분 31(P0 더미 큐 제거) — **시작 시 빈 큐 + 빈 상태 안내**(gap §5 Top10 #7 / §7 P0). 과거 시작 시 가짜 3항목을 시드하던 `SeedPlaceholderQueue` 제거 → 운영자가 직접 추가하거나 "최근 예배 순서"로 불러온다. `IsQueueEmpty`(Queue.CollectionChanged 단일 소스로 모든 변경 경로 반영) + WorshipListPanel 빈 상태 오버레이(IsHitTestVisible=false). 테스트는 CreateSut 가 기본 시드(seedSampleQueue 파라미터)로 기존 ~25개 보존, 빈 시작·토글 신규 핀. code-reviewer Approve(0 issue — 모든 mutation 경로·SelectedItem null 가드·시드 동등성 전수 확인). 1231 green. Release DLL 심볼 검증(IsQueueEmpty 존재, SeedPlaceholderQueue 제거 확인).
- (2026-06-01) ✅ 증분 32(배경 표시 모드) — **출력 배경 이미지 채움/맞춤/가운데/타일**(FrmMain Def_ImageMode: Tile/Centre/BestFit, gap §5 Top10 #9 / §2.2 / §3.2). enum `LyricsBackgroundMode` 설정 전배선(All·FindChangedKeys 타입스위치·GET/SET·DTO·LegacyMap·Validate enum 가드) + OutputRenderer 씬(LiveOutputRenderSettings·OutputSceneSnapshot) + OutputWindowViewModel `BuildBackgroundImageBrush`(모드별 ImageBrush Stretch/TileMode/Viewport, Freeze) + OutputWindow.xaml Image→Rectangle(Fill=브러시) + MainViewModel `ApplyBackgroundModeCommand`·`ActiveBackgroundMode`·체크 메뉴 4종. 기본 Fill=UniformToFill 무회귀. 라이브 즉시 반영(화이트리스트). code-reviewer Approve(0 critical/major, MINOR 2건 — Tile 비-96dpi 픽셀 뷰포트·Validate enum 가드 — 반영). 1241 green. Release DLL 심볼 검증.
- (2026-06-01) ✅ 증분 33(미디어 폴더 브라우저) — **폴더의 동영상·오디오를 골라 예배 순서에 추가**(FrmMain Media 탭, gap §3.3). 증분 12 PowerPoint 폴더 브라우저를 1:1 미러링: `MediaLibraryService`(13확장자, FolderFileEnumerator 재사용)·`MediaLibraryViewModel`·`MediaLibraryWindow`(폴더 변경·하위폴더 토글·더블클릭 추가) + 파일 메뉴 "미디어 폴더 찾아보기". 초기 폴더는 MediaDirectory→작업폴더→내문서. AddMedia(기존 외부파일 추가 경로와 동일) 위임. code-reviewer Approve(0 issue, 변경 요청 없음 — 미러 충실도·확장자·AddMedia 통합 전수 확인). 1263 green. Release DLL 심볼 검증.
- (2026-06-01) ✅ 증분 34(라이브러리 초성 점프) — **곡 목록 A/B/C(초성) 점프**(FrmMain 곡 목록 점프, gap §3.3). `SongInitialGrouping`(순수 헬퍼 — 한글 초성·쌍자음 정규화·영문·숫자#·기타, PraiseBookIndexService 의 중복 로직을 추출·공통화)·`LibraryViewModel.AvailableInitials`/`JumpToInitialCommand`/`JumpToInitial`(표시 곡 중 첫 일치 선택) + 라이브러리 탭 머리글자 점프 바(WrapPanel). PraiseBook 색인 리팩터(동작 동일, 기존 테스트 보존). code-reviewer Request-minor → MINOR 2건(ClearLibrary 가 점프 바 안 비우던 stale 버그·빈 제목 점프 필터 일치) 근본수정 + 회귀 테스트. 1282 green. Release DLL 심볼 검증. 정렬(획수/번호)·Use Song Numbering 은 후속.
- (2026-06-01) ✅ 증분 35(곡 목록 정렬) — **원래순서/제목순/곡번호순 정렬**(FrmMain 곡 목록 정렬, gap §3.3). `SongOrdering`(순수: Original=입력순 보존·Title=Ordinal 한글 가나다·Number=번호순+제목+SongId 결정적)·`LibrarySortMode`/`LibrarySortOption`·`LibraryViewModel.SortMode`(DB 재조회 없이 표시만 재정렬, 검색·초성점프와 합성)·라이브러리 탭 정렬 콤보. 기본 Original=무회귀. code-reviewer Request-changes → **MAJOR 근본수정**(정렬 중 곡 수동 이동이 화면≠DB 순서로 엉뚱하게 저장되던 문제 — 버튼 CanExecute + 모든 이동의 길목 `MoveSelectedSongToIndexAsync` 단일 가드로 드래그까지 차단, 재검증 에이전트가 write 경로 단일성 확인) + MINOR 2(번호 정렬 SongId 타이브레이크·Ordinal 의도 주석). 1290 green. Release DLL 심볼 검증. 획수/CJK 정렬·Use Song Numbering 은 후속.
- (2026-06-02) ✅ 증분 36(곡 번호 표시) — **Use Song Numbering**(FrmMain Edit 메뉴, gap §2.2/§3.3). 라이브러리 곡 목록에 "번호. 제목" 표시. `general.useSongNumbering` 설정 전배선(SettingKey·All·GeneralSettings DTO·GET/SET·LegacyMap·MigrateLegacyAsync)·`SongListFormatting`(순수)·`SongRowDisplayConverter`(MultiBinding: 제목+번호+표시여부)·`LibraryViewModel.UseSongNumbering`(설정 동기화, 토글 시 저장)·보기 메뉴 체크 토글. 기본 off=제목만(무회귀). code-reviewer Approve(0 critical/major, MINOR 1건 — 마이그레이션 대칭성 — 반영+테스트). 1302 green. Release DLL 심볼 검증. 획수/CJK 정렬만 §3.3 후속.
- (2026-06-02) ✅ 증분 37(처음/마지막 항목 이동) — **First/Last 예배 순서 이동**(FrmMain Next/Prev/First/Last 중 미구현분, gap §3.1). MainViewModel `FirstItemCommand`/`LastItemCommand`(Next/Prev 패턴 미러 — First=CanMovePrevious·Last=CanMoveNext 재사용, 라이브면 PublishSelectedItem 송출)·NotifyCommandStates 등록·메뉴/버튼(처음·마지막). code-reviewer Approve(0 issue, "Ship it" — CanExecute·NRE 가드·라이브 송출 경로·이중 advance 없음·무회귀 전수 확인). 1305 green. Release DLL 심볼 검증.
- (2026-06-02) ✅ 증분 38(성경 본문 우클릭 + 공지 화면으로 복사) — **인라인 성경 우클릭 메뉴**(gap §4 line 224·225 🔴 2건 해소). 본문 TextBox 컨텍스트 메뉴(전체 선택·복사·선택 구절 예배 순서 추가·공지 화면으로). "공지 화면으로"는 `NoticeScreenViewModel` 초기 텍스트(initialText)로 편집기를 프리필해 연다(즉시 송출 아님 — 운영자 검토 후 송출, 더 안전). `ResolveCopyText`(순수: 선택 우선·없으면 전체·둘 다 비면 null·trim) 추출·테스트. code-reviewer Approve(0 critical/major) — CommunityToolkit 백킹필드 ctor 패턴·읽기전용 TextBox Copy/SelectAll·무회귀 전수 확인. MINOR(폴백 로직 추출 테스트) 반영. 1312 green. Release DLL 심볼 검증. AddRegion2 우클릭은 후속.
- (2026-06-02) ✅ 증분 39(Display Panel 투명 배경) — **정보 패널 투명 토글**(FrmMain Def_PanelTransparent, gap §2.2). on 이면 곡번호·저작권·다음항목·위치 인디케이터 밴드의 반투명 검정(#66000000) 배경을 없애 슬라이드 위에 바로 보인다. `LyricsMonitorPanelTransparent` 설정 전배선(All·DTO·GET/SET·LegacyMap·LiveOutputRenderSettings·OutputSceneSnapshot 두 positional record 중간 삽입)·`OutputWindowViewModel.PanelBackgroundBrush`(Freeze 공유, Transparent vs 기본)·4밴드 바인딩·`TogglePanelTransparentCommand`·보기 메뉴 토글. 기본 off=기존 밴드(무회귀). code-reviewer Approve(0 critical/major — positional 4사이트 정렬·무회귀 비트동일·라이브 경로 전수 확인). MINOR(상태바 라벨 fallthrough — 헬퍼 자체 규약 위반)+테스트 반영. 1316 green. Release DLL 심볼 검증.
- (2026-06-02) ✅ 증분 40(데이터베이스 압축·정리) — **Compact & Repair**(FrmMain Tools, gap §3.8 🔴 해소). `AdminDatabaseRepository.CompactDatabaseAsync`(스키마 분석 → 빈 backupRoot 거부 → 백업 먼저 생성 → 트랜잭션 밖 `VACUUM;`(SQLite 제약 — VACUUM 은 트랜잭션 내 불가) → 실패 시 백업 복원·보고 → 성공 보고)·`AdminDatabaseWriteOperation.Compact`·11개 테스트 페이크를 안 건드리려 인터페이스 DIM(기본 미지원 보고) 도입·`LibraryViewModel.CompactDatabaseCommand`(IsBusy·쓰기가능 경로 가드, "압축했습니다(백업 먼저)" 상태)·도구 메뉴 "데이터베이스 압축·정리". code-reviewer Approve(0 MAJOR/MINOR 정확성 — "Ship it") + 선택 관찰(빈 backupRoot·복원 분기 직접 테스트)을 실제 VACUUM/누락DB/빈backupRoot 3테스트로 반영. 1321 green. Release DLL 심볼 검증(CompactDatabaseAsync/CompactDatabaseCommand/CompactDatabase 존재). Clear All Formatting·Clear Registry·Listing of Folder 는 §3.8 후속.
- (2026-06-02) ✅ 증분 41(성경 본문 송출 + 이중 언어) — **회중 화면에 성경 구절 본문 실제 송출**(gap §4 line 228/235 🟡→🟢, 최대 성경 갭). 과거 성경 항목은 IdString·제목만 들고 **본문이 화면에 안 나왔다**(GoLive 시 제목만). `BibleSelectionId`(순수 파서: "종류;주버전;보조버전;책;시작장;시작절;끝장;끝절;…" → 범위·버전 파일)·`BiblePassageComposer`(순수 조립: 절마다 한 페이지, 보조 언어는 `[region 2]` 마커로 합쳐 기존 곡 이중 언어 출력 렌더 재사용)·`BibleRepository.ExpandSelection`(interface DIM + 실제 — 범위별 구절 읽어 조립)·`BibleViewModel.ExpandSelectionBody`·`MainViewModel.AddBibleSelection` 본문 적재. 절 페이지네이션을 곡 전용 → **곡+성경 일반화**(`IsLyricsPaginated`, RefreshLyricsPages·ComputePositionLabel·AdvanceAutoRotation — 절 이동·위치 라벨·자동 회전이 성경에도 동작). code-reviewer가 잡은 **MAJOR 근본수정**: 일부 번역의 인용부호 »…« 가 곡 코드 마커(»)로 오인돼 본문이 잘리던 결함 — 성경은 ExpandNotations(코드 전처리) 건너뛰고, 분할 동안만 '»'를 사적 영역 문자로 보호(`GuardLiteralNotation`/`UnguardLiteralNotation`, 본문·절 수 계산 동일 입력)했다가 복원(정규화 먼저→보호 순서로 "Â»" 모지바케도 정리). MINOR(ArgumentException 폴백) 반영. 단일/이중 언어·인용부호·모지바케 회귀 테스트. 1351 green, code-reviewer 재검증 Approve(0 critical/major, 라운드트립·곡/공지 무회귀 전수 확인). Release DLL 심볼 검증(ExpandSelection·BibleSelectionId·BiblePassageComposer·GuardLiteralNotation 존재). **남은 성경 갭(정직)**: 본문 드래그→예배순서 드롭, AddRegion2 우클릭 서브메뉴 UI.
- (2026-06-02) ✅ 증분 42(성경 Add Region 2 우클릭) — **본문 우클릭으로 보조 버전을 골라 이중 언어 추가**(FrmMain `CMenuBible_AddRegion2`/`BuildBibleTextR2SubMenus`, gap §4 line 228/230 완결). 증분 41 의 동작하는 이중 언어 본문 송출 위에 운영 UI 를 얹었다: `BibleViewModel.Region2VersionOptions`(현재 주 버전 제외 후보, SelectedVersion·목록 변경 시 갱신)·`BuildSelectionWithRegion2`(드래그 선택을 주 버전으로 만든 뒤 고른 보조 버전과 합쳐 이중 IdString — 미리보기 콤보 불변, 자기 짝 가드)·본문 컨텍스트 메뉴 "Region 2와 함께 추가 ▶ [버전들]" 서브메뉴(`PlacementTarget.DataContext` 경유 VM 바인딩 + EventSetter)·`AddBibleVerseWithRegion2_Click`(기존 단일 추가 패턴 미러). 고른 이중 선택이 증분 41 `ExpandSelectionBody` 로 흘러 한/영 본문이 실제 송출. code-reviewer Approve(0 critical/major — ContextMenu 바인딩 해석·변경 알림 완전성·삼중 가드·IdString 라운드트립 확인). MINOR(비선택 버전 이름 변경 시 후보 갱신) 반영. 단일/이중·자기짝·후보 제외 테스트. 1354 green. Release DLL 심볼 검증(BuildSelectionWithRegion2·Region2VersionOptions·AddBibleVerseWithRegion2 존재). **남은 성경 갭(정직)**: 본문 드래그→예배순서 드롭.
- (2026-06-02) ✅ 증분 43(성경 본문 드래그→예배순서 드롭) — **본문에서 선택 구절을 끌어 큐에 직접 추가**(FrmMain `BibleText_MouseDown` DragDrop, gap §4 line 231 🟢). 기존 큐 재정렬 드래그-드롭(WorshipListPanel) 인프라에 성경 선택 추가 경로를 얹었다: 본문 TextBox 에서 **이미 선택된 글자 위를 눌러 끌면**(새 선택 제스처와 충돌 안 하게 무장 조건 — snapToText:false 로 빈 영역 헛-무장 차단) `BibleSelection` 을 실어 드래그 → 큐에 드롭하면 **타깃 항목 앞에 추가**. `AddBibleSelection` 을 `CreateBibleItem`(본문 확장 공유)로 리팩터하고 `AddBibleSelectionRelativeTo`(드롭 위치 삽입) 추가. code-reviewer가 잡은 **MAJOR 근본수정**: 삽입 위치를 값-동등 `Queue.IndexOf` 로 찾아 같은 값 중복 항목(입례/봉헌 반복)에서 엉뚱한 위치에 들어가던 결함 — 재정렬 경로와 동일하게 **참조 일치(`IndexOfReference`)** 로 "드롭한 바로 그 인스턴스" 위치를 고정(중복 타깃 회귀 테스트 추가). MINOR(snapToText 헛-무장) 반영. 1358 green, code-reviewer Request-changes→재수정 후 해소. Release DLL 심볼 검증(AddBibleSelectionRelativeTo·CreateBibleItem·BiblePassageBox_PreviewMouseMove 존재). **성경 영역 포팅 일단락**(본문 송출·이중 언어·AddRegion2 UI·드래그 드롭).
- (2026-06-02) ✅ 증분 44(PraiseBook 인터랙티브 목록) — **찬양집 색인에서 곡을 더블클릭하면 예배 순서에 추가**(FrmMain PraiseBook 운영 목록, gap §7 P0 #8). 기존 PraiseBook 창(머리글자 색인·명명 저장/열기/삭제·HTML 내보내기)은 표시 전용이었다 — 이제 각 곡 줄을 더블클릭하면 라이브러리에서 그 곡(가사 포함)을 찾아 큐에 넣는다. `MainViewModel.AddPraiseBookSong`(해석 우선순위 SongId→제목+번호→제목, 라이브러리에 없으면 안내)·`PraiseBookIndexWindow` 항목 Border 더블클릭(`SelectedEntryForLive`, LibraryWindow 추가 패턴 미러)·MainWindow 해석 배선. code-reviewer가 잡은 **MAJOR 근본수정**: 색인 항목이 SongId 를 버려 같은 제목·번호의 다른 곡(한/영 폴더)에서 첫 일치(엉뚱한 언어)로 가던 결함 — `PraiseBookIndexEntry` 에 SongId(기본 0, 저장 DTO 불변·저장본은 0 으로 우아 저하) 추가하고 **SongId 우선 해석**. MINOR(중복 redundant Close 제거) 반영. 중복 제목 SongId 해석·저장본 폴백 회귀 테스트. 1363 green, code-reviewer Approve→재수정 후 해소. Release DLL 심볼 검증(AddPraiseBookSong·SelectedEntryForLive 존재).
- (2026-06-02) ✅ 증분 45(획수 정렬) — **곡 목록 획수순(한자) 정렬**(FrmMain 곡 목록 정렬 획/CJK, gap §3.3). 증분 35 `SongOrdering` 에 `LibrarySortMode.StrokeCount` 추가 — **번체 중국어(zh-Hant) ICU 콜레이션이 한자를 획수 순으로 정렬**한다는 점을 활용(一1·人2·三3·愛13·龍16획, 별도 획수 데이터 테이블 불필요). `StrokeComparer`(zh-Hant CompareInfo, CultureNotFoundException→Ordinal 폴백)·정렬 콤보 "획수순(한자)" 추가. 빈 제목 뒤로·SongId 결정적 타이브레이크. code-reviewer Approve(0 critical/major — 정적 초기화 안전·설정 미영속·무회귀 전수 확인, invariant 모드는 예외 throw→폴백을 실증 확인). MINOR(테스트 brittleness) 반영: globalization-invariant 모드면 획수 콜레이션이 Ordinal 로 폴백해 조용히 뒤집히므로 **콜레이션 가용성 가드로 그 환경에선 건너뛰게**(운영 ICU 환경에서만 검증). 1365 green. Release DLL 심볼 검증(StrokeCount·StrokeComparer·CreateStrokeComparer 존재). CJK 단어수 정렬만 §3.3 후속.
- (2026-06-02) ✅ 증분 46(영역별 스타일 인스펙터) — **곡 편집기에서 Region 1/2 글자색·정렬 분리 편집**(FrmMain 영역별 스타일 분리, gap §2.2). 출력 렌더는 이미 FormatData 의 영역별 코드(29/30 색·31/32 정렬)를 적용했으나 **편집 UI 가 없어** FormatData 가 불투명하게 보존만 됐다. 이제 편집기에서 설정→저장→송출까지 닫힌 고리: `SongFormatData.Encode()`(Parse 의 역, 빈 필드 생략, 라운드트립)·`HexToArgb`(#AARRGGBB/알파생략)·`SongEditorViewModel` 인스펙터 속성(Region1/2 ColorHex·Alignment)·`SyncInspectorFromFormatData`(로드 시 디코드, _loading 가드로 dirty 안 됨)·`RebuildFormatDataFromInspector`(변경 시 인코드, `_preservedFormat` 로 폰트·크기·배경·굵게비트 보존)·편집기 "영역별 스타일(Region 1/2)" Expander(색 Hex·정렬 콤보). code-reviewer가 잡은 **MAJOR 정직성 결함 반영**: 굵게/기울임은 출력이 아직 영역별로 분리 렌더하지 않으므로(전역만) **"안 보이는 효과"를 약속하지 않게 인스펙터 편집 대상에서 제외**(기존 41 비트는 _preservedFormat 로 보존 — 편집해도 안 사라짐). MINOR(0-based 비트 주석·색 비움 시 보존 필드 유지 테스트) 반영. 1380 green(Release 구성 — Debug DLL 이 실행 중 앱에 잠겨 Release 로 검증). Release DLL 심볼 검증(Encode·HexToArgb·Region1ColorHex·Sync/Rebuild 존재). **차기 증분(47): 영역별 굵게/기울임 출력 렌더 배선**(완료 후 인스펙터 체크박스 복원).
- (2026-06-02) ✅ 증분 47(영역별 굵게/기울임 출력 렌더) — **Region 1/2 굵게·기울임을 출력이 영역별로 렌더**(증분 46의 정직성 보류분 완결, gap §2.2). 곡별 FormatData 41 비트(Bold1/Italic1/Bold2/Italic2)를 라이브 송출 경로로 흘린다: `LiveSessionSnapshot` 에 nullable bool 오버라이드 4종(끝에 추가)·GoLive 에서 `Bold1==true?true:null` 적재(레거시 비트는 "켜기"만 표현)·`OutputRenderer.CreateScene` 가 Region1=오버라이드∥전역, Region2=오버라이드∥Region1추종으로 해석·`OutputSceneSnapshot` 에 LyricsMonitorBold2/Italic2(끝에 추가)·`OutputWindowViewModel.BodyFont2Weight/Style`·OutputWindow.xaml Region2 밴드 바인딩 교체. 증분 46에서 뺐던 인스펙터 굵게/기울임 체크박스 **복원**(이제 편집→인코드→영역별 렌더 닫힌 고리). code-reviewer **Approve(0 MAJOR)** — positional record 끝-추가 전수 검증(두 레코드 construction 단일 사이트·`with` 없음·중간 재배선 슬롯 정렬 확인), scene.LyricsMonitorBold 단일 소비자(BodyFontWeight) 확인, isLive 가드·상속 캐스케이드 검증. 4 렌더 캐스케이드 분기 + 인스펙터 디코드/인코드 테스트. 1384 green(Release 구성). Release DLL 심볼 검증(OverrideBold1·LyricsMonitorBold2·BodyFont2Weight 존재). **영역별 스타일 분리 = 색·정렬·굵게·기울임 완결**. Underline·ChorusOnly(후렴만)·Region2 외곽선은 후속.
- (2026-06-02) ✅ 증분 48(가사 밑줄 전역 토글) — **출력 텍스트 효과 세트 완성**(굵게·기울임·그림자·외곽선에 밑줄 추가, FrmMain Ind_R1/R2Underline, gap §2.2). 기존 `LyricsMonitorShadow` bool 설정을 9개 터치포인트 전수 미러: SettingKey·All·DTO·GET/SET·LegacyMap·LiveOutputRenderSettings+From·OutputSceneSnapshot+CreateScene(**중간 삽입** — Shadow 와 PanelTransparent 사이, 양 construction 사이트 동일 위치)·AppearanceTemplate 캡처/적용·OutputWindowViewModel.BodyTextDecorations(TextDecorations.Underline∥null)+라이브 화이트리스트·MainViewModel 토글 커맨드·"밑줄" 라벨·동기화·출력 메뉴 항목·툴바 토글버튼(밑줄 "가")·OutputWindow.xaml Region1/2 TextDecorations 바인딩. code-reviewer **Approve(0 critical/major)** — 중간 삽입 positional을 양 레코드×양 construction 사이트 ordinal 매핑으로 전수 검증(후속 필드 무shift 확인)·SET 스위치 중괄호 균형·All↔LegacyMap 패리티 테스트 무드리프트 확인. **정직한 한계(반영)**: 외곽선 모드(OutlinedTextBlock — geometry 컨트롤)는 밑줄 미렌더(굵게/기울임은 적용). 렌더 스레딩·기본 off·VM 디코레이션·템플릿 라운드트립 테스트. 1389 green(Release 구성). Release DLL 심볼 검증.
- (2026-06-02) ✅ 증분 49(영역별 밑줄) — **Region 1/2 독립 밑줄**(증분 47 영역별 굵게/기울임 + 증분 48 전역 밑줄에 이어 per-region 밑줄 완성, gap §2.2). FormatData 41 비트의 빈 자리(bit2=Region1 밑줄, bit5=Region2 밑줄)를 채워 대칭 레이아웃 완성(region1=하위3비트 Bold/Italic/Underline, region2=상위3비트). 증분 47의 굵게/기울임 캐스케이드를 그대로 미러: `SongFormatData.Underline1/2`(Parse/Encode bit2/bit5)·`LiveSessionSnapshot.OverrideUnderline1/2`(끝에 추가)·`OutputRenderer` region1=곡별∥전역(증분 48 전역 밑줄을 Region1 폴백으로 재배선, global-only 케이스 무변경)·region2=곡별∥Region1 추종·`OutputSceneSnapshot.LyricsMonitorUnderline2`(끝에 추가)·`OutputWindowViewModel.BodyText2Decorations`·OutputWindow.xaml Region2 바인딩·곡 편집기 Region1/2 밑줄 체크박스. code-reviewer **Approve(0 critical/major)** — 비트 패킹 충돌 없음(밑줄 4/32, 굵게기울임 1/2/8/16 disjoint)·전역 밑줄 재배선 true no-op 증명·positional 끝-추가 단일 사이트·캐스케이드 3분기·XAML 미스왑 확인. MINOR(stale 비트 주석) 반영. 비트 라운드트립·렌더 캐스케이드·VM 디코레이션·편집기 디코드/인코드 테스트. 1395 green(Release 구성). Release DLL 심볼 검증. **per-region 텍스트 효과 = 색·정렬·굵게·기울임·밑줄 완결.**
- (2026-06-02) ✅ 증분 50(외곽선 모드 밑줄) — **외곽선 폰트 효과에서도 밑줄 렌더**(증분 48/49의 정직한 한계 해소, gap §2.2). 외곽선 본문은 일반 TextBlock 이 아니라 `OutlinedTextBlock`(FormattedText.BuildGeometry 로 글자 형상 생성 후 Fill+Stroke)이라 TextDecorations 가 안 먹던 문제: `Underline` DependencyProperty(ShapeChange — 형상 캐시 무효화) 추가, `EnsureFormatted` 에서 on 이면 `FormattedText.SetTextDecorations(Underline)` → BuildGeometry 가 밑줄 사각형까지 형상에 포함해 외곽선·채움과 같은 색으로 그린다. `OutputWindowViewModel.BodyUnderline`(외곽선 컨트롤용 bool, Region1 밑줄값)·OutputWindow.xaml 바인딩. code-reviewer **Approve(0 critical/major)** — SetTextDecorations→BuildGeometry 흐름·캐시 무효화·desync 불가(동일 소스 동일 블록)·무회귀(기본 off short-circuit) 확인. **실제 렌더 픽셀 카운트 테스트**(밑줄 on 이 off 보다 그려지는 픽셀 많음 — 형상 경로 직접 검증) + VM bool 테스트. MINOR(밑줄 stroke 하단 잘림 헤드룸) 반영(Padding 2→3px). 1396 green(Release 구성). Release DLL 심볼 검증. **텍스트 효과 = 전 모드(일반·그림자·외곽선) 밑줄 완결.**
- (2026-06-02) ✅ 증분 51(이중 언어 영역 표시 모드) — **Region 1만/2만/둘다 표시**(FrmMain Def_ShowRegion1/2/Both, gap §2.2). enum `LyricsRegionDisplay`(Both/Region1Only/Region2Only) 설정 전배선(LyricsBackgroundMode 미러 — SettingKey·All·DTO·Validate·FindChangedKeys 타입스위치·GET/SET·LegacyMap·LiveOutputRenderSettings+From·OutputSceneSnapshot+CreateScene)·라이브 화이트리스트·MainViewModel ApplyRegionDisplayCommand+체크 3종·출력 메뉴 "이중 언어 영역 표시" 서브메뉴. 핵심 로직은 scene 의 `ShowsBodyText`/`ShowsBodyText2` 가시성 계산에 영역 모드 반영(출력 VM 가시성·외곽선 경로 모두 scene 프로퍼티 경유라 즉시 적용). **안전장치**: 한쪽만 모드여도 다른 영역 본문이 비면 남은 본문을 유지(어느 모드도 내용 있는 곡을 빈 화면으로 안 만듦). code-reviewer **Approve(0 critical/major)** — 가시성 진리표 전수·enum 전배선·positional 끝-추가·Both 무회귀(공허참 환원) 증명. MINOR(Region1Only×보조만 있는 곡 빈 화면 비대칭) **근본수정** — Region2Only 안전장치를 Region1Only 에도 대칭 적용(불변식 완전화) + 회귀 테스트. 1402 green(Release 구성). Release DLL 심볼 검증.
- (2026-06-02) ✅ 증분 52(강조 후렴만) — **굵게·기울임·밑줄 강조를 후렴 절에만**(FrmMain Ind_*Italics 후렴만, gap §2.2). `LyricsDisplayFormatter.IsChorusLabel`(순수 — C·Chorus·Refrain·후렴 인식, 모호한 한 글자 "렴"은 제외)·전역 설정 `LyricsMonitorEmphasisChorusOnly`(bool 전배선)·`LiveSessionSnapshot.CurrentPageIsChorus`(GoLive 에서 현재 절 라벨로 계산)·`OutputRenderer.CreateScene` 가 후렴만 on + 비후렴 절이면 region1/2 굵게·기울임·밑줄을 끔(색·정렬은 강조 아니므로 무관)·라이브 화이트리스트·출력 메뉴 토글. code-reviewer가 잡은 **MAJOR 근본수정**: 이중 언어 `GetRegionSectionLabels` 가 라벨 없는 머리말 절을 빠뜨려 라벨↔페이지가 어긋나면 엉뚱한 절을 후렴으로 오인하던 결함 — MainViewModel 절 점프 바와 동일한 **라벨수==페이지수 정렬 가드**로 어긋나면 후렴 판정 비활성(안전 저하). 이중 언어 머리말 어긋남 회귀 테스트. MINOR(모호한 "렴" 제외) 반영. IsChorusLabel·후렴 감지·렌더 게이트(후렴/비후렴/off) 테스트. 1421 green(Release 구성). Release DLL 심볼 검증.
- (2026-06-02) ✅ 증분 53(이중 언어 줄 교차) — **Region 1·2 인터레이스**(FrmMain Def_Interlace, gap §2.2). on 이면 원문(Region1)·번역(Region2) 본문을 줄 단위로 번갈아 송출 — 각 줄이 자기 영역의 색·글꼴·크기·굵게·기울임·밑줄·정렬·줄높이를 그대로 지닌다(per-line 충실 렌더). `LyricsMonitorInterlace` bool 전배선·`OutputSceneSnapshot.ShowsInterlace`(인터레이스 on + 두 영역 다 보일 때만 — 영역 표시 모드/단일 언어와 자동 호환)·`InterlacedLine` 레코드(줄별 스타일)·`OutputWindowViewModel.RebuildInterlacedLines`(영역별 VM 값으로 줄 교차 빌드 + 기존 두 밴드 숨김)·OutputWindow.xaml ItemsControl·라이브 화이트리스트·MainViewModel 토글·출력 메뉴. code-reviewer **Approve(0 blocking)** — ApplyScene 속성 순서 의존성(모든 참조 VM 값이 rebuild 전 설정됨) 전수 검증·gap 로고/제목 상호작용·ShowsInterlace↔RegionDisplay 합성·positional 끝-추가·줄 수 불일치 처리 확인. MINOR 3건(그림자·줄높이·트리밍 누락) **반영** — 줄높이를 InterlacedLine 에 싣고 그림자를 목록 전체 DataTrigger 로(레거시 UseShadowFont 보존)·CharacterEllipsis 추가. 씬 ShowsInterlace·VM 줄 교차 빌드/순서/밴드 숨김·단일 언어 비활성 테스트. 1427 green(Release 구성). Release DLL 심볼 검증.
- (2026-06-02) ✅ 증분 54(항목별 개별 서식 사용) — **항목별 "개별 서식 사용" 토글**(FrmMain Ind_checkBox "Use Individual Settings", gap §2.2). `LiveQueueItem.UseIndividualFormatting`(기본 true=무회귀). off 면 `ResolveLiveProjection` 이 그 항목의 FormatData 를 비워 전역 기본 색·정렬·폰트·배경으로 송출. 예배 순서 우클릭 컨텍스트 메뉴 토글(PlacementTarget 경유 바인딩, incr 42 패턴)·`WorshipListStore` DTO 에 FormatData+플래그 영속(구버전 JSON 안전 기본값). code-reviewer가 잡은 **MAJOR 근본수정**: 토글 시 SelectedItem 재할당이 RefreshLyricsPages 로 LyricsPageIndex 를 0 으로 리셋해 라이브 다중 절 곡이 1절로 튀던 결함 — VM 인덱스가 아니라 세션의 실제 라이브 절을 쓰는 `RepublishLiveSongForBodyChange`(기존 동일 함정 해결책)로 재송출(다중 절 절 유지 회귀 테스트). 토글·라이브 게이팅(색 적용/제거)·다중 절 절 유지·스토어 라운드트립·구버전 기본값 테스트. 1432 green(Release 구성). Release DLL 심볼 검증.
- (2026-06-02) ✅ 증분 55(송출 후 다음 항목) — **btnToOutputMoveNext 2단 전송**(FrmMain Preview→Output 전송 분리, gap §3.1 line 123). `SendToOutputAndNextCommand` — 선택 항목을 출력에 송출하고 곧바로 다음 항목으로 선택을 옮긴다(전역 "자동 다음" 설정과 무관). `PublishSelectedItem(autoAdvance)` 리팩터 + 이동 로직을 `AdvanceSelectionToNext`(설정 무관, PPT 제자리)로 추출하며 값-동등 `Queue.IndexOf`→참조 일치 `IndexOfReference` 로 교체(중복 항목 footgun 정리, 다른 참조-기반 메서드와 일관). 출력 메뉴 "송출 후 다음 항목". code-reviewer **Approve(0 critical/major)** — 기존 5개 호출부 무회귀(autoAdvance 기본 true)·이중 advance 구조적 불가·published 캡처 안전·value→reference 교체 strictly safer·CanGoLive 게이트·안전 확인 경로 전수 확인. 자동다음 off 에도 advance·마지막 항목 제자리 테스트(중복-인스턴스 테스트는 SelectedItem 의 값-동등 특성상 도달 불가로 제거 — 정직). 1434 green(Release 구성). Release DLL 심볼 검증.
- ✅ **출력 메뉴 서식 파리티 완비** — 표시 토글 7(위치·곡번호·제목헤딩·외곽선·그림자·굵게·기울임) + 저작권 + 다음 항목 + 전환 효과(페이드/속도) + 배경 이미지 + 가로/세로 정렬 + 글자 크기 + 줄 간격(전부 인스펙터와 동일 설정 공유, 마우스 운영자도 메뉴로 전부 제어).
- **차기(후속 슬라이스): PraiseBook 명명 저장·관리 영속화 · 이미지 썸네일 비동기 디코딩(대용량) · 50여 종 셰이프/타일 전환 · InfoScreen 편집기 · 절 라벨 점프(P2 백로그).** 각 슬라이스를 단계별 빌드+code-review로 무회귀 검증하며 진행.

> 현황(정직): FrmMain 전체 포팅은 다증분 장기 과제다. UI/UX 표면(메뉴·단축키·상태바) + 운영 기능(절 점프) + **최대 갭이던 이중언어 송출이 이제 동작**한다(슬라이스 2). 남은 본질 갭: 영역별 스타일 분리·Display Panel·조옮김·전환 UI·이미지/PraiseBook 브라우징. "완벽 포팅"은 증분 3·5~10 완료 시 달성.

- (2026-06-02) 📝 **갭 문서 정직성 재조정(reconciliation)** — 코드 구현이 실제로 끝났는데 갭 문서가 🔴(미포팅)로 남아 있던 stale 마커 8건을 grep/codegraph로 교차 검증 후 정정. 세션 메모 편집(WorshipSessionNotesWindow 존재 → 🟢), 이미지 갤러리(ImageLibraryWindow 존재 → 🟢), 라이브러리 정렬+A/B/C 초성 점프(LibraryViewModel JumpToInitialCommand·SortOptions[원래/제목/곡번호/획수순] 존재 → 🟢), 절/슬라이드 라벨 점프(🟢), 코드/악상 표시(ExpandNotations 존재 → 🟢), Worship Sessions/Recent Edits/Session Notes(🟢), InfoScreen(NoticeScreenWindow 텍스트 편집·송출 존재 → 🟡 부분, .esi 풀 편집기만 후속). **세션 메모 중복 구현을 시작했다가 grep으로 기존 구현을 발견 → 중복 4파일 삭제·DI 롤백한 사건**의 후속 정리. 교훈: 새 기능 착수 전 codegraph/grep 으로 기존 구현 여부 필수 확인. 코드 무변경(문서 전용) → 1434 green·Release 0 errors 유지.

- (2026-06-02) ✅ 증분 56(출력 본문 여백) — **LyricsMonitorBodyLeft/Right/BottomMargin**(FrmMain ShowLeftMargin/ShowRightMargin/ShowBottomMargin, gap §2.3 line 87). 3개 int 설정(기본 0·범위 0~400px)을 전 플러밍 경로에 배선: SettingsService(SettingKey·All·DTO·GetValue·SET·Validate RequireRange) + LegacySettingsMap(Automated 3) + OutputRenderer(LiveOutputRenderSettings·From·OutputSceneSnapshot·CreateScene 위치 매칭) + OutputWindowViewModel(BodyContentMargin = 헤딩 상단여백 + 사용자 좌/우/아래 여백, 화이트리스트) + AppearanceTemplateStore(record 끝 추가·Capture·ApplyTo) + MainViewModel(active 3·Step 커맨드 6·RefreshActiveAppearance 동기) + MainWindow.xaml("본문 여백" −/+ 서브메뉴, 8px 단위). 본문 묶음을 화면 가장자리에서 안쪽으로 들여써 잘림·치우침 보정. 기본 0/0/0 이면 헤딩 유무별 기존 Thickness 와 완전 동일(무회귀). code-reviewer **Approve(0 critical/major)** — 9개 터치포인트 플러밍 대칭·완전, 중간삽입 positional record 가 모두 named-arg 호출이라 안전, 끝-추가 템플릿 record 호환, 기본0 무회귀 명시 테스트, clamp/validate 범위(0~400) 일치 확인. MINOR(WPF Margin 이 MaxWidth=1160/MaxHeight 바깥에 적용돼 줄바꿈 폭을 줄이지 않는 가산적 특성) 반영해 OutputWindow.xaml 에 동작 주석 추가. 신규 테스트 4(여백→BodyContentMargin 반영·기본0 무회귀·커맨드 step/persist·clamp). 1438 green(Release). Release DLL 심볼 검증(LyricsMonitorBodyLeft/Right/BottomMargin·ActiveLyricsLeftMargin·Increase/DecreaseLyrics*MarginCommand·StepLyricsRightMargin 전수 확인).

- (2026-06-02) ✅ 증분 57(직접 수치 입력) — **가사 크기·줄 간격 인스펙터 TextBox 직접 입력**(FrmMain NumericUpDown 대응, gap §2.1 line 29). 기존 읽기전용 TextBlock(−/+ 버튼만) → 양방향 바인딩 TextBox 로 교체. 단일 choke-point 설계: StepLyricsFontSize/LineSpacing 을 절대값 커밋 CommitLyricsFontSize/LineSpacing(int) 에 위임, 신규 LyricsFontSizeInput/LineSpacingInput 프로퍼티(get=>Active, set=>Commit). 재진입 안전 — set→Commit→Active 변경→OnActive*Changed 부분메서드→OnPropertyChanged(*Input) 1-hop 종료. 범위 밖 입력은 클램프(폰트 24~120·간격 100~220) 후 같은 값이면 빈-set 분기에서 OnPropertyChanged 로 입력 박스를 클램프값으로 스냅백. UpdateSourceTrigger=LostFocus(타이핑 중 클램프 방지). code-reviewer **Approve(0 critical/major)** — 무한루프 없음·equal-value 단축 처리 정확·Step→Commit 리팩터 기존 −/+ 동작 바이트 동일·StatusText 부작용 불변 확인. MINOR 2건 반영: (1) 비숫자/공백 입력 시각 피드백 → ValidatesOnExceptions=True 로 기본 빨간 테두리 검증, (2) 상한에서 범위초과 입력 시 스냅백 통지 회귀 테스트 추가. 신규 테스트 4(클램프·persist·step 동기·스냅백 통지). 1442 green(Release). Release DLL 심볼 검증(LyricsFontSizeInput·LyricsLineSpacingInput·CommitLyricsFontSize·CommitLyricsLineSpacing).

- (2026-06-02) ✅ 증분 58(본문 여백 직접 입력) — **본문 좌/우/아래 여백 3종 직접 수치 입력**(FrmMain ShowLeftMargin/Right/Bottom, gap §2.1 line 29 🟢 완결). 증분 57 의 검증된 Input-프로퍼티 패턴을 여백 3종에 동일 적용: StepLyricsLeft/Right/BottomMargin → CommitLyricsLeft/Right/BottomMargin(절대값) 위임 + LyricsLeft/Right/BottomMarginInput 프로퍼티 + OnActiveLyrics*MarginChanged 부분메서드(스냅백 통지). 인스펙터에 "본문 여백(px)" Grid 섹션(좌/우/아래 3 TextBox, 0~400px·TwoWay·LostFocus·ValidatesOnExceptions 빨간 테두리). 메뉴 −/+ 도 동일 커밋 경로로 유지. code-reviewer **Approve(0 issue)** — 3-트리플 교차배선 무오류(설정키·Active필드·Input·hook 통지 대상 전수 일치), Step→Commit 기존 −/+ 동작 바이트 동일, 재진입 안전. 신규 테스트 3(여백 3종 클램프·persist·step 동기). 1444 green(Release). Release DLL 심볼 검증(LyricsLeft/Right/BottomMarginInput·CommitLyricsLeft/BottomMargin). 이로써 gap §2.1 NumericUpDown 직접 입력(8종 중 핵심 크기·간격·여백 5종) 완결.

- (2026-06-02) ✅ 증분 59(헤딩 본문 정렬 따름) — **제목 헤딩 "본문(Region1) 정렬 따름"**(FrmMain Def_HeadAlign AsR1, gap §2.3 line 77). 신규 bool 설정 LyricsMonitorTitleHeadingFollowBody(기본 false). on 이면 헤딩이 본문이 실제 쓰는 resolved textAlignment(곡별 OverrideTextAlignment 포함)를 그대로 사용 → 헤딩 전용 L/C/R 무시. 해석을 OutputRenderer.CreateScene 의 헤딩 정렬 인자에서 삼항으로 처리(`FollowBody ? textAlignment : 헤딩전용`)해 기존 OutputSceneSnapshot 슬롯에 흘려보냄 → OutputWindowViewModel 무변경. 플러밍: SettingsService(decl·All·DTO·GetValue·SET) + LegacySettingsMap + OutputRenderer(LiveOutputRenderSettings 1 param·From, OutputSceneSnapshot 미확장) + AppearanceTemplateStore(끝 추가·Capture·ApplyTo) + MainViewModel(active·ToggleLyricsEffect 공유 커맨드·라벨·RefreshActiveAppearance) + MainWindow.xaml(제목 헤딩 섹션 "본문 정렬 따름" 토글). code-reviewer **APPROVE(0 issue)** — From() 위치 삽입 정확(후속 인자 미시프트)·resolved textAlignment(곡별 override 반영) 사용·플러밍 완전·END-append 안전·snapshot 미확장 정당(다운스트림 미참조) 전수 확인. 신규 테스트 3(따름 on→본문, off→헤딩전용, **곡별 override 추종**). 1447 green(Release). Release DLL 심볼 검증.

- (2026-06-02) ✅ 증분 60(절 헤딩) — **절 헤딩(현재 절 섹션 라벨 "1절"/"후렴" 송출)**(FrmMain Def_Head All, gap §2.3 line76·§3.5 line139). 신규 bool 설정 LyricsMonitorShowVerseHeading(기본 off). 섹션 라벨은 LiveSessionService 에서 계산해 LiveSessionSnapshot.CurrentSectionLabel 로 운반, 렌더러가 노출 게이트(ShowsVerseHeading = 설정 on + Live + 라벨 존재). **리팩터: ComputeCurrentPageIsChorus 가 새 ComputeCurrentSectionLabel 를 재사용**(후렴 판정과 절 헤딩이 같은 라벨을 보도록 단일화 — 이중언어 페이지수 패리티 가드·IsChorusLabel(빈문자열)=false 로 무회귀). 플러밍: SettingsService + LegacySettingsMap + OutputRenderer(LiveOutputRenderSettings·From, OutputSceneSnapshot 2필드 + ShowsVerseHeading 계산, CreateScene) + OutputWindowViewModel(VerseHeadingText/Visibility + 화이트리스트) + AppearanceTemplateStore(끝 추가·Capture·ApplyTo) + MainViewModel(active·ToggleLyricsEffect 공유 커맨드·라벨·RefreshActiveAppearance) + OutputWindow.xaml(상단 캡션 TextBlock, 제목 헤딩과 비겹침) + MainWindow.xaml(메뉴 "절 헤딩 (1절/후렴)"). code-reviewer **Approve(0 issue)** — 두 record 중간삽입(VerseLabel/ShowVerse) 위치 정확(PositionLabel/ShowTitle 무시프트)·chorus 리팩터 동작 동일(패리티 가드시 빈 라벨→후렴 false·헤딩 숨김)·플러밍 완전 전수 확인. 신규 테스트 3(렌더 게이트 on/off·라벨없음, 섹션 라벨 "C"/"1"). 1450 green(Release). Release DLL 심볼 검증.

- (2026-06-02) ✅ 증분 61(헤딩 AsR2) — **제목 헤딩 "보조 영역(Region2) 정렬 따름"**(FrmMain Def_HeadAlign AsR2, gap §2.3 line77·§3.5 line139 🟢 완결). 증분 59(AsR1)에 이어 추가 bool 설정 LyricsMonitorTitleHeadingFollowRegion2(기본 off). CreateScene 헤딩 정렬을 3단 우선순위로 확장: `FollowRegion2 ? textAlignment2 : FollowBody ? textAlignment : 헤딩전용`. textAlignment2 는 기존 resolved Region2 정렬(곡별 override 32, 없으면 Region1 폴백)이라 이중언어 곡에서 번역 영역 정렬을 추종한다. AsR2>AsR1 우선. 플러밍은 AsR1 과 동형(Settings·LegacyMap·OutputRenderer·AppearanceTemplate·MainViewModel·XAML). code-reviewer **Approve(0 issue)** — From() 위치삽입·3단 삼항 textAlignment2(전역 아님) 사용·플러밍 완전·교차배선 없음 전수 확인. 테스트 설계가 Region1=Left/Region2=Right override 로 분기를 명확 구별. 신규 테스트 2(AsR2 Region2 정렬·AsR2>AsR1 우선). 1452 green(Release). Release DLL 심볼 검증. **이로써 헤딩 표시(제목 on/off·첫화면·절 헤딩) + 정렬(L/C/R·AsR1·AsR2) FrmMain 파리티 완비.**

- (2026-06-02) ✅ 증분 62(배경 그라데이션 방향) — **2색 배경 그라데이션 방향 4종**(FrmMain Def_BackColour 패턴, gap §2.3 line83·§3.6 line143). 신규 enum 설정 LyricsGradientDirection(Vertical/Horizontal/DiagonalDown/DiagonalUp, 기본 Vertical=기존 위→아래 무회귀). 기존 하드코딩 세로 그라데이션을 방향 설정대로 LinearGradientBrush Start/End 점 계산(GradientPoints 헬퍼)으로 교체. enum 전 플러밍: SettingsService(enum·SettingKey·All·DTO·Validate Enum.IsDefined·FindChangedKeys 타입스위치 케이스·GetValue·SET) + LegacySettingsMap(Enum) + OutputRenderer(LiveOutputRenderSettings·From·OutputSceneSnapshot·CreateScene) + OutputWindowViewModel(CreateBackgroundBrush direction 인자·GradientPoints·양쪽 호출부·화이트리스트) + MainViewModel(active·ApplyGradientDirectionCommand·Is* 4 계산속성·RefreshActiveAppearance) + AppearanceTemplateStore(끝 추가·Capture·ApplyTo) + MainWindow.xaml("그라데이션 방향" 4 체크 메뉴). code-reviewer **Approve(0 issue)** — enum 2대 필수요소(Validate Enum.IsDefined·FindChangedKeys 타입케이스, 이 저장소에서 가장 자주 빠뜨리는 부분) 둘 다 정확·GradientPoints 4값 매핑 정확·Vertical fallthrough 기존 좌표 보존·중간삽입 위치 정확·양 호출부 갱신 전수 확인. 신규 테스트 3(가로·대각↘·대각↗ Start/End 점). 1455 green(Release). Release DLL 심볼 검증.

- (2026-06-02) ✅ 증분 63(Display Panel 밴드 색) — **정보 패널 밴드 배경색**(FrmMain Def_PanelColour, gap §2.3 line88·§3.5 line138). 신규 int(ARGB) 설정 LyricsMonitorPanelColorArgb(기본 0x66000000=반투명 검정=기존 하드코딩 색, 무회귀). 하드코딩 PanelDefaultBrush 를 설정 색으로 교체: `투명 토글 ? Transparent : CreateBrush(패널색)`. 투명 토글이 색보다 우선. 사용자는 RGB(색조)만 고르고 반투명 알파(0x66)는 항상 유지(밴드 뒤 가사 비침) — ApplyPanelColorHex 가 `(0x66<<24)|(rgb&0xFFFFFF)` 로 합성. 플러밍: SettingsService(decl·All·DTO·GetValue·SET, 일반 int) + LegacySettingsMap(Color) + OutputRenderer(LiveOutputRenderSettings·From·OutputSceneSnapshot·CreateScene, PanelTransparent 뒤 동일 위치) + OutputWindowViewModel(PanelBackgroundBrush·화이트리스트) + MainViewModel(ApplyPanelColorHexCommand) + AppearanceTemplateStore(끝 추가·Capture·ApplyTo) + MainWindow.xaml("정보 패널 색" 5 프리셋 메뉴). code-reviewer **Approve(0 issue)** — replace_all 양 record·From·CreateScene 위치 일치(무시프트)·알파 합성 비트연산 정확·기본 0x66000000 무회귀·일반 int 처리·투명 우선 전수 확인. 신규 테스트 3(커스텀 색·투명 우선·알파 유지·무효 hex no-op). 1458 green(Release). Release DLL 심볼 검증.

- (2026-06-02) ✅ 증분 64(보조영역 전역 폰트 크기) — **Region2 전역 폰트 크기**(FrmMain Ind_Reg2SizeUpDown, gap §2.3 line74). 신규 int 설정 LyricsMonitorFontSize2(기본 0=본문[Region1]과 동일 자동, 무회귀). 0 이 아니면 이중 언어 곡의 Region2 본문에 적용. 우선순위 곡별 region2 크기(48) > 전역 region2 크기(>0) > Region1 크기. 렌더러 fontSize2Px 해석을 중첩 삼항으로 확장. 플러밍: SettingsService(decl·All·DTO·Validate[0 허용·그외 24~120]·GetValue·SET) + LegacySettingsMap(Integer) + OutputRenderer(LiveOutputRenderSettings 만 추가[OutputSceneSnapshot 은 이미 resolved 필드 보유]·From·fontSize2Px) + MainViewModel(active·CommitLyricsFontSize2[0=자동·그외 24~120 클램프]·LyricsFontSize2Input·RefreshActiveAppearance) + AppearanceTemplateStore(끝 추가·Capture·ApplyTo) + MainWindow.xaml(인스펙터 "보조영역 크기" 직접 입력 박스). code-reviewer **Approve(0 issue)** — 설정-입력 vs resolved-출력 동명 필드 혼동 없음(설정 1회 소비·스냅샷 1회 기록)·From() 위치 정확·0-센티넬 3계층(Validate·Commit·렌더러) 일관·우선순위 정확·기본0 무회귀 전수 확인. 신규 테스트 4(전역 적용·0 추종·곡 override 우선·Input 클램프 0/1-23/>120). 1462 green(Release). Release DLL 심볼 검증.

- (2026-06-02) ✅ 증분 65(Display Panel 글자 크기) — **정보 패널 글자 크기 비율**(FrmMain Def_PanelFont 크기, gap §2.3 line88·§3.5 line138). 신규 int 설정 LyricsMonitorPanelFontScalePercent(기본 100=기존 크기 무회귀, 범위 50~200). 하드코딩 정보바 폰트(위치/곡번호=20·저작권/다음=16)를 VM 계산값(PanelPrimaryFontSize=20×비율·PanelSecondaryFontSize=16×비율)에 바인딩. 큰 장소·가독성 위해 확대/축소. 플러밍: SettingsService(decl·All·DTO·Validate RequireRange 50~200·GetValue·SET) + LegacySettingsMap(Integer) + OutputRenderer(양 record replace_all·From·CreateScene) + OutputWindowViewModel(Primary/Secondary FontSize 계산·base 상수 20/16·백킹필드 초기화·화이트리스트) + MainViewModel(active·Step ±10 클램프·−/+ 커맨드·RefreshActiveAppearance) + OutputWindow.xaml(4 FontSize 바인딩) + MainWindow.xaml("정보 패널 글자" 메뉴) + AppearanceTemplateStore(끝 추가·Capture·ApplyTo). code-reviewer **Approve(0 issue)** — replace_all 양 record·From·CreateScene 위치 일치(테스트 named-arg 안전)·스케일 산식 정확·기본100 무회귀(20/16)·4 바인딩 주/보조 매핑 정확·백킹필드 base 초기화·플러밍 완전 전수 확인. 신규 테스트 3(100%→20/16·150%→30/24·step ±10·클램프 50/200). 1464 green(Release). Release DLL 심볼 검증.

- (2026-06-02) ✅ 증분 66(Title-on-Panel) — **정보 패널에 곡 제목 표시**(FrmMain Def_PanelTitle, gap §2.3 line88·§3.5 line138 🟢 Display Panel 완비). 신규 bool 설정 LyricsMonitorShowTitleOnPanel(기본 off). on 이면 현재 곡 제목을 송출 화면 좌측 상단 정보 밴드에 작게 표시. **새 제목 문자열 필드 없이 기존 scene.DisplayTitle 재사용** — ShowsTitleOnPanel = 설정 on + Live + 제목 존재. 플러밍: SettingsService(decl·All·DTO·GetValue·SET, bool) + LegacySettingsMap(Boolean) + OutputRenderer(LiveOutputRenderSettings·From·OutputSceneSnapshot 중간삽입[ShowItemNumber↔ItemNumberLabel 사이]·CreateScene 위치 매칭·ShowsTitleOnPanel 계산) + OutputWindowViewModel(TitleOnPanelVisibility·화이트리스트) + MainViewModel(active·ToggleLyricsEffect 공유 커맨드·라벨·RefreshActiveAppearance) + MainWindow.xaml(메뉴 토글) + OutputWindow.xaml(좌측 상단 제목 밴드, PanelBackgroundBrush·PanelSecondaryFontSize) + AppearanceTemplateStore(끝 추가·Capture·ApplyTo). code-reviewer **Approve(0 critical/major)** — 최고위험 중간삽입(snapshot·CreateScene) 위치 정확(ItemNumberLabel 등 무시프트)·From() 위치 정확·게이트 DisplayTitle 재사용·bool 플러밍 완전·밴드 배치 비충돌 전수 확인. MINOR(제목 헤딩 좌측정렬+패널제목 동시 on 시 같은 제목 겹침, 비현실 조합) → XAML 주석으로 either/or 의도 명시. 신규 테스트 2(on+Live+제목→표시·off/비Live→숨김). 1466 green(Release). Release DLL 심볼 검증.

- (2026-06-02) ✅ 증분 67(영역 간 세로 간격) — **이중 언어 Region1↔Region2 세로 간격**(FrmMain Ind_Reg2TopUpDown의 클린 슬라이스, gap §2.3 line74). 신규 int 설정 LyricsMonitorRegionGapPx(기본 8=기존 하드코딩 Margin "0,8,0,0", 무회귀, 범위 0~100). Region2 본문 블록의 위쪽 여백을 VM 계산 BodyText2Margin=Thickness(0,gap,0,0)에 바인딩(하드코딩 8px 교체). 인터레이스 경로는 별도(영향 없음). 영역별 "절대 세로위치"는 StackPanel 레이아웃과 안 맞아 강제 포팅 대신 의미가 명확한 "영역 간 간격"만 정직하게 구현. 플러밍: SettingsService(decl·All·DTO·Validate 0~100·GetValue·SET) + LegacySettingsMap(Integer) + OutputRenderer(양 record replace_all·From·CreateScene) + OutputWindowViewModel(BodyText2Margin·백킹필드 new(0,8,0,0)·화이트리스트) + MainViewModel(active·Step ±4 클램프·−/+ 커맨드·RefreshActiveAppearance) + OutputWindow.xaml(Region2 Margin 바인딩, 하드코딩 제거) + MainWindow.xaml(본문 여백 메뉴 "영역 간격") + AppearanceTemplateStore(끝 추가·Capture·ApplyTo). code-reviewer **Approve(0 issue)** — replace_all 양 record·From·CreateScene 위치 정확(string-vs-int 슬롯이라 오정렬 시 컴파일 에러)·XAML Margin 정확히 1개(중복 제거 확인)·기본8 전계층 무회귀·인터레이스 비영향·int 플러밍 완전 전수 확인. 신규 테스트 2(8→Thickness·32→Thickness, step ±4·클램프 0/100). 1468 green(Release). Release DLL 심볼 검증.

- (2026-06-02) ✅ 증분 68(런타임 출력 모니터 이동) — **MoveTo 배선**(레거시 런타임 모니터 이동, gap §3.1 line130 🟢, 코드 주석 "MoveTo 아직 미배선" 해소). 기존 IOutputWindowService.MoveTo(상태 전용 서비스, 이미 존재)를 OnSelectedOutputDisplayChanged 부분메서드로 배선: 출력 창이 열린 상태에서 SelectedOutputDisplay 가 바뀌면 그 모니터로 즉시 이동 + LiveBar.OutputMonitorName/StatusText 갱신. 닫혀 있으면 다음 Open 때 반영(무동작). **재진입 안전**: OpenOutput 이 Open 직후 같은 모니터로 SelectedOutputDisplay 세팅해도 OutputDisplay 레코드 값 전체 동일 가드(current == value)로 재이동 안 함(무한 이동 방지). MoveTo 시 OnOutputChanged 가 새 모니터 해상도로 PPT 재렌더(IsOpen 가드, 기존 동작이 MoveTo 경로로도 도달). code-reviewer **Approve(0 critical/major)** — 재진입/무한루프 안전·null 안전·windowed:true OpenOutput 일관·Open/Close 무회귀 확인. MINOR(Id-only 가드보다 전체 레코드 동등이 모니터 재구성[같은 Id·다른 해상도]에 더 안전) 반영해 `current == value` 레코드 값 동등으로 강화. 테스트 인프라: CreateSut 에 IOutputWindowService 주입 파라미터 추가(공유 인스턴스 검증). 신규 테스트 2(열림→이동·LiveBar 반영, 닫힘→무동작). 1470 green(Release). Release DLL 심볼 검증(OnSelectedOutputDisplayChanged).

- (2026-06-02) ✅ 증분 69("코드 표시" 토글) — **출력 메뉴 "코드 표시"(Show Notations) 토글**(FrmMain Def_Notations, gap §2.3 line81·§3.7 🟢). LyricsMonitorShowNotations 설정·렌더(ExpandNotations)·라이브 재송출(OnSettingsChanged→RepublishLiveSongForBodyChange)은 이미 존재했으나 **메인 UI 토글이 없어 설정 창에서만 켤 수 있던** 운영 갭을 해소. 코드 표시는 라이브 운영 컨트롤(조옮김도 이게 켜져야 의미)이라 출력 메뉴 상단(조옮김 바로 위)에 토글 배치. 기존 ToggleLyricsEffect 헬퍼 재사용 — 5개 터치포인트(active·ctor·decl·라벨·RefreshActiveAppearance) 그림자/굵게 토글과 동형. code-reviewer **Approve(0 issue)** — 5터치포인트 완비·body-change 라이브 재송출 기존 경로 자동·IsChecked 라운드트립·이중토글 없음·설정창/조옮김 무회귀 전수 확인. 신규 테스트 1(off→on→off). 1471 green(Release). Release DLL 심볼 검증. **부수 재조정**: gap §2.3 line80(To Capo 0 원조복귀)·line82(반음 조옮김 ▲▼)·§3.7 헤더 stale 🔴→🟢(증분27/30 으로 이미 구현됐으나 §2.3 중복 항목이 미갱신이던 것 정정).

- (2026-06-02) ✅ 증분 70(예배순서 더블클릭 송출) — **예배 순서 항목 더블클릭 → 라이브 송출**(레거시 FrmMain WorshipListItems_DoubleClick → CopyPreviewToOutput 의 핵심 동작 충실 포팅). QueueList ListBox 에 LeftDoubleClick MouseBinding → 기존 GoLiveCommand(안전 확인 5초 후 PublishSelectedItem) 연결. 단일 클릭=선택, 더블클릭=송출. 레거시는 ShowRunning(출력 활성) 시 CopyPreviewToOutput 하는데, WPF 도 CanGoLive(`_output.Current.IsOpen`) 게이트로 출력 열렸을 때만 송출 → 동작 충실. code-reviewer **Approve(0 issue)** — 드래그 정렬 핸들러(PreviewMouseLeftButtonDown/Move)와 공존(e.Handled 미설정·드래그 임계값으로 더블클릭 미발동, 두 제스처 상호배타)·AsyncRelayCommand 비재진입(확인 중 2차 클릭 CanExecute 차단으로 이중 송출 없음)·SelectedItem null 가드 전수 확인. VM 무변경(GoLiveCommand 기존 테스트 커버)·XAML MouseBinding 이라 신규 테스트 불요(View=제스처/VM=로직 분리 일관). 1471 green(Release). Release DLL 무변경(XAML BAML 만 변경).

- (2026-06-02) ✅ 증분 71(전 항목 전역 서식 적용) — **"전 항목에 전역 서식 적용"**(FrmMain "Apply to All Except InfoScreens"/DefApplyDefaultsBtn, gap §2.3 line91 🟢·§3.5 line191 🟡). ApplyGlobalFormatToAllCommand — 모든 예배 순서 항목의 UseIndividualFormatting 을 false 로 바꿔 곡별 FormatData 대신 운영 기본(인스펙터) 서식으로 통일 송출. 단일항목 ToggleUseIndividualFormatting 패턴을 전체에 일반화: 선택 인덱스 잡아두고 교체 후 같은 자리 새 인스턴스로 재선택(선택 유지)·Id 로 라이브 항목 찾아 RepublishLiveSongForBodyChange(라이브 즉시 반영·0절 안튐). 빈 큐·이미 전역인 경우 구분 메시지. UseIndividualFormatting 은 곡·성경 텍스트 송출에만 영향(PPT/미디어/공지 무영향=레거시 "Except InfoScreens" 취지). 출력 메뉴 "전 항목에 전역 서식 적용". code-reviewer **Approve(0 critical/major)** — capture-index-before/re-select-after 정확(off-by-one 없음·미변경 항목 no-op)·ObservableCollection 인덱서 set(Replace) 안전(length 불변·열거 위험 없음)·Id 기반 라이브 lookup 교체 후에도 유효·선택 부작용(RefreshLyricsPages 0리셋)은 세션 실제 절 사용으로 무영향 전수 확인. 신규 테스트 2(전 항목 off·선택유지·3개 메시지, 이미 전역 no-op). 1473 green(Release). Release DLL 심볼 검증.

- (2026-06-02) ✅ 증분 72(출력 모양 기본값 복원) — **"기본값으로 복원"**(FrmMain Default Layout, gap §3.5 line191 🟢). ResetOutputAppearanceCommand — 출력 모양 전체를 기본값으로. ① 모양 템플릿 커버 30키는 신규 LyricsAppearanceTemplate.Defaults(Capture 와 같은 키 목록을 SettingKey.DefaultValue 로 읽어 설정 기본값과 항상 일치 — 단일 진실원)로 ApplyTo, ② 템플릿 밖 출력 모양 키(전환종류/길이·배경모드/이미지·영역표시·강조후렴만·인터레이스·패널투명·곡번호/저작권/다음항목)는 SettingKey.DefaultValue 로 직접 리셋. 출력 VM 이 SettingsChanged 로 라이브 즉시 반영. 인스펙터에 "기본값으로 복원" 버튼. **code-reviewer 1차 MAJOR(버튼이 "전체"를 약속하나 템플릿 30키만 리셋 — 전환·배경모드·영역표시 등 같은 패널 컨트롤이 안 돌아감) 반영**: 옵션②(리셋 확대)로 템플릿 밖 11키도 함께 리셋해 "전체" 약속을 실제로 충족. 드리프트 가드 테스트(Defaults == Capture(fresh) 레코드 동등)로 향후 새 모양 키 누락 방지. 신규 테스트 3(템플릿 키 6 + 템플릿 밖 키 6 리셋·드리프트 가드·이미 기본값 무동작 제거). 1475 green(Release). Release DLL 심볼 검증(ResetOutputAppearanceCommand·get_Defaults). gap §3.5 line191 Use Individual/Apply to All/Default Layout 3종 🟢 완비.

- (2026-06-02) ✅ 증분 73(본문 세로 위치 오프셋) — **본문 세로 위치 미세 이동**(FrmMain Ind_Reg1TopUpDown, gap §2.3 line74 🟢 완결). 신규 int 설정 LyricsMonitorBodyVerticalOffset(기본 0·범위 -300~300·음수=위). 영역 "절대 세로위치"는 StackPanel 레이아웃과 안 맞아 강제 포팅 대신, 정렬(위/가운데/아래)을 유지한 채 본문 묶음을 TranslateTransform.Y 로 위/아래 N px 시각 이동(레이아웃 재배치 없음). 기본 0=무이동(무회귀). 플러밍: SettingsService(decl·All·DTO·Validate -300~300·GetValue·SET) + LegacySettingsMap(Integer) + OutputRenderer(양 record replace_all·From·CreateScene) + OutputWindowViewModel(BodyVerticalOffset double·화이트리스트) + MainViewModel(active·Step ±8 클램프·위/아래 커맨드·RefreshActiveAppearance) + OutputWindow.xaml(본문 StackPanel RenderTransform TranslateTransform) + MainWindow.xaml(본문 여백 메뉴 "본문 위로/아래로") + AppearanceTemplateStore(끝 추가·Defaults·Capture·ApplyTo, 드리프트 가드 통과). code-reviewer **Approve(0 issue)** — replace_all 양 record 위치 정확(int↔다음타입 슬롯이라 오정렬 시 컴파일 에러)·RenderTransform 기존 변환 없음·post-layout 시각이동·기본0 무회귀·범위 음수 허용·Defaults/Capture/ApplyTo 트리오 일관·기본값복원(증분72)이 이 키도 리셋 전수 확인. 신규 테스트 2(오프셋 -40 반영·step ±8·클램프 ±300). 1477 green(Release). Release DLL 심볼 검증. **이로써 §2.3 영역별 폰트 크기·세로위치(Ind_Reg1/2 Size/Top) 🟢 완비.**

- (2026-06-02) ✅ 증분 74(이미지 카테고리 필터) — **이미지 갤러리 카테고리(하위폴더) 필터**(FrmMain Images Scenery/Tiles, gap §3.6 line157, Images 트랙 첫 바운디드 증분). 기존 ImageLibraryViewModel(폴더 썸네일→출력 배경 적용)에 카테고리 구분 추가: 각 이미지를 루트 바로 아래 하위 폴더명(CategoryOf — "Scenery"/"Tiles", 루트 직속은 "(기본)")으로 분류, Categories 콤보("전체"+발견 하위폴더)로 필터. _allItems 원본 유지·SelectedCategory 로 Images 부분집합 재구성(같은 인스턴스라 썸네일 유지). 재로드 시 카테고리 "전체" 초기화. code-reviewer **MAJOR(필터/재로드로 Images 비울 때 SelectedImage 가 숨은 항목을 가리킨 채 "배경 적용"이 동작) 반영** — ApplyCategoryFilter 가 선택이 결과에 없으면 SelectedImage=null 로 해제. MINOR(".." escape 체크가 "..bar" 정상 폴더 오분류) 반영 — 정확히 ".." 또는 ".."+구분자만 escape 로. 신규 테스트 8(CategoryOf 4 + 카테고리목록·필터·재로드초기화·선택해제). 1485 green(Release). Release DLL 심볼 검증. **남은 Images: 우클릭 메뉴(버튼·더블클릭 배경적용은 제공).**

- (2026-06-02) ✅ 증분 75(이미지 우클릭 메뉴) — **이미지 갤러리 우클릭 컨텍스트 메뉴**(FrmMain Images "우클릭 배경 적용", gap §3.6 line157 🟢 Images 트랙 완비). 썸네일 ListBox 에 우클릭 메뉴("배경으로 적용"/"배경 해제") — 기존 ApplyAsBackground/ClearBackground 커맨드 재사용(PlacementTarget.DataContext, WorshipListPanel 패턴). PreviewMouseRightButtonDown 핸들러가 우클릭한 썸네일을 먼저 선택(ItemsControl.ContainerFromElement→ListBoxItem.IsSelected)해, 메뉴가 직전 좌클릭 선택이 아닌 우클릭한 그 이미지에 동작(SelectedImage 양방향 바인딩→NotifyCanExecuteChangedFor 로 CanExecute 즉시 재평가). code-reviewer **Approve(0 issue)** — ContextMenu 바인딩 정확·ContainerFromElement 2-arg 올바름·빈공간 우클릭 null 안전·우클릭 선택이 메뉴 열기 전 SelectedImage 갱신·더블클릭/양방향 바인딩 무충돌·CanExecute 비대칭(Apply 가드/Clear 무가드) 정당 전수 확인. VM 무변경(커맨드 기존 테스트)·우클릭 선택은 View 제스처라 신규 단위테스트 불요. 1485 green(Release). Release DLL 무변경(XAML/code-behind).

- (2026-06-02) ✅ 증분 76(명명 정보 화면 저장/불러오기) — **InfoScreen 트랙 첫 바운디드 증분**(FrmMain FrmInfoScreen .esi 목록, gap §3.6 line160·line97). 기존 NoticeScreenViewModel(공지 텍스트 송출만)에 명명 저장 추가: 신규 InfoScreenStore(InfoScreenDto{Text,FontSize} JSON 영속 — PraiseBookStore/AppearanceTemplateStore 와 동일 원자적 쓰기·StoreFileNaming 경로 안전 패턴, `%AppData%/EasislidesNext/InfoScreens/{name}.json`). VM: SavedScreens 콤보·NewScreenName·SaveAs/Open/Delete 커맨드(생성자 옵션 store 주입으로 기존 호출부 무회귀). 공지 창에 이름 TextBox+저장·콤보+불러오기·삭제 행 추가. 재사용 정보화면("환영 인사"·"헌금 안내") 저장·복원. code-reviewer **Approve(0 critical/major)** — PraiseBookStore 충실 미러(보안로직 재구현 없음)·원자적 쓰기·CanExecute 와이어링·생성자 FS 읽기 안전(dir 없으면 빈 목록)·기존 2-arg 호출부 무회귀·FontSize 센티넬 무해 전수 확인. MINOR(Open 실패 분기가 Delete 와 달리 SelectedScreen 미해제) 반영 — dto null 분기에 SelectedScreen=null 추가(선택 일관). 신규 테스트 8(스토어 round-trip·missing·정렬·삭제·손상파일 null + VM save/open·이름없음 비활성·삭제 선택해제). 1493 green(Release). Release DLL 심볼 검증. **착수 전 PraiseBook 트랙이 이미 증분44로 완료된 것을 grep 으로 발견·갭문서 false-🔴 정정(세션 메모 중복 방지 원칙) → 남은 트랙은 별도 Preview 모니터·InfoScreen 풀 편집기뿐.**

- (2026-06-02) ✅ 증분 77(공지 가로 정렬) — **정보 화면 가로 정렬**(InfoScreen 트랙 2번째 슬라이스, FrmInfoScreen 정렬). 공지에 정렬(기본/왼쪽/가운데/오른쪽=0/1/2/3) 추가 — 명명 정보화면에 저장되고 송출 시 적용. 핵심: PublishNotice 가 글자크기(47=pt)와 정렬(31=1왼/2가운데/3오른)을 레거시 FormatData 로 조립(BuildNoticeFormatData 순수 함수), 공지 LiveQueueItem 이 곡과 동일한 kind-무관 FormatData→OverrideTextAlignment 디코드 경로(LiveSessionService.MapAlignment(Alignment1))를 타 정렬이 렌더에 적용된다(곡 폰트 오버라이드 파이프라인 그대로 재사용). InfoScreenDto 에 Alignment 끝 추가(구버전 JSON 무회귀). 송출 콜백 Func<string,int,bool>→<string,int,int,bool>(text,size,align), 모든 호출부·테스트 갱신. 공지 창에 정렬 콤보. code-reviewer **Approve(0 issue)** — SongFormatData.Parse 가 "47=60>31=2>" 연결 세그먼트를 모두 파싱(FontSize1·Alignment1 둘 다)·notice kind 도 GoLive 에서 동일 디코드·OutputRenderer isLive 게이트(kind 무관) 적용·범위밖(9) compose+decode 이중 검증으로 무시·DTO 끝추가 무회귀·델리게이트 전 호출부 갱신 전수 검증. 신규 테스트 6(BuildNoticeFormatData 5 + 정렬 round-trip, Send 정렬 전달). 1499 green(Release). Release DLL 심볼 검증.

- (2026-06-02) ✅ 증분 78(공지 글자색 + NoticeOptions 리팩터) — **정보 화면 글자색**(InfoScreen 트랙 3번째 슬라이스) + 송출 옵션 레코드화. 공지에 글자색(ARGB int, 0=기본) 추가 — 명명 정보화면에 저장·송출 적용. **정석 리팩터**: 증분77 에서 (text,int,int)로 커진 송출 콜백을 색 추가 시 정수 3개 위치 모호가 되므로 `NoticeOptions(FontSizePt,Alignment,ColorArgb)` 레코드로 묶어 인자 순서 실수를 막음. BuildNoticeFormatData 가 47=pt·31=정렬·29=색(ARGB int)을 조립, 공지가 곡과 동일한 FormatData→OverrideTextColorArgb 디코드 경로(ParseArgb 십진 ARGB, 음수도 정상 — 흰색 -1)를 타 색 렌더 적용. PublishNotice(string, NoticeOptions? = null) 옵션화로 기존 무서식 호출부 무회귀. 공지 창에 글자색 콤보(흰/노랑/하늘/연두 프리셋). code-reviewer **APPROVE(0 issue)** — 음수 ARGB round-trip(ParseArgb NumberStyles.Integer)·기존 SongFormatDataTests 로 커버·0 센티넬·옵션 무회귀·델리게이트 전 호출부 갱신·프리셋 full-alpha·DTO 끝추가 무회귀 실제코드 대조 검증. 신규 테스트(BuildNoticeFormatData 색 케이스·옵션 전달·정렬+색 round-trip·**OverrideTextColorArgb/Alignment 엔드투엔드 렌더경로 증명**). 1502 green(Release). Release DLL 심볼 검증.

- (2026-06-02) ✅ 증분 79(전역 출력 글꼴) — **전역 출력 가사 글꼴 설정**(레거시 FrmMain Def_FontName). 기존엔 곡별 글꼴(FormatData 43)만 있어 곡에 글꼴 지정이 없으면 항상 테마 기본 글꼴로 떨어졌다(gap §3.2 line147 🟡 "곡별만"). 이제 `LyricsMonitorFontFamily`(string 설정)을 추가해 우선순위가 **곡별 글꼴(43, Live 한정) > 전역 글꼴(설정) > 테마 기본("")**. 16-터치포인트 설정 배선(SettingKey·All·DTO·GET/SET switch·LegacySettingsMap String·OutputRenderer 입력 레코드 끝 추가+From() 위치 일치+else 분기 ""→전역, 출력 VM 라이브 리프레시 화이트리스트, MainViewModel ActiveLyricsFontFamily 옵저버블·LyricsFontFamilyInput·CommitLyricsFontFamily(trim·Ordinal 비교)·추천 글꼴 목록·RefreshActiveAppearance 동기화·ResetOutputAppearance "전체" 복원에 글꼴 포함). MainWindow 인스펙터에 편집 가능 글꼴 콤보(목록 밖 글꼴명 직접 입력 가능, 비우면 기본). 비-Live 시에도 전역 글꼴 적용은 형제 폰트 크기 동작과 일치(곡별 오버라이드만 isLive 게이트). code-reviewer **APPROVE(0 critical)** — 위치 레코드 정렬(전 호출부 named-arg)·비-Live 일관성·화이트리스트 경로·Ordinal 비교+snap-back 통지·끝추가 무회귀 실제코드 대조 검증. 신규 테스트 5(렌더 곡없음→전역·곡별>전역, VM set/clear trim·RefreshActiveAppearance 동기화·Reset 글꼴 복원). 1507 green(Release). Release DLL 심볼 검증.

- (2026-06-02) ✅ 증분 80(보조영역 전역 글꼴) — **보조 영역(Region2) 전역 출력 글꼴**(레거시 Ind_Reg2Font). 증분79 의 본문(Region1) 전역 글꼴과 대칭이며, 이미 있던 보조영역 전역 크기(`LyricsMonitorFontSize2`, 0=본문 추종)와 동일 구조. 우선순위 **곡별 region2 글꼴(44, Live 한정) > 전역 region2 글꼴(설정, 비어있지 않으면) > 본문(Region1) 글꼴 추종**. 비우면 본문 추종(무회귀). 16-터치포인트(SettingKey·All·DTO·GET/SET·LegacySettingsMap String·OutputRenderer 입력 레코드 끝 추가+From() 위치 일치+fontFamily2 3분기, 출력 VM 화이트리스트, MainViewModel ActiveLyricsFontFamily2·LyricsFontFamily2Input·CommitLyricsFontFamily2·RefreshActiveAppearance·ResetOutputAppearance "전체" 포함). MainWindow 인스펙터에 보조영역 글꼴 편집 콤보(보조영역 크기 박스 아래). code-reviewer **APPROVE(0 critical)** — 인접 동일타입(string×2) 위치 레코드 정렬 char 단위 대조(decl 순서==From() 순서, named-arg 테스트라 무회귀)·fontSize2 우선순위 대칭·빈 전역 시 본문 추종 무회귀(테스트가 Region1/Region2 독립 단언)·Ordinal+snap-back·끝추가 무회귀 검증. 신규 테스트 6(렌더 본문추종·전역사용·곡별우선, VM set/clear·RefreshActiveAppearance·Reset). 1513 green(Release).

- (2026-06-02) ✅ 증분 81(자동 회전 4종 모드) — **레거시 One/One-Repeat/Group/Group-Repeat 자동 회전 모드**. 기존엔 "다음 절/슬라이드, 무한 순환"(=One-Repeat)만 있었다(gap §3.1 line169 🟡). `AutoRotateMode` enum 설정 추가, `AdvanceAutoRotation()` 을 (1) 현재 항목 내 다음 절/슬라이드 진행 → (2) 항목 끝이면 모드별 처리로 재작성. OneRepeat(기본=기존, 같은 항목 순환), One(끝나면 자동 회전 정지), Group(다음 예배 항목으로/마지막이면 정지), GroupRepeat(다음 항목/마지막이면 첫 항목 순환). Group 이동은 PublishSelectedItem(autoAdvance:false)로 선택 중복 이동 방지, SelectedItem 변경이 LyricsPageIndex 를 0 으로 리셋해 새 항목 첫 절부터. enum 설정 풀 배선(SettingKey·All·DTO·GET/SET·**FindChangedKeys 타입 스위치 케이스**·Validate Enum.IsDefined·LegacySettingsMap Enum). 라이브바에 모드 콤보(현재항목반복/한항목만/그룹/그룹반복). code-reviewer **APPROVE(0 issue)** — 기본 OneRepeat 무회귀(기존 2 테스트 그대로 통과: 절 0→1→2→0·슬라이드 1→2→3→1, off-by-one 없음 3번째 호출서 wrap)·autoAdvance:false 중복이동 방지·신원 가드(_liveItemId 갱신)·빈/단일 큐 엣지·enum 풀 touchpoint(throw 기본 스위치라 누락 시 throw=커버)·JSON 무회귀 실제코드 대조. IsAutoRotating=false 가 DispatcherTimer 실제 정지. 신규 테스트 4(기본·One 정지·Group 다음→정지·GroupRepeat 순환). 1517 green(Release).

- (2026-06-02) ✅ 증분 82(보조영역 전역 글자색) — **보조 영역(Region2) 전역 출력 글자색**(이중 언어 번역 영역 색). 증분80(글꼴2)·기존 크기2(0=추종)와 동일한 "0=본문 추종" 센티넬 구조. 우선순위 **곡별 region2 색(30, Live 한정) > 전역 region2 색(설정, 0 아니면) > 본문(Region1) 색 추종**. 0(투명)=본문 색 추종(무회귀 — 투명 글자는 보이지 않으므로 안전한 센티넬, 증분78 공지색과 동일 논리). 16-터치포인트(SettingKey·All·DTO·GET/SET·LegacySettingsMap Color·OutputRenderer 입력 레코드 끝 추가(int, 직전 string 다음이라 swap=컴파일 캐치)+From() 위치 일치+textColor2 3분기, 출력 VM 화이트리스트, MainViewModel ActiveLyricsTextColor2Argb·LyricsTextColor2Presets(본문동일/흰/노랑/하늘/연두/연회색)·LyricsTextColor2Input·RefreshActiveAppearance·ResetOutputAppearance "전체" 포함). 인스펙터에 보조영역 글자색 콤보(보조영역 글꼴 콤보 아래). int 설정이라 FindChangedKeys 추가 케이스·Validate 불요(enum 만 필요). code-reviewer **APPROVE(0 issue)** — 위치 레코드 정렬(named-arg 테스트)·우선순위 대칭·0 센티넬 안전·비-Live 일관·빈 전역 시 본문 추종 무회귀(Region1/Region2 독립 단언)·끝추가 무회귀 검증. 신규 테스트 6(렌더 본문추종·전역사용·곡별우선, VM set/clear·Refresh·Reset). 1523 green(Release).

- (2026-06-02) ✅ 증분 83(보조영역 전역 가로 정렬) — **보조 영역(Region2) 전역 가로 정렬**(FrmMain Ind_Reg2Align). Region2 전역 외형 셋(글꼴80·색82) 완성. 정렬 enum 은 "추종" 값이 없어 **전용 enum `LyricsRegion2Alignment{FollowRegion1=0,Left=1,Center=2,Right=3}`** 으로 FollowRegion1=0 센티넬 기본(2-설정 토글 대신 1 enum 설정). 우선순위 **곡별 region2 정렬(32, Live 한정) > 전역 region2 정렬(FollowRegion1 아니면) > 본문(Region1) 정렬 추종**. 정렬값 1/2/3 이 LyricsTextAlignment 0/1/2 와 **의도적 1 오프셋**(FollowRegion1 이 0 차지)이라 `MapRegion2Alignment` 가 **이름으로** 변환(숫자 캐스트면 off-by-one). enum 풀 배선(SettingKey·All·DTO·GET/SET·FindChangedKeys 케이스·Validate Enum.IsDefined·LegacySettingsMap Enum). 인스펙터에 정렬 콤보(보조영역 색 콤보 아래). code-reviewer **APPROVE(0 defect)** — **enum 오프셋 매퍼 이름-정확성**(테스트가 off-by-one 판별: 본문=Right, Region2=Left 독립 단언)·위치 레코드 정렬(직전 int 다음 enum=swap 컴파일 캐치)·enum 풀 touchpoint·JsonStringEnumConverter 문자열 직렬화로 0-오프셋 무손상·비-Live 대칭·추종 무회귀 검증. 신규 테스트 6(렌더 추종·전역사용·곡별우선, VM set/clear·Refresh·Reset). 1529 green(Release).

- (2026-06-02) ✅ 증분 84(예배 순서 전체 비우기 + 되돌리기) — **예배 순서 전체 비우기(레거시 Empty Worship List) + 1단계 되돌리기(복구)**. 기존 WPF 엔 단건 제거(RemoveSelectedItem)만 있고 전체 비우기 명령이 없었다(gap §3.4 line167). ClearWorshipListCommand 이 현재 큐를 복구용 스냅샷(`_clearedWorshipBackup`)에 담고 비우며, RestoreClearedWorshipListCommand(되돌리기)가 스냅샷을 되살린다 — 레거시 Empty→Trash 폴더 복구의 WPF 대응(실제 폴더 대신 세션 내 1단계 실행취소). 비우기는 `_liveItemId=null`(RemoveSelectedItem 과 동일한 고아 Id 가드, 세션 송출은 유지). Restore 는 방어적으로 Queue.Clear 후 복구(중복 append 방지, 리뷰어 제안 반영). 명령 활성: Clear=큐 비어있지 않을 때(CollectionChanged 람다+NotifyCommandStates 갱신, 생성 순서 때문에 null-conditional 가드), Restore=스냅샷 있을 때(NotifyCommandStates 갱신). WorshipListPanel 에 비우기(Dismiss24)·되돌리기(ArrowUndo24) 버튼. code-reviewer **APPROVE(0 critical)** — 1단계 undo 의미·라이브 상태 일관성(RemoveSelectedItem 패턴 재사용)·불변 레코드 참조 안전·명령 갱신 경로·생성 순서 가드·테스트 진정성(seedSampleQueue:false 필요) 검증, append→replace 방어 제안 반영. 신규 테스트 3(round-trip·CanExecute 게이팅·빈 큐 무동작). 1532 green(Release).

- (2026-06-02) ✅ 증분 85(보조영역 전역 굵게) — **보조 영역(Region2) 전역 굵게(3-상태)**. Region2 전역 외형 시리즈(크기·글꼴·색·정렬) 이어 굵게 추가. bool 은 "추종" 상태가 없어 정렬2 와 같은 센티넬 enum `LyricsRegion2Emphasis{FollowRegion1=0,On=1,Off=2}`(italic2/underline2 재사용 위해 범용 명명). 우선순위 **곡별 region2 굵게 비트(Live 한정) > 전역 region2 굵게(FollowRegion1 아니면 On/Off) > 본문(Region1) 굵게 추종**. `ResolveRegion2Emphasis(emphasis, followValue)` 가 On→true·Off→false·FollowRegion1→본문값. **후렴만 강조 게이트는 하류(downstream)라 전역 On 도 비-후렴 절에서 함께 억제**(출처 무관, 신규 타깃 테스트로 직접 고정). 기본 FollowRegion1 → 본문 굵게 추종(무회귀, 라이브/비-라이브 양쪽 기존과 동일). enum 풀 배선(SettingKey·All·DTO·GET/SET·FindChangedKeys 케이스·Validate·LegacySettingsMap Enum). 인스펙터에 굵게 콤보(본문동일/굵게/보통). code-reviewer **APPROVE(0 issue)** — **후렴 게이트 하류 억제**·기본 FollowRegion1 무회귀(라이브/비-라이브)·ResolveRegion2Emphasis 정확성·위치 레코드 정렬(직전 enum 과 다른 enum 타입=swap 컴파일 캐치)·enum 풀 touchpoint·JsonStringEnumConverter 무손상 검증. 신규 테스트 7(렌더 전역On/Off 영역 독립·곡별우선·후렴게이트 억제, VM set/clear·Refresh·Reset). 1539 green(Release).

- (2026-06-02) ✅ 증분 86(보조영역 전역 기울임) — **보조 영역(Region2) 전역 기울임(3-상태)**. 증분85 의 `LyricsRegion2Emphasis` enum 과 `ResolveRegion2Emphasis` 헬퍼를 **그대로 재사용**(새 enum·헬퍼 없음). FindChangedKeys 도 기존 `SettingKey<LyricsRegion2Emphasis>` 케이스가 굵게·기울임 둘 다 커버(타입 스위치라 키 추가에 케이스 불필요). 우선순위 **곡별 region2 기울임 비트(Live 한정) > 전역(FollowRegion1 아니면 On/Off) > 본문(Region1) 기울임 추종**. 기본 FollowRegion1 무회귀, 후렴만 강조 게이트 하류 억제 동일. code-reviewer **APPROVE(0 critical)** — **인접 동일타입(굵게·기울임 둘 다 LyricsRegion2Emphasis) 위치 레코드 정렬**(decl·From() 둘 다 Bold→Italic 순서 char 단위 확인, 컴파일러가 못 잡는 swap 을 굵게/기울임 독립 단언 테스트가 가드)·FindChangedKeys 재사용 정확성·무회귀·enum 풀 touchpoint 검증. 신규 테스트 5(렌더 전역On/Off 영역 독립·곡별우선, VM set/clear·Reset). 1544 green(Release).

- (2026-06-02) ✅ 증분 87(보조영역 전역 밑줄) — **보조 영역(Region2) 전역 밑줄(3-상태)**. 강조 트리오(굵게85·기울임86·밑줄87) 완성 → **Region2 외형 전역 셋(크기·글꼴·색·정렬·굵게·기울임·밑줄) 완비**. `LyricsRegion2Emphasis` enum·`ResolveRegion2Emphasis` 헬퍼·FindChangedKeys 케이스 모두 재사용. 우선순위 곡별 비트 > 전역(FollowRegion1 아니면) > 본문 밑줄 추종. code-reviewer **APPROVE(0 issue)** — **이제 3개 연속 동일타입(굵게·기울임·밑줄 모두 LyricsRegion2Emphasis) 위치 레코드 정렬**(decl·From() 둘 다 Bold→Italic→Underline 순서 확인, 컴파일러가 못 잡는 3중 swap 을 각 효과 단일-인자-입력/단일-속성-단언 테스트로 교차 가드)·무회귀(FollowRegion1→region1Underline)·후렴 게이트 밑줄 억제·enum 풀 touchpoint·씬 와이어링 기존 검증. 신규 테스트 5(렌더 전역On/Off 영역독립·곡별우선, VM set/clear·Reset). 1549 green(Release).

- (2026-06-02) ✅ 증분 88(메인 창 크기·위치 저장/복원) — **레거시 FrmMain 창 상태 저장(레지스트리)의 WPF 대응**. 기존 WPF 는 매 실행 기본 크기로 시작했다(gap §3.6 line193 🟡). 5개 General 설정(MainWindowLeft/Top/Width/Height int, MainWindowMaximized bool) + **순수 테스트 가능 헬퍼 `WindowPlacementCalculator.ComputeRestore`**(저장 bounds 를 가상 데스크톱 안으로 클램프 — 화면 밖·모니터 분리·해상도 축소·최소 크기 보정) + 얇은 View 배선(OnSourceInitialized 복원, OnClosing 저장). 닫을 때 일반=현재 좌표·실제크기, 최대화/최소화=RestoreBounds(원래 크기) 저장 → 다음에 un-maximize 시 올바른 크기 복귀. 복원은 화면 안으로 보정 후 적용(미저장=폭 0 이면 XAML 기본 유지, 무회귀). View 코드는 try/catch 로 감싸 복원/저장 실패가 시작·종료를 막지 않게 함. _settings 는 nullable(GetService) 라 미해석 시 복원/저장만 건너뜀. LegacySettingsMap 5 엔트리(커버리지 테스트 AllCurrentSettingKeys 가 누락 잡아 추가). code-reviewer **APPROVE(0 critical)** — **화면 밖 안전 클램프 수학**(모든 비-null 입력이 [screenLeft,screenRight]×[top,bottom] 안, 유일 오버플로=최소폭>화면폭 퇴화 케이스도 title-bar 잡힘 가능으로 양성)·최대화 round-trip·5키 GET/SET/DTO/All/Legacy 완비(default throw 라 누락 시 런타임 throw)·null 회복력·저장 타이밍(Minimized→RestoreBounds 라우팅) 검증. 신규 테스트 11(헬퍼 엣지: 미저장 null·음수크기 null·정상 그대로·우/하 넘침 당김·음수위치 원점·화면초과 축소·최소미만 확대·최대화 플래그·듀얼모니터·잘못된 화면 null). 1560 green(Release).

- (2026-06-02) ✅ 증분 89(인스펙터 접힘 상태 저장) — **우측 출력 모양 인스펙터 펼침/접힘 상태 지속**(레거시 FrmMain 패널 상태 저장). 기존엔 IsInspectorExpanded 가 매 실행 기본 펼침으로 리셋됐다(gap §3.6 line193). 증분88 설정 인프라 재사용: SettingKey MainInspectorExpanded(bool, true) + General DTO/GET/SET/All/LegacySettingsMap. MainViewModel: OnIsInspectorExpandedChanged 가 토글을 설정에 저장, ApplyOperationalSettings 가 복원. 복원 중 되저장·재진입 방지 가드(`_isApplyingOperationalSettings`) — **try/finally 로 예외 안전**(리뷰어 MAJOR 반영: 복원 중 예외가 플래그를 true 로 가둬 이후 저장이 조용히 멈추는 일 방지). 재진입은 구조적으로도 안전(MainInspectorExpanded 는 ContainsOperationalSetting 화이트리스트 밖이라 Set→SettingsChanged 가 ApplyOperationalSettings 를 재호출 안 함 — 리뷰어 확인). code-reviewer **APPROVE(0 critical, MAJOR 1 반영)** — 재진입 루프 없음(운영키 필터+가드 2중)·가드 범위(인스펙터 1줄만)·5키 완비(default throw)·무회귀(기본 true)·생성 순서 NPE 없음 검증. 신규 테스트 1(토글 저장+새 VM 복원). 1561 green(Release).

- (2026-06-02) ✅ 증분 90(좌측 패널 splitter 비율 저장) — **브라우저/예배순서 패널 높이 비율 저장/복원**(레거시 FrmMain splitter 상태). 창 상태 트랙(88 창 bounds·89 인스펙터) 완비. 두 별(star) 행 비율을 int %(위 브라우저 %, 0=미저장)로 저장 + **순수 헬퍼 `ComputeSplitPercent`**(합≤0/음수=0 센티넬, 그 외 [15,85] 클램프 — 한 패널 찌그러짐 방지, MinHeight 와 이중 안전). 종료 시 ActualHeight 로 비율 계산 저장, 시작 시 OnSourceInitialized 에서 star GridLength(percent/100-percent) 적용(측정 전에도 동작·리사이즈에 비율 유지). 미저장(0)·범위 밖이면 XAML 기본 유지(무회귀). View 는 try/catch + nullable settings 로 안전. code-reviewer **APPROVE(0 critical)** — **0 센티넬 충돌 없음**(min=15>0 라 유효 입력 클램프 결과가 0 과 절대 안 겹침, top=0 도 15 로)·복원 가드 대칭([15,85] 포함)·star 비율(리사이즈 안전·Auto splitter 행 제외)·타이밍(OnSourceInitialized star 적용·OnClosing ActualHeight)·5키 완비·null/try-catch 회복력 검증. 리뷰어 제안(minPercent≥1 센티넬 불변식 doc) 반영. 신규 테스트 5(반반 50·비례 75/30·합0 센티넬·극단 클램프·유효입력 0 불가). 1566 green(Release).

- (2026-06-02) ✅ 증분 91(절 점프 키보드 매핑) — **숫자(1~9)·문자(c·b·t·w·e·p·q) 키 → 절/섹션 점프**(레거시 FrmMain PreviewBtnVerse_Click, gap §3.9 line213 🔴→🟢). 점프 로직(JumpToLyricsSectionCommand·AvailableSectionLabels)은 기존, 이번엔 키보드 배선만. 순수 헬퍼 `VerseJumpKeyMap.MapKeyToLabel`(D1-9/NumPad1-9→"1".."9", C/B/T/W/E/P/Q→소문자, 그 외 null) + OnPreviewKeyDown 에서 **포커스 가드**(수식 키 없음 + 텍스트박스/편집콤보 포커스 아님 + CanExecute(label) 일 때만). 팔레트 가드 다음, _shortcuts 앞에서 처리하되 CanExecute false 면 fall-through(없는 라벨·라이브 아님이면 단축키 정상). 라벨 대소문자 무시 매칭이라 곡 라벨이 "Verse 1" 식이면 graceful no-op. code-reviewer **APPROVE(0 critical)** — **타이핑 충돌 안전**(수식키+TextBoxBase/편집콤보 가드, 메인 창에 PasswordBox·편집 DataGrid 없음 grep 검증)·**bare 키 단축키 섀도잉 없음**(CommandCatalog 의 무수식 단축키는 F키·Space 뿐, 문자 단축키는 전부 Ctrl)·CanExecute 게이팅·대소문자 무시·NumPad 패리티 검증. 리뷰어 제안(향후 PasswordBox 추가 시 가드 확장) 주석 반영. 신규 테스트(VerseJumpKeyMap 숫자·문자·미매핑·NumPad 패리티). 1586 green(Release).

- (2026-06-02) ✅ 증분 92(전환효과 항목 vs 슬라이드 분리) — **항목 전환(곡→곡)과 슬라이드/절 전환(같은 곡 절·슬라이드 이동)을 각각 다른 효과로**(레거시 FrmMain 항목 vs 슬라이드 전환, gap §3.6 line142 🟡→🟢). 핵심 통찰: 같은 곡 절·슬라이드 이동은 같은 LiveQueueItem(같은 Id)로 재송출되고 다음 곡은 다른 Id → **item.Id 로 전환 종류 판별**. LiveSessionSnapshot 에 `CurrentItemId`(끝 추가) + GoLive 가 item.Id 실음. 새 설정 LyricsMonitorSlideTransitionKind(기본 Fade=기존 단일 전환과 동일, 무회귀). OutputWindowViewModel.ApplySession 이 콘텐츠 적용 **전에** `UpdateContentTransitionKindForChange`로 ContentTransitionKind 를 항목/슬라이드 종류 중 선택(_previousLiveItemId 비교, 빈 Id 는 슬라이드로). MainViewModel: 32-케이스 라벨 스위치를 `TransitionKindLabel` 정적으로 추출(항목 메뉴·슬라이드 콤보 공용)·`TransitionKindOptions`(전 종류)·ActiveSlideTransitionKind·SlideTransitionKindInput·RefreshActiveAppearance 동기화. 전환 효과 메뉴에 "슬라이드 전환" 콤보(StaysOpenOnClick) + 항목 모션 라벨 명확화. code-reviewer **APPROVE(0 issue)** — **항목 vs 슬라이드 판별**(절/슬라이드 재송출 같은 Id·다음 항목 다른 Id·첫 송출 항목전환·blackout/Restore 는 Id 보존→슬라이드 검증)·타이밍(ApplySession 선행·OnSettingsChanged 가 RefreshDisplayText 후 ApplyTransitionSettings 라 무충돌)·**인접 동일타입 string 끝추가 안전**(GoLive 만 위치 생성, named-arg)·설정 완비·30 enum 전수 라벨·UI 패턴 검증. 리뷰어 informational(화이트리스트 의도적 제외) 주석 반영. 신규 테스트 4(GoLive Id·ApplySession 항목/슬라이드 선택·VM 저장동기화·옵션 전수). 1590 green(Release).

- (2026-06-02) ✅ 증분 93(예배 순서 병합) — **저장된 예배 순서를 현재 큐 뒤에 이어 붙이는 병합**(레거시 .esw 병합, gap §3.4 line166). 불러오기(LoadWorshipListAsync)=대체와 달리 `MergeWorshipListAsync` 는 큐를 비우지 않고 항목을 append. 빈 큐 병합은 불러오기처럼 첫 항목 선택, 비어있지 않으면 현재 선택 유지. 세션 이름(CurrentWorshipListName)·최근 목록은 안 바꿈(결과는 두 순서 조합이라 어느 한쪽 이름이 아님). ManageWorshipListsWindow 에 "병합" 버튼(삭제·불러오기 사이). code-reviewer **APPROVE(0 blocking/major)** — **중복 Id 무해**(슬라이드 가드는 _liveItemId==SelectedItem.Id 로 선택 *참조* 비교, Id 룩업 아님; 병합 항목은 역직렬화 새 인스턴스라 참조 구별)·선택 의미·라이브 중 비파괴 append·없는/빈 목록 graceful(LoadAsync 빈 배열·catch 필터)·recent/name 미변경 합리·RefreshPowerPointLimitState(false) 상태메시지 보존·async void 내부 catch 검증. 신규 테스트 3(append 순서·빈큐 첫선택·빈이름 무동작). 1593 green(Release).

- (2026-06-02) ✅ 증분 94(텍스트/공지 항목을 예배 순서에 추가) — **자유 텍스트(공지) 항목을 예배 순서(큐)에 추가**(레거시 InfoScreen 항목, gap §3.4 line166). 기존엔 NoticeScreen 이 텍스트를 즉시 송출(모달)만 했는데, 이제 "순서에 추가"로 큐에 저장해 나중에 다른 항목처럼 골라 송출 가능. MainViewModel.AddTextItem: 종류=Notice·고유 Id `text:{guid}`(센티넬 NoticeLiveId 와 구별)·제목=첫 줄(30자 줄임)·본문(Lyrics)=전체. NoticeScreenViewModel ctor 끝에 옵셔널 `addToWorshipQueue` 콜백 추가(무회귀)·AddToQueueCommand(콜백 있고 텍스트 있을 때만). 공지 창에 "순서에 추가" 버튼. code-reviewer **APPROVE(0 defect)** — **Notice 큐 항목 go-live 정확성**(ComputeBodyText 가 Kind==Notice 로 Lyrics 그대로 렌더, Id 무관; IsLyricsPaginated 가 Notice 제외라 LyricsPageCount=0 → 절/슬라이드 nav·절점프 자연 비활성)·JSON named-property round-trip(Kind·Lyrics 영속)·옵셔널 ctor 무회귀(named-arg store:null 후 addToWorshipQueue:)·제목 자름 안전(Hangul BMP·길이 가드)·삽입 위치 기존 AddBibleSelection 패턴 일관 검증. 신규 테스트 6(AddTextItem Notice종류·첫줄제목·전체본문·빈무동작·긴제목줄임; AddToQueueCommand 콜백호출·콜백없으면비활성·빈텍스트비활성). 1599 green(Release).

- (2026-06-02) ✅ 증분 95(선택 항목 복제) — **예배 순서 항목 복제(모던 WPF UX)** — 같은 곡을 예배 중 두 번 부르는 등. record `with { Id = $"dup:{guid}" }` 로 모든 내용(가사·서식·번호·종류)을 복사하되 새 Id 부여(중복 Id 로 라이브 추적·재송출 혼선 방지). IndexOfReference(참조)로 원본 위치 찾아 바로 뒤에 삽입·복제본 선택. WorshipListPanel 에 복제 버튼(Copy24). code-reviewer **APPROVE(0 critical)** — **공유 PreviewSource(ImageSource) 무해**(frozen BitmapSource 라 불변·공유 표준, go-live 시 ResolveLiveProjection 이 ContentPath 로 새로 렌더, 직렬화 안 됨)·새 Id 정확(Id 접두사 파싱 없음, dispatch 는 Kind)·삽입 위치(Insert index+1, 마지막 항목도 append 유효)·라이브 안전(복제본 새 Id 라 비활성, 원본 라이브 유지)·save/load·CanExecute(OnSelectedItemChanged→NotifyCommandStates) 검증. 신규 테스트 3(복제 내용·위치·선택; 무선택 무동작; CanExecute 선택 추종). 1602 green(Release).

- (2026-06-02) ✅ 증분 96(맨 위로/맨 아래로 이동) — **선택 예배 순서 항목을 맨 위/맨 아래로 한 번에 이동**(모던 재정렬 UX — 여러 번 ↑↓ 클릭·드래그 없이). 기존 검증된 `MoveQueueItem`(참조 기반·클램프·선택 유지) 재사용. CanMoveSelectedToBoundary 로 이미 끝이면 비활성(불필요한 이동 방지). WorshipListPanel 에 맨위(ArrowUpload24)·맨아래(ArrowDownload24) 버튼. code-reviewer **APPROVE(0 issue)** — 경계/빈 큐 안전(SelectedItem 가드+MoveQueueItem 클램프)·CanExecute 정확(index>0 / index<Count-1, 참조 기반)·선택 유지·NotifyCommandStates 갱신 검증. 신규 테스트 2(맨위/맨아래 재배치+선택유지·경계 CanExecute). 1604 green(Release).

- (2026-06-02) ✅ 증분 97(항목 복제 Ctrl+D 단축키 + 팔레트) — **선택 항목 복제(증분95)에 Ctrl+D 단축키 + 명령 팔레트 검색**(모던 UX 보강). MainCommandIds.WorshipDuplicateItem + CommandCatalog 항목("예배 순서" 카테고리, Ctrl+D Shortcut) + BindShortcuts 의 BindGated(→DuplicateSelectedItemCommand). 기존 카탈로그 테스트(고유 Id·알려진 명령 참조·충돌 없음)가 새 항목 자동 검증 통과. code-reviewer **APPROVE(0 issue)** — Ctrl+D 충돌 없음(테스트 통과, TextBox 편집 제스처 아님, Ctrl+B 와 동일 전역 경로)·게이팅(BindGated→CanExecute 선택 있을 때만)·팔레트 실행 경로(registry.Bind 공용, LiveAutoRotate 동일)·새 카테고리 무해 검증. 신규 테스트 1(Ctrl+D 매핑+팔레트 존재). 1605 green(Release).

- (2026-06-02) ✅ 증분 98(곡 DB 존재 검증) — **예배 순서 검증에 곡 DB 존재 점검 추가**(레거시 ValidateWorshipListItems 의 DB 차원, gap §3.4 line171 — 증분29 가 "비동기·DB 의존"으로 미룬 부분). 기존 파일 검사(WorshipListValidator)는 유지하고, song:{id} 항목을 가사 DB(_songDetail.GetSongDetailAsync)에 있는지 비동기 확인 → 없으면 새 SongNotInDatabase 경고. 검증 명령을 AsyncRelayCommand 로 전환. **무회귀 설계**: DB 경로(Search.DatabasePath)가 없으면 곡 검사 블록을 건너뛰어 await 에 닿지 않아 동기적으로 완료 → 기존 동기 .Execute(null) 테스트 그대로 통과. TryGetSongDatabaseId 가 "song:{int}" 만 파싱(dup:·text:·bible:·ppt: 제외 — 복제는 가사 보유라 검사 불요). **조용한 생략 방지**(리뷰어 지적): DB 경로 없는데 곡이 있으면 상태바에 "(곡 DB 검증 생략 — DB 경로 없음)" 명시. code-reviewer **APPROVE(0 blocking)** — 동기 완료 무회귀 추론·파싱 엣지(AsSpan(5)·빈 span TryParse false·null 가드)·async 명령 전환 소비자 무영향(XAML ICommand)·상태 메시지 통합 검증, 조용한 생략 MINOR 반영. 신규 테스트 4(곡 없음→경고·곡 있음→정상·DB경로 없음→생략+힌트, 기존 3 동기 테스트 무회귀). 1608 green(Release).

- (2026-06-02) ✅ 증분 99(라이브러리 곡 → 예배 순서 드래그-드롭) — **곡 목록에서 곡을 끌어 예배 순서에 드롭**(레거시 외부 소스 드래그, gap §3.4 line168). 증분43(성경 본문→큐 드래그) 패턴을 그대로 확장. VM `AddSongRelativeTo`(동기 — 라이브러리 곡은 가사 보유) 가 AddBibleSelectionRelativeTo 와 동일하게 IndexOfReference(참조)로 드롭 위치 앞에 삽입(같은-값 중복 안전). QueueList_DragOver/Drop 에 SongSummary 분기 추가, LibrarySongList 에 드래그 소스(PreviewMouseDown 항목 위 무장→PreviewMouseMove 임계 거리 시작, 성경 드래그와 동일). code-reviewer **APPROVE(0 issue)** — 승인된 성경 드래그 패턴과 충실한 패리티·참조 기반 삽입 중복 안전·드래그 소스 무장(빈 영역 제외)·드래그/선택/더블클릭 무간섭·드롭 타입 분기·record 불변·라이브/빈큐 무크래시·SelectedSong 동기 선택 검증. 신규 테스트 3(타깃 앞 삽입+내용+선택·타깃 없으면 끝·null 무동작; View 드래그는 성경 드래그와 동일하게 무테스트 패리티). 1611 green(Release).

- (2026-06-02) ✅ 증분 100(Word 문서를 텍스트 항목으로 추가) — **Word 항목**(레거시, gap §3.4 line166). OfficeLib.WordDoc.GetContents(기존 — try/finally 로 Document·Application COM 해제, 실패 시 빈 문자열)로 **본문 텍스트만 추출**해 검증된 AddTextItem(증분94) 재사용 → Notice 항목으로 큐에 추가. "외부 파일 추가" 필터/분기에 .doc/.docx 추가(.ppt→PPT, .doc→Word텍스트, 그 외→미디어). **검증 시임 분리**: 인터롭 추출은 View, 빈 결과 처리(안내 메시지·무추가) 판단은 VM `AddWordTextItem`(테스트 2). **정직성**: 이미지 페이지 렌더가 아니라 텍스트만(OfficeLib 가 텍스트 추출만 제공) — 긴 문서는 한 화면, 페이지네이션 후속(gap 문서 명시). Word 미설치·읽기 실패는 graceful(빈 문자열→안내). 리뷰어 지적 반영: Word COM 은 STA 의존이라 Task.Run(MTA) 대신 `await Dispatcher.Yield(Render)` 로 "읽는 중" 상태를 먼저 그린 뒤 UI/STA 스레드에서 읽음. code-reviewer **APPROVE(0 critical)** — 정직한 실인터롭(스텁 아님)·COM 무누수(fire-and-forget, GetContents finally 해제)·검증 시임 분리·graceful 실패·필터 분기 순서/소문자화 검증. 신규 테스트 2(텍스트→Notice 항목·빈/공백/null→안내 무추가). 1613 green(Release).

- (2026-06-02) ✅ 증분 101(legacy .esw 파서, 1단계) — **레거시 .esw(EasiSlides v3.2 예배 순서) 파서**(gap §3.4 line172). **사실 정정**: 레거시 .esw 는 "바이너리"가 아니라 **XML**임을 레거시 소스(FrmMain.Logic.cs LoadIndexFile=XmlTextReader, line1389)로 확인. 순수 `EswWorshipListParser.Parse(xml)` → `EswWorshipListItem(TypeCode·Id·Title·Folder·FormatData)` 추출: XDocument.Descendants("Item")로 항목만(헤더 SystemID/FormatData/Notes 제외), ItemID 첫글자=종류코드(D=곡/P=PPT/B=성경)·나머지=식별자(레거시 Left/Right 규칙), 손상 XML→빈 목록 graceful. code-reviewer **APPROVE(0 critical)** — 레거시 read 구조 충실 포팅(타입/Id 분할·헤더 제외·빈 ItemID 스킵 모두 일치 소스 대조)·예외 범위(XmlException 충분)·Substring 안전·Trim 정규화 안전·**1단계 스코핑 정직성**(종류코드 매핑은 전체 코드셋 검증 후 — 추측 회피) 검증. 신규 테스트 6(샘플 .esw→D/P/B 추출·헤더 FormatData 비혼동·null/빈/공백·손상 XML graceful·빈 ItemID 스킵·1글자 ItemID·자식 누락 기본값). 1619 green(Release). 종류코드→WPF 매핑·내용 해석·파일 가져오기는 2단계 후속.

- (2026-06-02) ✅ 증분 102(legacy .esw 가져오기, 2단계 — 매핑·가져오기·파일 픽커) — **레거시 .esw 종류코드→WPF 항목 매핑 + 파일 가져오기 UI**(gap §3.4 line172, 증분101 파서 위에). **전체 종류코드 검증**: 레거시 `WriteItemtoWorshipList`(FrmMain.Logic.cs 1798-1923) 전수로 D=DB곡·P=PowerPoint·B=성경·T=텍스트·I=InfoScreen·W=Word·M=미디어 확정 후 매핑(추측 회피). `BuildEswQueueItem`: **D→곡**(esw.Id int 파싱 후 라이브러리에 같은 SongId 있으면 가사·번호·저작권까지 온전히, 없거나 비숫자면 제목만 — 가사 미해석)·**P→PowerPoint**·**M→미디어**(둘 다 esw.Id 를 ContentPath 로 보존)·**B→성경 참조**(본문 미확장 — 레거시 ItemID 는 WPF BibleSelectionId 와 다른 인코딩이라 무리 확장 회피)·**T/I/W/미상→텍스트(공지)**(제목을 본문으로). `ImportEswWorshipList`(LoadQueue 패턴: Clear→매핑 add→첫 항목 선택→상태→RefreshPowerPointLimitState(false, 가져옴 안내 보존)→NotifyCommandStates, 입력 null 가드). 파일 메뉴 "레거시 예배 순서(.esw) 가져오기..."(OpenFileDialog→ReadAllText[IOException/권한 가드]→Parse→**빈/손상 파일은 현재 큐 보존 후 안내**→Import) + MainCommandIds·CommandCatalog("예배 순서")·메뉴 배선. **최선 노력 정직성**: 순서·제목·종류·곡 가사(라이브러리)는 복원하되 PPT/미디어 경로 재해석·성경 본문·라이브러리 밖 곡 가사는 운영자 보정(코드·주석·gap 문서 명시 — 풀 내용 재구성 안 함). code-reviewer **APPROVE(0 critical, 0 major)** — 레거시 save/load 왕복 대조로 종류코드 의미 검증(P/M id=파일참조·B는 신구 인코딩 불일치라 참조 유지가 정답)·LoadQueue 형제 패리티·**정직성(과대표현 없음)**·파일 픽커 가드(IOException/권한·빈 가드로 작업물 보존)·테스트 충분성 검증. 리뷰어 minor 4 반영(입력 null 가드·updateStatus:false 의도 주석·FormatData 보존 테스트·D 비숫자 식별자 테스트). 신규 테스트 8(종류코드→종류 매핑+큐 교체·D 라이브러리 가사 채움·D 미존재 제목만·빈 제목→Id·빈 목록→Clear·FormatData 전 분기 보존·D 비숫자→제목만·null→예외 + 메뉴 배선 1). 1628 green(Release).

- (2026-06-02) ✅ 증분 103(출력 글꼴 콤보 = 설치된 시스템 글꼴 전체) — **인스펙터 글꼴 선택을 추천 10종 → 설치 글꼴 전체로 확장**(현대적 WPF UX 업그레이드, gap §3.2 line75 🟡→🟢). 글꼴 콤보 VM·XAML(본문 Region1·보조 Region2 편집 콤보)은 이미 있었고, 이번엔 **목록**을 확장. 순수 헬퍼 `SystemFontCatalog.BuildFontFamilyList(installed, favorites)`: 추천 글꼴 맨 앞(순서 유지)·설치 나머지 가나다/ABC 순(문화권 정렬)·**대소문자 무시 중복 제거(seen 집합 한 곳)**·빈 이름 제거·앞뒤 공백 다듬기·설치 열거 null/빈이면 추천만(무회귀). `LyricsFontFamilyOptions` 를 ObservableCollection 으로(추천 시드) + `MergeInstalledFonts`(헬퍼 결과로 Clear+Add → 바인딩 콤보 즉시 갱신). View(MainWindow_Loaded)는 `EnsureInstalledFontsMergedOnce`로 1회 `Fonts.SystemFontFamilies.Source` 열거해 병합(열거=View 책임/환경 의존, 계산=검증된 헬퍼). **편집 콤보라 Text 바인딩(선택값)과 ItemsSource(목록)가 분리** → 목록 교체가 운영자가 고른/입력한 글꼴을 건드리지 않음(회귀 안전). code-reviewer **APPROVE(0 critical, 0 major)** — Clear+Add 가 Text 선택 안 건드림(Text≠SelectedItem, 하이드레이션 선행)·헬퍼 병합/정렬/중복 정확·1회 가드 멱등(EnsureLibraryLoadedOnce 패턴)·ObservableCollection 바인딩 무회귀·순수헬퍼+얇은View 패턴 검증. 리뷰어 minor 반영(중복 제거를 seen 한 잣대로 단일화해 죽은 가드·정렬/중복 비대칭 제거·`.Source`=영문 불변명 한계 주석/후속 명시·입력 보존 회귀 테스트·멱등 테스트 추가). 신규 테스트 10(헬퍼 6: 추천앞+설치정렬·대소문자중복·null/빈→추천만·미설치 추천유지·빈/공백 제거·추천순서유지; VM 4: 병합 추천앞+중복없음·null 추천유지·**입력 글꼴 보존**·**2회 병합 멱등**). 1638 green(Release). 항목별(per-item) 인스펙터 글꼴·한글 표시명(FamilyNames[ko-kr]) 노출은 후속.

- (2026-06-02) ✅ 증분 104(미디어 플레이어 전역키) — **라이브 미디어 재생 중 키보드 제어**(레거시 FrmMain.HandleMediaPlayerKey Esc/Space/Enter/S/M, gap §3.9 line212 🔴→🟢). 레거시 충실 매핑: Space=재생/일시정지·Enter=처음부터 다시·S=정지·Esc=정지·M=음소거. 순수 `MediaPlayerKeyMap.MapKey`(키→동작) + `MediaPlayerKeyRouter.Resolve`(우선순위·포커스·실행가능 결정) + MediaPlaybackViewModel 에 `RestartCommand`(Seek(0)+Play, 레거시 Remote_ResumeItemFromStart). View(TryHandleMediaPlayerKey)는 상황만 모아 라우터에 위임. **안전 게이팅**: 미디어 명령 4종이 모두 CanUsePlaybackControls(Empty 면 비활성, 항목 전환 시 Unload→Empty) → 미디어 없으면 라우터가 None 반환해 **Space=다음 항목 등 평소 단축키 유지**; **버튼 포커스 시 Space/Enter 는 그 버튼 우선**(Esc/S/M 은 버튼 키 아니라 그대로 동작). 미디어 컨트롤에 ↺(처음부터) 버튼 + 키 힌트 툴팁(Space/Enter/Esc·S/M). code-reviewer **APPROVE(0 critical, 0 major)** — 초판에서 리뷰어가 **MAJOR(창 레벨 PreviewKeyDown 이 포커스된 버튼의 Space/Enter 가로챔)** 지적 → 라우터에 ButtonBase 포커스 가드(Space/Enter만) 추가로 해결, 재검증 APPROVE. 레거시 save/load·Remote_* 대조로 매핑 충실성·CanExecute 게이팅이 "훅 활성=미디어 라이브" 조건의 정직한 등가·MediaCommandFor↔None 짝·정직성(Esc 의 영상숨김=Unload 는 후속이라 주석/문서 명시) 검증. 신규 테스트 19(MapKey 11: 각 키→동작·Enter=Return·미매핑5; Router 6: 미디어라이브→동작·미디어없음→None(Space 살아있음)·수식키→None·텍스트포커스→None·**버튼포커스 Space/Enter→None·Esc/S/M 동작**·미매핑→None; VM Restart 2: 되감고재생·미디어없음 비활성, +Unload 4종 비활성 보강). 1657 green(Release). 레거시 Esc 의 "영상 숨김→가사 복귀"(Unload)는 후속.

- (2026-06-02) ✅ 증분 105(미디어 컨트롤 아이콘 현대화) — **미디어 전송 버튼을 텍스트 글리프(◀◀ ▶▶ ■ ↺)에서 Fluent SymbolIcon 으로 업그레이드**(지시문 "아이콘·디자인 현대적 WPF" 직접 대응). EsIcons 에 미디어 7종(MediaPause/Stop/FastForward/Rewind/Restart/Mute/Unmute/Repeat) 추가. MediaPlaybackViewModel 에 **상태 구동 아이콘** `PlayPauseSymbol`(재생↔일시정지)·`MuteSymbol`(소리켜짐↔음소거) — 기존 `PlayPauseLabel` 선례와 동일 패턴(ApplySnapshot 에서 함께 설정). 되감기/재생일시정지/처음부터/정지/빨리감기 5종은 아이콘만(순간 동작이라 상태 모호성 없음), 음소거·반복 토글은 **아이콘+글자 라벨**(켜짐/꺼짐 한눈에). 모든 버튼 AutomationProperties.Name(한국어) 유지 → 스크린리더 무손실. code-reviewer **APPROVE(0 critical)** — 초판에서 리뷰어가 **MAJOR(아이콘만 토글: 정적 음소거 아이콘이 "지금 음소거 중"으로 오해, 켜짐/꺼짐 시각 약함)** 지적 → 음소거 아이콘 상태 구동(MuteSymbol)으로 정직하게 + 두 토글 아이콘+라벨로 해결. VM→SymbolRegular 노출은 기존 PlayPauseLabel 선례와 일관(EsIcons 도메인 추상 의존)·아이콘 의미·접근성 이름 유지·무회귀 검증. 리뷰어 minor 반영(아이콘만 버튼 접근성 이름 회귀 테스트·글리프 비교 Content="" 정밀화·PlayPauseLabel 유지 사유 주석). 신규 테스트 17(VM 2: PlayPauseSymbol·MuteSymbol 상태 토글; XAML 15: 정적 아이콘 5·상태구동 바인딩 2·접근성 이름 7·옛 글리프 제거 1). 1674 green(Release).

- (2026-06-02) ✅ 증분 106(절 순서 인라인 편집) — **선택한 곡의 절 순서(Sequence)를 예배 순서 패널에서 직접 편집**(레거시 절 순서 모델, gap §3.5 line183 🔴→🟡). 기존엔 곡 DB 의 Sequence 가 추가 시 실리기만 하고 UI 에서 못 바꿨는데, 이제 운영자가 "1 2 C 3 C C"(후렴 반복 등)를 입력칸에 적어 즉시 재페이지네이션·라이브 재송출. `CanEditSelectedItemSequence`(곡+가사일 때만 활성)·`SelectedItemSequenceInput`(양방향, LostFocus)·`CommitSelectedItemSequence`(빈입력→null 선형 복귀·앞뒤공백 다듬기·같은값 무시·레코드 교체+재선택+RepublishLiveSongForBodyChange) — **기존 ToggleUseIndividualFormatting 패턴을 그대로 미러**(레코드 불변 교체·참조 일치·라이브 절 보존). WorshipListPanel 에 입력칸(곡 아니면 IsEnabled=false). OnSelectedItemChanged 가 입력칸·활성 상태 통지. code-reviewer **APPROVE(0 critical, 0 major)** — 미러 패턴 정확성(라이브 절 0 안 튐=세션 실제 절 재송출)·바인딩 피드백 루프 없음(LostFocus+같은값 가드)·곡 한정 게이팅 정직(성경은 [라벨] 없어 무의미라 제외)·참조 일치 중복 안전·정규화 검증. 리뷰어 minor 반영(라이브 경로 테스트 추가·페이지 수 축소 시 위치 라벨 "4/3" 클램프 근본 수정·편집 후 AvailableSectionLabels 재계산 단언). 신규 테스트 5(설정+페이지확장+입력반영+라벨재계산·빈입력→선형·CanEdit 게이팅·비곡 무시·라이브 절 보존). 1679 green(Release). 항목별 색·글꼴·정렬 인라인 즉시 편집은 후속.

- (2026-06-02) ✅ 증분 107(항목별 글자색 인라인 편집) — **선택한 곡만 글자색 변경**(레거시 Ind_ 항목별 색, FormatData 코드29, gap §3.5 line183 — 증분106 절 순서에 이어). 우클릭 "이 항목 글자색" 프리셋(흰/노랑/하늘/연두/주황) + "전역 기본색(해제)". 기존 검증된 `SongFormatData.Parse/Encode`(왕복 보장) 재사용 — 코드29만 바꾸고 나머지 곡별 서식(배경·정렬·폰트·영역2)은 보존. `SetSelectedItemTextColor(hex)`: HexToArgb(빈/오류→null=해제)·같은색 무시·Parse→with{TextColorArgb1}→Encode(빈→null)·레코드 교체+재선택+RepublishLiveSongForBodyChange. **색을 주면 개별 서식 자동 켬**(끄면 FormatData 무시되므로) + 그 사실 상태줄 정직 고지. `CanEditSelectedItemColor`(곡+가사)·`SelectedItemTextColorHex`(미리보기)·`SetSelectedItemTextColorCommand`(우클릭 바인딩). **증분106 패턴 미러**. code-reviewer **APPROVE(0 critical, 0 major)** — Parse→with→Encode 타 필드 보존·빈→null 정규화·UseIndividualFormatting 자동 켬 정당(라이브 색 게이팅)·라이브 절 0 안 튐·int? 같은색 가드·ContextMenu CommandParameter=""→해제 검증. 리뷰어 minor 반영(자동 켬 상태줄 고지·비0절 라이브 절 보존 테스트·OFF→재켬 테스트). 신규 테스트 7(코드29 기록+개별켬+hex반영·타 서식 보존·해제·라이브 오버라이드·비0절 절 보존·OFF 재켬 고지·비곡 무시+명령 비활성). 1686 green(Release). 항목별 글꼴·정렬·배경 인라인은 후속.

- (2026-06-02) ✅ 증분 108(항목별 정렬 인라인 + 색·정렬 공통 헬퍼) — **선택한 곡만 가로 정렬 변경**(레거시 Ind_ 정렬, FormatData 코드31, gap §3.5 line183 — 증분107 색에 이어) + **색/정렬 공통 경로 리팩터**. 종단 확인: `LiveSessionService` 가 이미 FormatData Alignment1(코드31)→`OverrideTextAlignment`(MapAlignment 1/2/3=Left/Center/Right)로 라이브 송출에 반영 → 정직한 종단 기능(스텁 아님). 우클릭 "이 항목 정렬" 왼쪽/가운데/오른쪽 + "전역 기본 정렬(해제)". **공통 헬퍼 `ApplySelectedSongFormatChange(mutate, wantsIndividual, out turnedOn)`** 추출 — 한 필드만 바꾸는 mutate 델리게이트로 색·정렬이 공유(Parse→mutate→**레코드 동등 무변경 가드**→Encode(빈→null)→레코드 교체+재선택+Republish). 색 메서드는 이 헬퍼를 쓰는 얇은 래퍼로 리팩터(증분107 동작 그대로 — 색 테스트 7개 무변경 통과). `SetSelectedItemAlignment`(1~3 검증·0/오류→해제)·`SelectedItemAlignment`(0=없음 미리보기)·`SetSelectedItemAlignmentCommand`. code-reviewer **APPROVE(0 critical, 0 major)** — 리팩터 동작 보존 증명(record 값-동등이 per-field 가드를 일반화·타 필드 동일이면 동등)·코드31 왕복·1..3 검증·라이브 OverrideTextAlignment·공통 게이트 재사용(이름 호환 유지)·mutate 델리게이트 DRY 검증. 리뷰어 minor 반영(정렬 같은값 무교체 테스트 추가; 게이트 이름 일괄 리네임은 다음 공용 기능 때로 연기). 신규 테스트 8(정렬 1/2/3 기록+개별켬·기존색 보존·0해제·라이브 오버라이드·같은값 무교체·비곡 무시+명령 비활성). 1694 green(Release). 항목별 글꼴·크기·배경 인라인은 후속.

- (2026-06-02) ✅ 증분 109(항목별 글자 크기 인라인) — **선택한 곡만 글자 크기 변경**(레거시 Ind_ 크기, FormatData 코드47 pt, gap §3.5 line183 — 증분108 정렬에 이어). 증분108 공통 헬퍼 `ApplySelectedSongFormatChange` 위의 얇은 래퍼(정렬과 동일 패턴). 종단 확인: `LiveSessionService` 가 FontSize1(코드47)→`OverrideFontSizePx`(LegacyPointToPixel pt*96/72 반올림·8~240 클램프)로 라이브 송출에 반영 → 정직한 종단(스텁 아님). 우클릭 "이 항목 글자 크기" 작게(36)/보통(54)/크게(72)/아주크게(90) + "전역 기본 크기(해제)". `SetSelectedItemFontSize`(6~100 검증·0/범위밖/오류→해제)·`SelectedItemFontSize`(0=없음)·`SetSelectedItemFontSizeCommand`. code-reviewer **APPROVE(0 critical, 0 major)** — 코드47 왕복·6~100 검증(VM+ParseFontSize 이중 방어 정당)·pt→px 라이브(72pt→96px)·공통 헬퍼로 무변경가드/타필드보존/개별자동켬/재송출 동일 이득·프리셋 전부 유효범위·라벨↔값 일치 검증. 리뷰어 minor 반영(포함 경계 6/100 수용·101 해제 테스트 추가로 뮤테이션 갭 차단). 신규 테스트 10(크기 6/36/72/90/100 기록+개별켬·0/5/101/200/abc 해제·기존색 보존·라이브 px·비곡 무시+명령 비활성). 1707 green(Release). 항목별 글꼴명·배경 인라인은 후속.

- (2026-06-02) ✅ 증분 110(항목별 글꼴명 인라인) — **선택한 곡만 글꼴 변경**(레거시 Ind_ 글꼴, FormatData 코드43, gap §3.5 line183 — 증분109 크기에 이어). 공통 헬퍼 위 얇은 래퍼. 종단 확인: `LiveSessionService` 가 FontName1(코드43)→`OverrideFontName`(NormalizeFontName 공백→null/Trim)로 라이브 반영 → 정직한 종단. 우클릭 "이 항목 글꼴" 맑은 고딕/나눔고딕/굴림/Segoe UI/Arial(자주 쓰는 한·영 빠른 선택) + "전역 기본 글꼴(해제)". `SetSelectedItemFontName`(빈/공백→해제·Trim)·`SelectedItemFontName`·`SetSelectedItemFontNameCommand`. **문자열 "" 센티넬**(int? null 형제와 다름)이라도 레코드 동등 무변경 가드가 정확히 동작(FontName1 기본 ""). code-reviewer **APPROVE(0 critical, 0 blocking)** — "" 센티넬 무변경 가드·공백 포함 글꼴명(맑은 고딕/Segoe UI) 왕복(IndexOf('=')·'>' 분리 보존)·Trim·라이브 OverrideFontName·공통 헬퍼 동일 이득 검증. **리뷰어 MAJOR(잠재): FormatData 구분자 '>'가 글꼴명에 섞이면 포맷 손상·코드 주입** 지적(SongFormatData 전반의 기존 설계 갭) → 본 증분 진입점(setter)에서 '>'·'=' 제거로 방어(근본 차단). 리뷰어 minor 반영(같은글꼴 무교체 테스트·구분자 정화 테스트). 신규 테스트 8(코드43 기록+Trim+개별켬·빈/공백 해제·기존색 보존·라이브 글꼴·비곡 무시+명령 비활성·같은글꼴 무교체·구분자 정화 무주입). 1715 green(Release). SongFormatData 포맷 차원의 '>' 이스케이프(타 문자열 필드 포함)는 후속 과제. 항목별 배경 인라인은 후속.

- (2026-06-02) ✅ 증분 111(항목별 배경색 인라인) — **선택한 곡만 배경색 변경**(레거시 Ind_ 배경, FormatData 코드26, gap §3.5 line183 — 6번째 인라인 서식, 증분107 글자색의 정확한 미러). 공통 헬퍼 위 얇은 래퍼. 종단 확인: `LiveSessionService` 가 BackgroundColorArgb1(코드26)→`OverrideBackgroundColorArgb`로, `OutputRenderer` 가 Live+개별서식 게이트 통과 시 단색 채움(그라데이션 억제)으로 라이브 반영 → 정직한 종단. 우클릭 "이 항목 배경색" 검정/진회색/네이비/짙은보라/짙은녹색(가사 가독 위해 어두운 프리셋) + "전역 기본 배경(해제)". `SetSelectedItemBackgroundColor(hex)`·`SelectedItemBackgroundColorHex`·`SetSelectedItemBackgroundColorCommand`. code-reviewer **APPROVE(0 critical, 0 major)** — 코드26 왕복(region2 코드27과 분리 무혼선)·라이브 렌더 실제 동작(UseIndividualFormatting+Live 게이트·단색 강제·그라데이션 억제 확인)·공통 헬퍼 DRY 무중복·타 facet(글자색29) 보존 검증. 리뷰어 minor 반영(배경 **이미지**(코드61)가 배경색을 덮는 상호작용 — 메서드 doc·메뉴 주석에 명시). 신규 테스트 5(코드26 기록+개별켬+hex반영·기존 글자색 보존·해제·라이브 오버라이드·비곡 무시+명령 비활성). 1720 green(Release). **항목별 인라인 포맷 6종(절순서·글자색·정렬·크기·글꼴·배경색) 완료 → gap §3.5 line183 🔴→🟢.** 배경 이미지(코드61) 인라인만 후속.

- (2026-06-02) ✅ 증분 112(항목별 배경 이미지 인라인) — **선택한 곡만 배경 이미지 변경**(레거시 Ind_ 배경 이미지, FormatData 코드61 — 인라인 서식 7번째이자 마지막 facet). VM=얇은 래퍼+View=파일 픽커. 종단 확인: `LiveSessionService`가 BackgroundImagePath(코드61)→`OverrideBackgroundImagePath`(NormalizeImagePath)로 경로 운반, 출력 창 VM 이 파일을 읽어 배경색 위에 표시 → 정직한 종단(경로만 기록, 로딩은 출력 VM). 우클릭 "이 항목 배경 이미지 → 이미지 선택..."(WorshipListPanel 코드비하인드 OpenFileDialog) + "전역 기본 배경(해제)"(빈 경로 명령). `SetSelectedItemBackgroundImage(path)`(빈/공백→해제·Trim·**'>'만 제거**[경로 구분자, '='는 경로 허용이라 보존])·`SelectedItemBackgroundImagePath`·`SetSelectedItemBackgroundImageCommand`. code-reviewer **APPROVE(0 critical, 0 blocking)** — 코드61 왕복·**비대칭 정화('>'만, '=' 보존) 정당**(Parse 첫'='로 키 분리→값의 '=' 생존, '>'가 진짜 주입 벡터)·숫자/빈 경로 잔여 위험 없음·View=픽커/VM=로직 깔끔·핸들러 재가드 정당·주입 테스트 양면 검증·정직성(경로만 기록, 존재검증 안 함은 전역 배경과 동일) 검증. 리뷰어 minor 반영(같은경로 무교체 테스트·배경색↔이미지 상호참조 doc). 신규 테스트 7(코드61 기록+Trim+개별켬·기존색 보존·해제·라이브 경로 운반·'>'정화 무주입(양면)·같은경로 무교체·비곡 무시+명령 비활성). 1727 green(Release). **항목별 인라인 포맷 7종 전부 완료.**

- (2026-06-02) ✅ 증분 113(항목별 강조 인라인 토글: 굵게/기울임/밑줄) — **선택한 곡만 강조**(레거시 Ind_ 굵게/기울임/밑줄, FormatData 코드41 비트 — 인라인 서식 8번째, Region1). 종단 확인: `LiveSessionService`가 Bold1/Italic1/Underline1(코드41 bit0/1/2)→`OverrideBold1/Italic1/Underline1`(`==true?true:null`)로 라이브 반영 → 정직한 종단. 우클릭 "이 항목 강조" 굵게/기울임/밑줄(IsChecked OneWay 상태표시 + 토글 명령). `ToggleSelectedItemBold/Italic/Underline()` + `ToggleSelectedItemEmphasis`(현재값 읽어 반전·켤 때만 wantsIndividual)·`SelectedItemBold/Italic/Underline`(체크 바인딩). **코드41은 6비트 합산 단일 정수**라 한 비트만 토글해도 record `with`+Encode 가 나머지 5비트 보존(굵게+기울임=41=3, 굵게만 끄면 41=2). code-reviewer **APPROVE(0 critical, 0 major)** — 비트 보존(record 6비트 OR 재합산)·on→off→41제거 왕복·이중파싱 무해(토글은 이전값 필요·in-memory)·wantsIndividual=newValue 정당(켤때만 개별켬)·**전역/후렴 강조 상호작용 무회귀**(per-item OFF→null→전역 추종, ON→전역 OFF 이겨, 후렴게이트는 항목별·전역 동일 적용=의도)·IsChecked OneWay+명령 패턴 검증. 리뷰어 minor 반영(한 비트만 끌 때 다른 비트 보존 테스트·후렴 게이트 주석). 신규 테스트 6(굵게 토글 on/off+41제거·굵게+기울임=41=3·굵게만 끄면 41=2 보존·밑줄+기존색 보존·라이브 OverrideBold1·비곡 무시+3명령 비활성). 1733 green(Release). Region2/이중언어 항목별 강조는 후속.

- (2026-06-02) ✅ 증분 114(이중 언어 보조 영역 Region2 항목별 글자색) — **선택한 곡만 보조 영역(이중 언어) 글자색**(레거시 Ind_ Region2 색, FormatData 코드30 — Region2 첫 facet). 종단 확인: `LiveSessionService`가 TextColorArgb2(코드30)→`OverrideTextColorArgb2`로, `OutputRenderer`가 [region 2] 본문에 적용(없으면 본문 색 추종)으로 라이브 반영 → 정직한 종단(이중 언어 곡에서만 보임, 단일 언어 무영향 — doc/메뉴 명시). 우클릭 **"이중 언어(보조 영역)" 신규 상위 메뉴**(Region2 facet 묶음 — 메뉴 비대화 방지, 앞으로 정렬·크기·글꼴 여기 추가) → "글자색" 프리셋5+"본문과 동일(해제)". `SetSelectedItemTextColor2(hex)`(Region1 색 미러, 코드30)·`SelectedItemTextColor2Hex`·`SetSelectedItemTextColor2Command`. **정직성 결정**: 항목 미디어 배정(코드51/MediaPath)은 SongFormatData 파싱·인코딩만 있고 출력 투영에 소비자 없음(LiveSessionService·OutputRenderer 미사용) 확인 → 안 그려지는 경로를 쓰면 가짜 기능이라 **미노출**(리뷰어 grep 재확인). code-reviewer **APPROVE(0 critical, 0 major)** — 코드30 왕복(코드29/27/26 무혼선)·빈→null→본문추종·라이브 OverrideTextColorArgb2·**ARGB 상수 자가 정정**(#FFFFFF00=0xFFFFFF00=-256, 빨강 -65536 아님 — 실패 테스트로 잡아 수정=정직)·단일언어 무영향 정직 공시(레거시 동일, 게이트 추가는 오히려 회귀)·4단계 중첩 메뉴 바인딩(AncestorType=ContextMenu 깊이 무관)·코드51 미소비 재확인 검증. 리뷰어 minor 반영(코드27 보존 테스트 추가·시드색 주석 정정). 신규 테스트 5(코드30 기록+개별켬+hex반영·본문색+Region2배경(27) 보존·해제→본문추종·라이브 오버라이드·비곡 무시+명령 비활성). 1738 green(Release). Region2 정렬·크기·글꼴·강조는 후속.

- (2026-06-02) ✅ 증분 115(이중 언어 보조 영역 Region2 항목별 정렬) — **선택한 곡만 보조 영역 가로 정렬**(레거시 Ind_ Region2 정렬, FormatData 코드32 — Region2 두 번째 facet, 증분108 Region1 정렬+증분114 Region2 색 미러). 종단 확인: `LiveSessionService`가 Alignment2(코드32)→`OverrideTextAlignment2`(MapAlignment)로, `OutputRenderer`가 Region2 본문에 적용(없으면 본문 정렬 추종) → 정직한 종단(이중 언어 곡만 보임). 기존 "이중 언어(보조 영역)" 메뉴에 "정렬"(왼/가운데/오른+본문동일) 추가(글자색과 형제). `SetSelectedItemAlignment2`(1~3·0/오류→본문추종)·`SelectedItemAlignment2`·`SetSelectedItemAlignment2Command`. code-reviewer **APPROVE(0 critical, 0 major)** — 코드32 왕복(코드31/30 무혼선)·1..3 검증·0→본문추종·라이브 OverrideTextAlignment2·두 형제(Region1 정렬·Region2 색) 충실 미러·4단계 중첩 메뉴 바인딩(AncestorType=ContextMenu 깊이 무관)·이중파싱 방어·단일언어 무영향 정직 검증. 리뷰어 minor 반영(스테일 "앞으로 추가" 주석 정정 — 정렬 완료 반영). 신규 테스트 7(정렬 1/2/3 기록+개별켬·본문정렬(31) 보존·0해제→본문추종·라이브 오버라이드·비곡 무시+명령 비활성). 1745 green(Release). Region2 크기·글꼴·강조는 후속.

- (2026-06-02) ✅ 증분 116(이중 언어 보조 영역 Region2 항목별 글자 크기+글꼴) — **선택한 곡만 보조 영역 글자 크기(코드48)·글꼴(코드44)**(레거시 Ind_ Region2 크기/글꼴, 증분109/110 Region1 미러 — 두 facet 묶음). 종단 확인: `LiveSessionService`가 FontSize2(48)→`OverrideFontSizePx2`(pt→px), FontName2(44)→`OverrideFontName2`로, `OutputRenderer`가 Region2 본문에 적용(없으면 본문 추종) → 정직한 종단(이중 언어 곡만 보임). 기존 "이중 언어(보조 영역)" 메뉴에 "글자 크기"(작게/보통/크게+본문동일)·"글꼴"(한·영 4종+본문동일) 추가. `SetSelectedItemFontSize2`(6~100)·`SetSelectedItemFontName2`(Trim·'>'·'=' 제거)·미리보기 속성·명령. code-reviewer **APPROVE(0 critical, 0 major)** — 코드48/44 왕복(47/43 본문·30/32 Region2색정렬·서로 무혼선, 4코드 공존 테스트로 입증)·6~100 검증·pt→px(72→96)·글꼴 '>'·'=' 제거(폰트명은 '=' 불가라 정당)·본문추종 해제·두 facet 묶음 합리·4중첩 메뉴 바인딩 검증. 리뷰어 minor 반영(turnedOn 자동켬 상태줄 테스트; **메뉴 캐스케이드 깊어지면 전용 다이얼로그 승격**은 백로그 기록). 신규 테스트 11(크기 36/72·무효 0/5/abc 해제·라이브 px·글꼴 기록+Trim·글꼴 해제·4코드 공존·자동켬 상태줄·비곡 명령 비활성). 1756 green(Release). Region2 강조(Bold2/Italic2/Underline2, 코드41 상위비트)만 후속.

- (2026-06-02) ✅ 증분 117(이중 언어 보조 영역 Region2 항목별 강조: 굵게/기울임/밑줄) — **선택한 곡만 보조 영역 강조**(레거시 Ind_ Region2 굵게/기울임/밑줄, FormatData 코드41 **상위비트** bit3/4/5 — Region2 마지막 facet). 종단 확인: `LiveSessionService`가 Bold2/Italic2/Underline2→`OverrideBold2/Italic2/Underline2`로 라이브 반영 → 정직한 종단(이중 언어 곡만, 후렴만 게이트는 Region1·Region2 동일 적용=의도, 주석 명시). **증분113 제네릭 `ToggleSelectedItemEmphasis` 헬퍼 그대로 재사용**(필드 접근자/뮤테이터만 Region2로) — Region1 가정 누수 없음. 기존 "이중 언어(보조 영역)" 메뉴에 "강조"(IsChecked OneWay+토글). code-reviewer **APPROVE(0 critical, 0 major)** — **코드41 단일 비트필드 6비트 공존**(본문 굵게1+보조 굵게8=41=9, 비대칭 끄기 41=25→17 입증)·record가 진실원본이라 매 쓰기 재합산으로 구조적 격리·제네릭 헬퍼 무누수 재사용·후렴 게이트 Region2 동일+정직 문서화·IsChecked OneWay+명령·wantsIndividual=newValue 검증. 리뷰어 minor 반영(비대칭 끄기 잔여비트 보존 테스트 41=25→17 추가). 신규 테스트 6(보조 굵게 on/off+41제거·본문+보조 굵게 공존 41=9·비대칭 끄기 41=17·기울임+기존색 보존·라이브 OverrideUnderline2·비곡 무시+3명령 비활성). 1762 green(Release). **항목별 인라인 포맷 Region1(8종)+Region2(5종) 전부 완료.** (백로그: `CanEditSelectedItemColor` → `...Format` 리네임 — 이제 색 외 다수 facet 게이트.)

- (2026-06-02) ✅ 증분 118(예배 순서 세션 콤보) — **저장된 예배 순서 빠른 전환 콤보**(레거시 FrmMain 세션 콤보, gap §3.2 line105 🟡→🟢). WorshipListPanel 헤더에 콤보+"불러오기" 버튼. **명시적 적재 설계**: 콤보 선택만으로는 적재 안 하고(자동 적재→실수 클릭으로 작업물 날림·선택 피드백 루프 위험) 버튼을 눌러야 LoadWorshipListAsync 적재. `SavedWorshipListNames`(콤보 ItemsSource)·`SelectedSavedWorshipList`·`RefreshSavedWorshipListNames`(콤보 열 때 DropDownOpened·저장·삭제·이름변경 시 갱신)·`LoadSelectedWorshipListCommand`. code-reviewer **APPROVE(0 critical, 0 major)** — 명시적 적재 설계 정당(LoadQueue=Queue.Clear 무경고 대체라 자동적재 위험 확인)·View 제스처/VM 로직 분리·async 위생(AsyncRelayCommand·ConfigureAwait(true))·DRY(LoadWorshipListAsync 재사용) 검증. **초판 MAJOR: 콤보 Clear()+재추가 시 WPF Selector 가 TwoWay 선택을 null 로 풀어 유효 선택을 새로고침마다 날림**(단위 테스트엔 안 나타나는 순수 Selector 동작) → `RefreshAppearanceTemplateNames` 의 캡처/복원 패턴(Clear 전 선택 캡처→재추가 후 있으면 복원, 없으면 해제)으로 해결, 재검증 APPROVE. 리뷰어 minor 반영(삭제·이름변경도 콤보 갱신 대칭·선택 유지 테스트). 신규 테스트 7(콤보 채움+저장 반영·외부 추가 반영·CanExecute 선택 추종·고른 목록 적재·없어진 이름 선택 해제·선택 유지·삭제 콤보 반영). 1768 green(Release). (후속 백로그: `RefreshAppearanceTemplateNames` 도 사라진 선택 명시 해제 분기 이식 — 동일 잠재 stale 선택.)

- (2026-06-02) ✅ 증분 119(Gap=Black 진짜 검은 화면 수정) — **대기(Ready) 화면에서 Gap 모드 Black 이 운영 기본 배경에 "BLACK" 글자만 나오던 것을 진짜 검은 빈 화면으로 수정**(레거시 Gap=Black "검은 화면", gap §3.4 line170 일부). Gap 4종(None/Black/Default/User) 중 Black 만 배경이 검정으로 안 칠해지던 종단 누락 확인 → OutputRenderer.CreateScene 에서 `kind==Ready && GapItemOption==Black` 이면 bgColorArgb=bgColor2Argb=검정·그라데이션 해제, ShouldShowPanelOverlay 에서 같은 조건에 오버레이(타이틀/상태) 숨김 → Cleared 와 같은 깨끗한 검은 화면. GetReadyDisplayText 의 죽은 "BLACK" 타이틀도 ""로 정리. code-reviewer **APPROVE(0 critical, 0 major)** — 솔리드 검정+그라데이션 해제+오버레이 숨김이 진짜 검은 빈 화면 생성·color1==color2 단락으로 견고·Ready 한정 게이트 정확(Standby=출력창 없음이라 무의미·Live/Cleared 불변)·User 로고 경로(User만)·페이드 무충돌·기존 모드 무회귀 검증. 리뷰어 minor 반영(죽은 BLACK 타이틀 비움+DisplayTitle 빈 단언·Live+Black 검정 강제 안 함 음성 테스트). 신규 테스트 3(Ready+Black 검정+오버레이숨김+타이틀빔·Ready+Default 검정강제안함+오버레이유지·Live+Black 검정강제안함). 1771 green(Release). Gap 모드 빠른 토글/로고 픽커 메인 UI 노출은 후속.

- (2026-06-02) ✅ 증분 120(대기 화면(Gap) 조작 UI를 출력 메뉴로 노출) — **Gap Item 모드/페이드/로고를 설정 창에서만 → 출력 메뉴 "대기 화면(Gap)"에서 즉시 조작**(레거시 Gap Item 조작 UI, gap §3.4 line170 🟡→🟢). 기존 AutoRotateMode 패턴 미러: `ActiveGapItemOption/UseFade/LogoFile`(ObservableProperty)·`GapItemModeOptions`(없음/검정/기본/로고 콤보)·`GapItemModeInput`(설정 저장+Active)·`ToggleGapItemUseFadeCommand`·`SetGapItemLogoFile`(경로 다듬기·**고르면 모드 자동 User 전환**해 바로 보이게)·`ClearGapItemLogoFileCommand`. RefreshActiveAppearance 가 3종 동기화(설정 창↔메뉴 양방향). 출력 메뉴에 모드 콤보(StaysOpenOnClick)+페이드 토글+로고 픽커(View OpenFileDialog→SetGapItemLogoFile)+지우기. Gap 설정은 OutputWindowViewModel 라이브 갱신 화이트리스트라 즉시 반영. code-reviewer **APPROVE(0 critical, 0 major)** — Active/Input/RefreshActiveAppearance 동기 루프 닫힘(SettingsChanged 마다 무조건 Refresh)·로고 자동 User 전환을 GapItemModeInput(영속) 경유 정확·재진입 무한루프 없음(값-동등 가드+Current 선갱신+변경키 0 무이벤트)·_isApplyingOperationalSettings 무간섭·View 픽커/VM 로직 분리·콤보-인-메뉴 패턴 검증. 리뷰어 minor 반영(설정창→메뉴 재동기 회귀 테스트·페이드/로고 상태줄 단언). 신규 테스트 7(모드 입력 영속+동기+상태·페이드 토글·로고 영속+자동User+상태·로고 지움 모드 유지·생성시 동기·설정직접변경 메뉴추종·페이드 상태줄). 1777 green(Release). Gap 조작 UI 완료 — 남은 §3.4 Gap 항목 없음.

- (2026-06-02) ✅ 증분 121(탐색기 외부 파일 드래그→예배 순서) — **탐색기에서 PPT/미디어 파일을 끌어다 예배 순서에 추가**(레거시 외부 소스 드래그, gap §3.5 line168 🟡→🟢). 증분99(라이브러리 곡 드래그) 위에 외부 FileDrop 분기 추가. 순수 `ExternalFileClassifier.Classify`(확장자→PowerPoint/Media/Unsupported, 대소문자 무시·빈/null 안전, 파일추가 메뉴 필터와 같은 확장자 집합) + VM `AddExternalFiles(paths)`(분류→AddPowerPoint/AddMedia 재사용, 모르는 형식 건너뜀+개수 안내, 순서 보존). WorshipListPanel QueueList_DragOver/Drop 에 FileDrop 분기(내부 타입 검사 뒤=오분류 방지). **정직한 스코핑**: Word(.docx)는 OfficeLib(View) 필요라 드래그 미지원→건너뜀(메뉴로), 이미지는 배경 경로라 큐 아님. code-reviewer **APPROVE(0 critical, 0 major)** — 분류 정확(확장자/대소문자/blank)·AddExternalFiles 순서/건너뜀/상태/반환 정확·**FileDrop 마지막 검사로 내부 드래그 오분류 불가**(typed CLR 포맷 vs CF_HDROP 분리)·View 제스처/VM 로직 분리·경로-only 추가(File.Exists 불요, 메뉴와 일관)·정직 스코핑 검증. 리뷰어 minor 반영(선택 뒤 묶음 삽입 의도 주석 + 비-빈 큐 삽입 위치 테스트; 드롭 위치 정밀 삽입은 백로그). 신규 테스트 18(분류 12+blank3·AddExternalFiles 혼합/전부미지원/전부지원/선택뒤연속삽입). 1795 green(Release). 이미지→배경 드래그·드롭 위치 정밀 삽입은 후속.

- (2026-06-02) ✅ 증분 122(외부 파일 드롭 위치 정밀 삽입 — 곡/성경 드래그와 통일) — **증분121 외부 파일 드래그의 "선택 뒤 추가"를 드롭한 위치(타깃 항목) 앞에 묶음 삽입으로 정렬**(곡 99·성경 43 드래그와 동일, gap §3.5 line168 backlog). `AddExternalFilesRelativeTo(paths, targetItem)`: 타깃 IndexOfReference→그 앞에 insertIndex++ 로 순서 유지 연속 삽입(타깃 없으면 끝)·묶음 첫 항목 선택. `AddExternalFiles(paths)`는 NextAfterSelectedOrNull 로 위임해 기존 "선택 뒤" 무회귀. 항목 생성 `CreateExternalFileItem` 추출(DRY). 드롭 핸들러가 AddExternalFilesRelativeTo(files, targetItem) 호출 → 4종 드래그(재정렬·성경·곡·파일)가 모두 같은 ItemFromPoint 타깃·insert-before-target·참조 안전으로 통일. code-reviewer **APPROVE(0 critical, 0 major)** — insert-before-target 인덱스(insertIndex++ 묶음 연속성)·위임 무회귀(기존 4테스트 그대로 통과)·CreateExternalFileItem 동치·값-중복 IndexOfReference 안전·4드래그 통일·메뉴 경로 무영향 검증. 리뷰어 minor 반영(위임 중복 삼항 제거·묶음 첫항목 선택 단언·doc). 신규 테스트 3(타깃 앞 순서삽입+첫선택·null→끝·값중복 정확 인스턴스). 1798 green(Release). 이미지→배경 외부 드래그만 후속.

- (2026-06-02) ✅ 증분 123(이미지→출력 배경 외부 드래그) — **탐색기에서 이미지 파일을 중앙 미리보기 영역에 끌어다 놓으면 출력 배경 이미지로 설정**(레거시 이미지→배경 드래그, gap §3.5). 기존 검증된 `SetOutputBackgroundImage` 재사용(메뉴 픽커의 드래그 대안). `ExternalFileClassifier` 에 `Image` 종류 추가(.jpg/.jpeg/.png/.bmp/.gif, 배경 대화상자 필터와 동일). **큐 무회귀**: 큐 드롭 스위치의 `_ => null` 이 Image 를 건너뛰어(이미지는 큐 항목 아님) 미리보기 영역 드롭만 배경으로 처리 — 두 드롭 타깃 분리. 미리보기 Grid 에 AllowDrop+DragOver(이미지일 때만 Copy)+Drop(첫 이미지→배경), View=제스처/파일추출·VM=SetOutputBackgroundImage. code-reviewer **APPROVE(0 critical, 0 major)** — Image 분류·드롭 핸들러 null 안전(FileDrop 없음/비이미지)·**enum 소비자 전수 grep으로 무회귀 입증**(스위치 2곳: 큐 _=>null 스킵·==비교, throw 없음)·드롭 버블링(자식 핸들러 없음)·두 타깃 무간섭·MVVM 분리·다중 이미지 첫장 결정 검증. 리뷰어 minor 반영(미리보기 Grid Background="Transparent"로 빈 여백까지 드롭). 신규 테스트 6(이미지 분류 5: png/JPG대소문자/jpeg/bmp/gif·큐 이미지 스킵 1). 1803 green(Release). PPT/미디어 폴더 컬렉션 브라우징 외부 드래그만 후속.

- (2026-06-02) ✅ 증분 124(공지(InfoScreen) 배경색) — **공지 화면에 배경색 추가**(레거시 FrmInfoScreen 배경, gap §3.5 — 76~78 텍스트/크기/정렬/글자색에 이어). 공지가 이미 곡 FormatData 파이프라인(BuildNoticeFormatData → 코드47/31/29)을 쓰므로 **코드26(배경) 한 줄 추가로 종단 완성** — GoLive 가 Kind 무관하게 FormatData 파싱 → OverrideBackgroundColorArgb → OutputRenderer 가 라이브에서 렌더(곡 개별서식 게이트는 런타임 렌더에 없음). NoticeOptions/InfoScreenDto 에 BackgroundColorArgb 끝에 추가(positional default 0 — 옛 JSON 무회귀). NoticeScreenViewModel BackgroundColorArgb 속성+프리셋(검정/진회색/네이비/짙은보라)+Send/Save/Open 스레딩. 공지 창에 "배경색" 콤보. code-reviewer **APPROVE(0 critical, 0 major)** — **종단 렌더 확인**(PublishNotice→GoLive Kind-무관 파싱→OverrideBackgroundColorArgb→isLive 게이트, UseIndividualFormatting 런타임 게이트 없음)·곡 코드26 경로 동일·append-at-end JSON 무회귀(옛 파일 0)·코드26 조건부(≠0)·VM 스레딩·주입 불가(텍스트 별도) 검증. 리뷰어 minor 반영(PublishNotice→OverrideBackgroundColorArgb 종단 테스트 추가). 신규 테스트 7(Send 배경 전달·Save/Open 왕복·Build 코드26 포함/생략·Store 왕복·옛파일 0·종단 PublishNotice→Override). 1810 green(Release). 풀 InfoScreen 편집기(7337줄)·.esi 레거시 포맷만 후속.

- (2026-06-02) ✅ 증분 125(공지(InfoScreen) 강조 — 굵게/기울임/밑줄) — **공지 본문에 강조 추가**(레거시 FrmInfoScreen 강조, gap §3.5 — 124 배경색에 이어). 공지가 이미 곡 FormatData 파이프라인을 쓰므로 **코드41(강조 비트밭) 추가로 종단 완성** — 비트0=굵게(1)·비트1=기울임(2)·비트2=밑줄(4) → GoLive 가 Kind 무관하게 파싱 → OverrideBold1/Italic1/Underline1 → OutputRenderer 가 라이브에서 region1Bold/Italic/Underline 로 본문(영역1=공지) 렌더. **주의(정직)**: 출력의 "강조 후렴만" 설정이 켜지면 공지(후렴 아님)에서 강조 억제 — 기본은 꺼짐이라 공지도 굵게 보임(코드 주석·gap 문서·렌더 테스트로 못박음). NoticeOptions/InfoScreenDto 에 Bold/Italic/Underline 끝에 추가(positional default false — 옛 JSON 무회귀). VM 3 bool 속성+Send/Save/Open 스레딩. 공지 창에 굵게/기울임/밑줄 체크박스 3종(AutomationProperties.Name). code-reviewer **APPROVE(0 critical, 0 major)** — **종단 렌더 전 구간 추적 확인**(코드41 비트밭이 SongFormatData region1 비트(0/1/2)·0..127 가드와 정확히 일치, GoLive Kind-무관, region1Bold 가 본문과 같은 Live 분기에서 전달 → 진짜 화면 굵게, 세션 필드만 세팅 아님)·append JSON 무회귀 STJ 보증·후렴 게이트 캐비엇 정확·3-arg 위치 호출 무회귀·한글 주석 검증. 리뷰어 minor+suggestion 둘 다 반영(부정 테스트를 문자열 부분일치→디코더 파싱 기반으로 격상, 후렴 게이트 기본 동작을 못박는 렌더 종단 Theory 추가). 신규 테스트 9(Send 강조 전달·Save/Open 왕복·Store 왕복·옛파일 false·Build 코드41 비트 포함/생략(파싱)·종단 PublishNotice→OverrideBold1·렌더 후렴게이트 2케이스). 1819 green(Release). 풀 InfoScreen 편집기(7337줄)·.esi 레거시 포맷만 후속.

- (2026-06-02) ✅ 증분 126(공지(InfoScreen) 글꼴명 — 코드43) — **공지 본문 글꼴 선택 추가**(레거시 FrmInfoScreen 글꼴, gap §3.5 — 125 강조에 이어). 공지가 이미 곡 FormatData 파이프라인을 쓰므로 **코드43(글꼴명) 추가로 종단 완성** — GoLive 가 Kind 무관하게 파싱 → OverrideFontName(NormalizeFontName) → OutputRenderer 가 라이브에서 영역1(=공지 본문) 글꼴로 렌더(글꼴은 후렴 게이트 없음 — isLive 만 보면 됨). '>'·'=' 구분자 제거+다듬기는 곡 SetSelectedItemFontName 과 **공통 규약 `SongFormatData.SanitizeFontName`** 로 통합(중복 제거, 리뷰어 suggestion). 추천 글꼴 목록도 `SystemFontCatalog.DefaultFavorites` 한 곳으로 모아 가사·공지 콤보가 공유(중복 제거). NoticeOptions/InfoScreenDto 에 FontName 끝에 추가(positional default null — 옛 JSON 무회귀). VM FontName 속성+FontNameOptions(생성자 주입, 없으면 추천)+Send/Save/Open 스레딩. 공지 창은 편집 콤보(IsEditable, Text 단방향 바인딩 → SelectedItem/Text 충돌 회피, 빈=전역 글꼴), 행이 많아져 StackPanel→WrapPanel 로 줄바꿈. MainWindow 가 가사 글꼴 콤보 목록(LyricsFontFamilyOptions, 설치 글꼴 합친 것)을 그대로 넘김. code-reviewer **APPROVE(0 critical, 0 major)** — **종단 렌더 전 구간 추적 확인**(코드43→FontName1→GoLive Kind-무관→OverrideFontName→OutputRenderer region1 글꼴, 글꼴은 후렴 게이트 없음·isLive 만)·DRY 리팩터 정적 초기화 순서 안전(beforefieldinit, 순환 없음)·편집 콤보 바인딩 함정 회피(SelectedItem 미바인딩)·append JSON 무회귀·라이브 ObservableCollection 별칭 안전(읽기만)·8-arg 위치 스레딩 검증. 리뷰어 suggestion(중복 strip 헬퍼) 반영. 신규 테스트 13(Build 코드43/구분자제거/빈생략·종단 PublishNotice→OverrideFontName·Send 전달·Save/Open 왕복·옵션 기본/주입·Store 왕복·옛파일 null·SanitizeFontName 6케이스). 1837 green(Release). 풀 InfoScreen 편집기(7337줄)·.esi 레거시 포맷만 후속.

- (2026-06-02) ✅ 증분 127(공지 "순서에 추가"가 서식 보존 — 드롭 버그 수정) — **편집기 서식이 큐 항목으로 안 실리던 버그 수정**. 공지 편집기에서 글꼴·색·크기·강조·배경을 정하고 "순서에 추가"를 누르면 이전엔 텍스트만 들어가 서식이 사라졌다("송출"은 적용). 이제 "순서에 추가"도 같은 NoticeOptions 를 BuildNoticeFormatData 로 인코드해 항목 FormatData 에 실어, 나중에 송출될 때 "송출"과 같은 모양으로 보인다. 종단: FormatData → ResolveLiveProjection(UseIndividualFormatting 기본 true 라 보존) → GoLive Kind 무관 파싱 → Override*. **AddTextItem(text, NoticeOptions? options=null)** 선택 인자 추가(옵션 없으면 FormatData null — 기존 호출·Word 추출 무회귀). NoticeScreenViewModel 은 Send·AddToQueue 가 공유하는 **CurrentOptions()** 헬퍼(두 버튼이 다시 어긋나지 않게) + 콜백 타입 Func<string,bool>→Func<string,NoticeOptions,bool>. code-reviewer **APPROVE(0 critical, 0 major)** — **큐-송출 경로 종단 렌더 직접 추적 확인**(GoLiveCommand→GoLiveAsync→PublishSelectedItem→ResolveLiveProjection 3367 FormatData 보존→GoLive 124 Kind 무관 Parse→Override, 즉시-송출 PublishNotice 와 별개의 진짜 경로)·옵션 인자 순수 가산(AddWordTextItem 등 단일인자 호출 무회귀)·콜백 변경 생산 호출자 하나뿐(MainWindow)·CurrentOptions 추출 Send 동작 동일·개별서식 토글 일관 검증. 리뷰어 minor(8-arg 위치 인자 footgun) 반영(CurrentOptions 명명 인자). 신규 테스트 3(옵션→FormatData+개별서식·옵션없음 무회귀·**종단 추가→GoLive→OverrideBackground**) + 기존 2 갱신. 1840 green(Release). **부수: gap 문서 정직 정정** — Bible typed-ref(JumpToReference)·copy-to-InfoScreen 은 이미 구현됨인데 "없음"으로 잘못 표기돼 있던 것 바로잡음(이중영역만 후속).

- (2026-06-02) ✅ 증분 128(항목별 인라인 서식을 성경 항목까지 확대) — **레거시 Ind_* 가 항목 종류 무관이었던 것을 따라, 곡 전용이던 항목별 서식을 성경에도 적용**. 성경 항목도 본문(구절)을 회중 화면에 렌더하고(CreateBibleItem 이 Lyrics=ExpandSelectionBody) GoLive 가 Kind 무관 파싱이라, 곡과 똑같이 우클릭 항목별 글자색(29)·정렬(31)·크기(47)·글꼴(43)·배경색(26)·배경이미지(61)·강조(41)와 이중언어 Region2(30/32/44/48+강조)를 적용할 수 있다. **변경은 게이트 한 곳**: `CanEditSelectedItemColor` 를 `Kind: Song` → `Kind: Song or Bible`(본문 있을 때) 로 확대. 컨텍스트 메뉴가 이 게이트에 IsEnabled 바인딩이라 자동 활성. **안 건드린 것(정직)**: 절 순서 편집은 곡만(CanEditSelectedItemSequence 별도 게이트), 코드/악상(ExpandNotations)·후렴 라벨은 Kind 로 따로 분기 — 성경에 잘못 트리거 안 됨. **알려진 뉘앙스**: 성경은 절 라벨이 없어 CurrentPageIsChorus 늘 false → "강조 후렴만" 켜지면 성경 강조 억제(자연스러움 — 성경엔 후렴 없음, 색·정렬·글꼴은 무관). LiveSessionService 에 그 주석 추가. code-reviewer **APPROVE(0 critical, 0 major)** — **성경 종단 렌더 전 구간 확인**(ResolveLiveProjection→GoLive Kind 무관 Parse→OutputRenderer 가 isLive 만 보고 Region1/Region2 본문에 Override 적용, 곡과 동일)·Region2/배경이미지/배경색 모두 일관·절 순서 게이트 분리 확인·or 상수 패턴 정확·RepublishLiveSongForBodyChange 가 라이브 성경 절 안 튐 검증. 리뷰어 minor(중복 `<summary>` 스택·잔여 "곡" 표현) 전부 반영. 신규 테스트 4(성경 게이트 true/본문없음 false·성경 색→FormatData·**종단 성경 색→GoLive→OverrideTextColorArgb**). 1844 green(Release).

- (2026-06-02) ✅ 증분 129("이 항목 서식 모두 지우기" — 레거시 Clear All Formatting) — **항목별 FormatData 전부를 한 번에 지워 전역 기본으로 되돌리는 명령**(이전엔 facet 마다 따로 해제해야 했음). 우클릭 컨텍스트 메뉴 끝에 구분선+메뉴 추가. 구현은 검증된 공통 헬퍼 재사용: `ApplySelectedSongFormatChange(_ => new SongFormatData(), wantsIndividual:false)` — 빈 SongFormatData 는 Encode 가 빈 문자열 → 헬퍼가 FormatData=null 세팅 → 전역 복귀. 레코드 동등 무동작 가드가 양방향 정확(서식 있으면 진행·없으면 무동작). 게이트 `CanClearSelectedItemFormatting = CanEditSelectedItemColor && FormatData 있음`(지울 게 있을 때만 활성, 곡·성경 모두). 라이브 중이면 RepublishLiveSongForBodyChange 가 같은 절로 재송출해 즉시 전역 모양 복귀. UseIndividualFormatting 은 의도적으로 안 건드림(null FormatData 면 어차피 전역, facet 개별 해제와 일관). 메뉴는 Command.CanExecute 로 자동 비활성(WPF ICommandSource). code-reviewer **APPROVE(0 critical, 0 major)** — **무동작 동등 가드 양방향 정확 확인**(Parse vs new() 비대칭 없음·Encode 빈→null)·**라이브 복귀 종단 진짜**(record copy 가 Id 보존→_liveItemId 일치→GoLive 가 null FormatData 파싱→Override null→렌더 전역)·메뉴 자동 비활성·이중 통지(NotifyCommandStates+OnPropertyChanged) 검증. 리뷰어 minor(헬퍼가 이제 성경도 담당 주석) 반영. 신규 테스트 4(모두 지움→null·서식 있을 때만 활성·성경 동작·**종단 라이브 색→지움→Override null**). 1848 green(Release).

- (2026-06-02) ✅ 증분 130(이 항목 서식 복사/붙여넣기) — **한 항목의 항목별 서식을 담아 다른 항목들에 통일 적용**(129 "모두 지우기"와 짝). 우클릭 메뉴에 "이 항목 서식 복사"·"이 항목에 서식 붙여넣기" 추가. VM 에 서식 클립보드(`_copiedItemFormatData` 문자열 스냅샷) + 두 명령. 복사는 검증된 헬퍼 재사용 — 붙여넣기는 `ApplySelectedSongFormatChange(_ => Parse(담아둔것), wantsIndividual:true)` 로 대상 FormatData 를 **통째로 교체(합치지 않음)**, 개별서식 켜고 라이브면 즉시 재송출. 게이트: 복사=서식 있을 때(=지우기 조건), 붙여넣기=담아둔 게 있고 대상 편집 가능(곡·성경). 본문·제목은 안 건드리고 서식만. 메뉴는 Command.CanExecute 로 자동 활성/비활성. code-reviewer **APPROVE(0 critical, 0 major)** — (a)**통째 교체 확인**(mutate 가 current 무시·next 통째 Encode, 동등 가드 양방향 정확·동일 항목 붙여넣기 무동작), (b)**Parse→Encode 왕복 무손실**(앱이 읽는 모든 코드 26/27/29/30/31/32/41/43/44/47/48/51/61 대칭 — 붙여넣기가 새 손실 도입 안 함, 범위 밖 레거시 코드는 어차피 모든 편집서 정화됨), (c)**클립보드 값 안전**(string 불변·record 항목, 원본 수정/삭제와 무관) 검증 + 교차종류(곡→성경)·게이트 양방향·라이브 종단 확인. 리뷰어 testing 제안 반영(사전 서식 있는 대상에 붙여넣어 **합치지 않고 교체** 확인 테스트 추가). 신규 테스트 5(복사→붙여넣기 적용·통째 교체·게이트·성경 대상·**종단 라이브 붙여넣기→Override**). 1853 green(Release).

- (2026-06-02) ✅ 증분 131(복사한 서식을 전 항목에 적용) — **서식 클립보드(130)를 큐의 모든 곡·성경 항목에 한 번에 적용**(여러 곡 같은 색·글꼴로 통일). 기존 "전 항목에 전역 서식 적용"(71, 개별서식 끄기)의 짝 — 이건 담아 둔 한 서식으로 통일. 상단 메뉴에 추가. 구현은 ApplyGlobalFormatToAll 구조 그대로 미러(선택 인덱스 보존·라이브 재송출·NotifyCommandStates). 클립보드를 Parse→Encode 로 정규화(잘못된 코드 정리+비교 표준형), 각 IsPerItemFormattable(곡·성경+본문) 항목에 FormatData=정규화·UseIndividualFormatting=true, **이미 같은 서식이고 개별서식 켜진 것만 건너뜀**(서식 동일하나 개별서식 꺼진 건 켜 줌). PPT·미디어·공지는 건너뜀. **DRY**: 게이트 기준을 `IsPerItemFormattable` 한 곳으로 추출해 CanEditSelectedItemColor 와 공유. code-reviewer **APPROVE(0 critical, 0 major)** — (a)**CanEditSelectedItemColor 리팩터 동작 동일**(4케이스 진리표 동등), (b)**스킵 가드 4조합 정확**(match&개별 → 스킵, match&개별꺼짐 → 적용해 켬, 핵심 엣지 정확)·Encode 고정 코드순이라 Ordinal 비교 정규형 안전, (c)**큐 변경 게이트 반응성**(모든 Queue 변경 경로가 NotifyCommandStates 호출 — 메뉴 안 stale) 전부 확인 + 선택 보존·라이브 재송출 안전·정규화 필요성 검증. 리뷰어 nit(테스트 주석이 무동작 암시하나 실제 정규화) 반영(`.Be("47=80")`로 정규화 명시). 신규 테스트 3(전 곡·성경 적용+PPT 건너뜀·게이트 클립보드/대상 요구·대상없음 무동작). 1856 green(Release).

- (2026-06-02) ✅ 증분 132(미디어·PowerPoint 폴더 브라우저 파일명 검색) — **현대 기능 업그레이드**: 미디어/PPT 폴더 찾아보기 창에 **검색 상자** 추가(큰 폴더에서 파일명으로 빠르게 거르기, 대소문자 무시, 입력 즉시 갱신). 공용 순수 함수 `FileNameFilter.Matches`(부분일치·대소문자 무시·빈 검색어=전부)로 두 브라우저가 공유(중복 방지). 두 VM 에 `_allFiles`(원본) + `FilterText` + `ApplyFilter`(폴더 재탐색 없이 메모리에서 걸러 화면 목록 재구성, 상태줄 "{걸러진}/{전체}개"). XAML 은 체크박스 행에 검색 TextBox 추가(UpdateSourceTrigger=PropertyChanged, ToolTip·AutomationProperties). code-reviewer가 **MAJOR 발견·수정**: 선택한 파일이 검색에 걸러지면 SelectedFile 가 dangling 으로 남아 "숨은 항목 추가" 사고 가능 — ImageLibraryViewModel 이 이미 같은 가드(이전 리뷰 MAJOR 반영)를 갖고 있는데 새 VM 들이 빠뜨림. **두 VM ApplyFilter 끝에 `SelectedFile 가 목록에 없으면 null` 가드 추가 + 회귀 테스트 2**(걸러진 선택 해제→추가 비활성). 리뷰어 확인: OrdinalIgnoreCase 가 파일명에 정답(터키-i 회피·NTFS 정합)·필터 Load 횡단 지속 정상·_allFiles 중복은 소스생성기 충돌로 추상화 부적합(공용 로직은 이미 FileNameFilter)·XAML 무회귀. 신규 테스트 16(FileNameFilter 9·미디어 4·PPT 3, 회귀 2 포함). 1872 green(Release). **부수: gap 문서 정직 정정** — 미디어·PPT 폴더 브라우징은 "없음"이 아니라 별도 창으로 이미 존재(검색까지 추가).

- (2026-06-02) ✅ 증분 133(이미지 갤러리 파일명 검색 — 브라우저 검색 패리티 완성) — 이미지 갤러리 창에 **파일명 검색 상자** 추가(132의 미디어·PPT에 이어 세 브라우저 검색 통일). 132에서 만든 공용 `FileNameFilter` 재사용. 기존 카테고리(Scenery/Tiles 하위폴더) 필터와 **AND 합성**: ApplyCategoryFilter 가 `categoryMatch && FileNameFilter.Matches(name, FilterText)` 로 거른다(빈 검색어=항상 통과 → 카테고리만, 무회귀). 이미 있던 stale-선택 가드가 이름 제외에도 자동 적용(걸러진 선택 해제→배경 적용 비활성). code-reviewer **APPROVE(0 critical, 0 major)** — (a)**AND 합성 4조합 정확**·빈 검색어→true 불변식이 카테고리 무회귀 보장, (b)**비동기 썸네일 루프와 중간 필터링 안전**(썸네일은 인스턴스에 저장·_allItems 같은 인스턴스라 필터 왕복해도 유지, await 가 UI 스레드 재개라 동시변경 없음), (c)검색 개수 상태줄은 의도적 미표시(이미지 Load 가 async 라 StatusText 를 await 횡단 소유 → ApplyCategoryFilter 가 쓰면 경쟁; 동기 Load 인 미디어·PPT 와 구조가 달라 cargo-cult 회피가 정답) 확인. 리뷰어 testing 제안 반영(검색어 재로드 지속 핀 테스트 + 필터 왕복 같은-인스턴스 핀 테스트). FluentAssertions params 함정(`Equal("x","because")`→2개로 오인)을 실패 테스트로 잡아 `Equal(new[]{"x"},"because")` 로 교정. 신규 테스트 6(이름 검색·카테고리 AND·걸러진 선택 해제·재로드 지속·같은 인스턴스 + 132 패리티). 1877 green(Release). 후속(backlog): 검색 개수 상태줄(공용 RefreshStatus 추출로 async clobber 경쟁 없이).

- (2026-06-02) ✅ 증분 134(이미지 갤러리 검색 개수 상태줄 — 133 backlog 완료) — 133 리뷰가 backlog 로 남긴 **검색·필터 결과 개수 상태줄 표시**를 리뷰어 설계대로 구현(미디어·PPT 와 패리티). 이미지 Load 는 async 라 StatusText 를 await 횡단 소유 → 단순히 ApplyFilter 가 개수를 쓰면 비동기 루프 끝의 전체 개수가 덮어쓰는 경쟁이 있었음. **공용 `RefreshStatus()`**(비었으면 안내·다 보이면 전체·걸러졌으면 "{보임}/{전체}개(검색·필터)"·디코딩 중이면 "불러오는 중" 꼬리)를 ApplyCategoryFilter 끝과 LoadAsync 끝 양쪽에서 호출 + `_thumbnailsLoading` 플래그로 조율. 플래그는 **첫 await 이전(동기 구간)에 true** 로 세워, 새 로드가 옛 로드 취소 후 동기로 다시 true 를 세우므로 잘못된 중간 상태가 안 보인다. code-reviewer **APPROVE(0 critical, 0 major)** — (a)**플래그가 loads settle 후 stuck-true 안 됨**(완료 로드마다 false, 취소-후속없음만 true 인데 실사용 불가능), (b)**기존 상태 문자열 무회귀**(최종 "N개 이미지"·빈 "없습니다"·로딩 꼬리 — 테스트된 문자열 동일), (c)**취소 early-return 이 플래그 true 로 둠이 안전**(죽는 로드가 stale 개수 안 씀, 후속 로드가 소유) 전부 추적 확인 + 엣지(전부일치=꼬리없음·0일치="0/N"·빈폴더 단락) 검증. 리뷰어 learning 제안 반영(플래그를 첫 await 전에 두는 이유 주석 명시). 신규 테스트 1(걸러진 개수 "1/3개 이미지(검색·필터)"). 1878 green(Release). **세 폴더 브라우저(미디어·PPT·이미지) 검색+개수 패리티 완성.**

- (2026-06-02) ✅ 증분 135(F11 = 송출 후 다음 항목 — 레거시 btnToOutputMoveNext 단축키) — VM 명령(SendToOutputAndNextCommand, 증분55)은 이미 있고 테스트됐지만 **command-id·카탈로그·단축키가 없어 키보드·⌘K 로 못 닿던 것**을 배선. MainCommandIds.LiveGoAndNext 추가 + CommandCatalog 에 "송출 후 다음 항목"(isDangerous, **F11** 단축키) + 레지스트리 바인딩(형제 LiveGo 와 동일 방식 — 게이트 없이 ExecuteAsync, CanGoLive·안전확인은 명령 내부에서). 다른 라이브 F-키(F12·F9·F3·F5·F4·F1)는 이미 매핑돼 있었고 **F11 만 빠진 갭**. code-reviewer **APPROVE(0 critical, 0 major)** — (a)**F11 무충돌 확인**(EasislidesNext 전체에 F11 없음·레거시 WinForms 는 별 바이너리·충돌 시 Register 가 throw + DoNotCollide 테스트), (b)**게이트-무게이트 형제 비대칭**: 처음엔 CanExecute 게이트 넣었으나 리뷰어가 LiveGo 와 통일 권고 → **게이트 제거**(안전 확인 prompt 는 SendToOutputAndNextAsync 내부라 게이트가 prompt 를 건너뛰지 않음을 추적 확인), (c)isDangerous·fire-and-forget·포커스 가드(F-키는 비인쇄라 텍스트 입력 안 가로챔·F12/F9/F3 와 동일 경로)·팔레트 열림 중 억제 모두 일관 검증. 신규/갱신 테스트 3(F11 단축키 매핑·카탈로그 위험표시·레지스트리 바인딩 TryInvoke 잠금) + 기존 무충돌·known-command 테스트 자동 검증. 1879 green(Release). 레거시 Restart 는 F5 충돌로 팔레트 전용 유지(정직).

- (2026-06-02) ✅ 증분 136(처음으로·출력 새로고침 현대 단축키 + 절-라벨 점프 gap 정정) — 팔레트 전용이던 라이브 화면 제어 명령 둘에 **현대 단축키** 부여: 처음으로(LiveRestart)=**Ctrl+R**(니모닉), 출력 새로고침(LiveRefresh)=**Ctrl+F5**(하드 새로고침 관용). 명령·레지스트리 바인딩(BindGated)·카탈로그는 이미 있었고 단축키만 추가. 레거시 F5=Restart 는 WPF 에서 "다음 항목"으로 재배정됐으므로 충돌 없는 Ctrl 조합 사용. code-reviewer **APPROVE(0 critical, 0 major)** — (a)**Ctrl+R·Ctrl+F5 무충돌**(전체 키맵 열거 — Ctrl B/H/L/D/K 만 사용 중, XAML 은 Ctrl+K 만·Register 가 충돌 시 throw), (b)**Ctrl+F5 vs F5 수식 키 분리 정확**(TryHandle 가 Key+Modifiers 둘 다 == 비교, IsGlobal:false 라 전역 훅 안 탐), (c)**포커스 가로채기 없음**(OnPreviewKeyDown 은 MainWindow 한정·곡/공지 편집기는 별 창·Ctrl+R/Ctrl+F5 는 WPF TextBox 표준 편집키 아님·BindGated 가 CanExecute 게이트·팔레트 열림 억제) 전부 확인 + isDangerous false 일관·DisplayText "Ctrl+R"/"Ctrl+F5" 정상. 신규 테스트 3(Ctrl+R/Ctrl+F5 매핑 theory·F5 None/Control 공존). 1882 green(Release). **부수: gap 정정** — 절-라벨 점프(1~9·c·t·b·w·e·p·q, VerseJumpKeyMap)는 "없음"이 아니라 키보드로 이미 구현(전용 버튼 행 UI 만 후속).

- (2026-06-02) ✅ 증분 137(예배 순서 항목 종류별 Fluent 아이콘 — 아이콘 업그레이드) — 예배 순서 목록이 제목 텍스트만 보이던 것(DisplayMemberPath="Title")을 **[종류 아이콘]+제목** ItemTemplate 로 바꿔 곡/성경/PPT/미디어/공지를 한눈에 구분(directive "아이콘·디자인 현대 업그레이드"). 순수 헬퍼 `WorshipItemIcon.SymbolForKind`(곡=Notebook·성경=Book·PPT=SlideLayout·미디어=Video·공지=Comment·그외=Document 폴백, LiveItemKinds 상수 switch 라 종류 리네임 시 컴파일 에러) + 얇은 `KindToSymbolConverter`(SongRowDisplayConverter 선례와 동일 분리). EsIcons 에 Notice(Comment24)·GenericItem(Document24) 추가. code-reviewer **APPROVE(0 critical, 0 major)** — (a)**선택·드래그·컨텍스트메뉴·더블클릭 무회귀**(DisplayMemberPath↔ItemTemplate 는 ListBoxItem 컨테이너 안쪽만 바꿈, 모든 바인딩은 ListBox 레벨), (b)**a11y 회귀 발견·수정**(SymbolIcon 은 WPF 에서 자동화 비가시 아님 → 글리프가 제목 앞에 읽힘. 리뷰어가 제안한 AccessibilityView=Raw 는 **WinUI 전용이라 WPF 빌드 실패(MC3072)** → 빌드가 잡음 → 행 StackPanel 에 AutomationProperties.Name="{Binding Title}" 로 WPF 정석 수정해 행이 제목으로 읽히게), (c)SymbolRegular→Symbol 바인딩 타입 정확(MediaPlaybackViewModel 선례) 확인. 패널 바인딩-표면 테스트가 DisplayMemberPath 제거를 잡아 갱신(테스트가 회귀 방어 작동). 신규 테스트 10(종류별 아이콘·폴백·Document 핀) + 패널 테스트 갱신(아이콘 바인딩·제목·행 a11y 이름). 1892 green(Release). (드래그/우클릭은 ListBoxItem 까지 올라가는 핸들러라 구조상 무영향 — 헤드리스라 수동 드래그 검증은 못 했음, 정직.)

- (2026-06-02) ✅ 증분 138(예배 순서 LIVE 배지 — 송출 중 항목 표시) — 예배 순서 목록에서 **지금 출력 중인 항목에 빨강 "LIVE" 알약 배지**를 보여 운영자가 한눈에 라이브 항목을 안다(선택 하이라이트는 선택일 뿐 — "송출 후 다음" 자동 이동 시 선택≠라이브). private `_liveItemId` 필드를 **알림 속성 `LiveItemId` + 단일 통로 `SetLiveItemId`**(no-op 가드+OnPropertyChanged)로 노출하고 7개 대입 사이트를 모두 그 통로로 라우팅(읽기 16곳은 불변). 순수 `WorshipLiveState.IsLive(itemId, liveItemId)` + `LiveItemBadgeConverter`(MultiBinding [항목 Id, VM.LiveItemId]→Visibility). 배지 색은 LiveBar 와 같은 `Brush.Live.Active` 토큰(테마 일관). code-reviewer **APPROVE(0 critical, 0 major)** — (a)**7-사이트 중앙화 무회귀**(누락 없음·_liveItemId 는 부수효과 없는 평범한 필드라 no-op 가드 안전·읽기 동일 값), (b)**MultiBinding 라이브 갱신 확인**(VM 이 ObservableObject·OnPropertyChanged 가 UI 스레드(ConfigureAwait(true))·LiveItemId 변경 시 모든 항목 MultiBinding 재평가 — 표준 WPF), (c)**중복 Id 엣지 안전**(DuplicateSelectedItem 가 새 guid Id 부여라 배지 둘 안 뜸)·공지 센티넬(NoticeLiveId)은 큐 항목과 안 맞아 공지 송출 중 배지 없음·큐 텍스트 항목은 실제 text:guid Id 라 배지 정상 검증. 리뷰어 minor(하드코딩 #D32F2F→Brush.Live.Active 토큰) 반영. 신규 테스트 7(IsLive 진리표 6 + LiveItemId 송출 반영·통지·중지 해제). 1899 green(Release).

- (2026-06-02) ✅ 증분 139(메뉴 단축키 힌트 — 135/136 발견가능성 완성) — 135/136 에서 단축키를 줬지만 메뉴에 힌트(InputGestureText)가 안 보이던 세 항목에 힌트 추가: 송출 후 다음 항목=**F11**, 출력 새로고침=**Ctrl+F5**, 현재 항목 처음으로=**Ctrl+R**. 기존 F12/F9/F3/Space/Ctrl+H 패턴과 동일(표시 전용, 실제 배선은 CommandCatalog). code-reviewer **APPROVE(0 critical, 0 major)** — **anti-drift 테스트가 진짜 가드 확인**(Shortcut.DisplayText 는 Key/Modifiers 에서 *계산*되지 카탈로그 Description 에서 복사 아님 → 카탈로그 키를 바꾸면 DisplayText 가 달라져 테스트 실패. 3중 교차검증: 카탈로그 Key/Modifiers→계산 DisplayText→XAML 문자열)·InputGestureText 는 표시 전용이라 키 핸들러 안 만듦·**단일 단축키 명령 중 힌트 빠진 것 이제 0**(남은 건 다중 단축키의 보조 제스처뿐, 메뉴는 하나만 표시 가능)·하드코딩 패턴 일관 검증. 신규 테스트 6(힌트 노출 3 + 카탈로그 일치 anti-drift 3). 1905 green(Release).

- (2026-06-02) ✅ 증분 140(⌘K 명령 팔레트 단축키 힌트 — VS Code 식) — 팔레트가 명령명+분류+위험 배지만 보이던 것에 **단축키 힌트**를 명령 옆에 흐리게 표시(139 메뉴 힌트와 짝). CommandDescriptor 에 `ShortcutHint`(기본 단축키 첫 번째의 DisplayText, 없으면 빈 문자열) 추가 + AccessibleName 에 단축키 있으면 ", 단축키 X" 끝에 붙여 스크린리더도 안내(없으면 무변—기존 계약 테스트 통과). 팔레트 항목 우측을 [단축키][위험 배지] StackPanel 로. code-reviewer **APPROVE(0 critical, 0 major)** — (a)**AccessibleName 계약 변경 무회귀**(전 트리에서 AccessibleName 소비처는 빈-단축키 테스트뿐·실명 명령 정확값 단언 없음·스캐너는 바인딩 존재만 봄)·키바인딩 음성 안내는 좋은 a11y(소음 아님), (b)**팔레트(첫=니모닉)↔메뉴(F-키) 차이 수용 가능**(둘 다 유효한 대체 단축키·venue 별 적절), (c)**XAML 재구성이 위험 배지·레이아웃 보존**(Auto 열이 [힌트][배지] 묶음 수용·빈 힌트=0폭 무이동·DangerBadge 테스트 통과)·이중 발화 회피(힌트 TextBlock 자동화 이름 없음)·전역 [0] 단축키 없음 확인. 리뷰어 optional 제안 반영(메뉴 모든 InputGestureText 가 실제 카탈로그 단축키인지 검증하는 가짜-힌트 금지 테스트 추가 — 기존 F12/F9 힌트까지 전부 검증). 신규 테스트 3(ShortcutHint·AccessibleName 단축키 부착·메뉴 힌트 전수 카탈로그 일치). 1908 green(Release).

- (2026-06-02) ✅ 증분 141(예배 순서 재정렬 — 팔레트 + Ctrl+Shift+Up/Down) — 위/아래/맨위/맨아래 이동이 툴바 버튼 전용이던 것을 **⌘K 팔레트 검색 + 키보드 재정렬**로 확장. 명령(MoveSelectedItemUp/Down/ToTop/ToBottom)·CanExecute 게이트는 그대로, command-id 4개 + 카탈로그 4개(category "예배 순서") + 레지스트리 BindGated 4개 + 위/아래 **Ctrl+Shift+Up/Down** 단축키만 추가. code-reviewer **APPROVE(0 critical, 0 major)** — **make-or-break 3개 전부 확인**: (a)**Ctrl+Shift+Up 이 Key.Up 으로 도착**(Key.System 승격은 Alt 만 — Ctrl/Shift 는 e.Key 그대로, 기존 Ctrl+L/H/B/R/F5 와 동일 경로로 디스패치 작동·verse-jump/media 가드는 수식 키 있으면 즉시 bail), (b)**평이 화살표 ListBox 내비 무회귀**({Up,None}/{Up,Shift}/{Up,Control} 미등록이라 통과·QueueList 는 Single 모드라 범위 확장 없음), (c)**게이트-off 시 전역 삼킴 일관**(BindGated no-op 이라도 TryHandle true→e.Handled, LiveClear 등과 동일). 터널링 순서(Window OnPreviewKeyDown 가 ListBox 보다 먼저)·무충돌·DisplayText "Ctrl+Shift+Up" 정상 확인. 리뷰어 minor(미래에 MainWindow 에 멀티라인 텍스트박스 들어오면 주의 — 현재 곡/공지 편집기는 별 창이라 무관) 비차단. 신규/갱신 테스트 2(카탈로그 4종+이동 단축키·바인딩 TryInvoke 4종). 1909 green(Release).

- (2026-06-02) ✅ 증분 142(상단 LiveBar 에 라이브 위치 표시 "3/12") — LiveBar 가 상태·제목·모니터·단축키는 보였지만 **현재 송출 항목 안 어디인지(절/슬라이드)** 안 보이던 갭 채움. 위치 데이터는 이미 세션 스냅샷 CurrentItemPositionLabel(ComputePositionLabel — 곡 절·PPT 슬라이드 "N/M", 단일이면 빈문자열)에 있음. LiveBarViewModel 에 PositionLabel 속성 + ApplyLiveSnapshot(세션 변경마다 호출)에서 스냅샷→LiveBar.PositionLabel 주입 → 절/슬라이드 이동마다 갱신. XAML 에 위치 알약(빈 문자열이면 DataTrigger 로 숨김) 6번째 열 추가. 자동화 피어 이름에 위치 합성(빈 부분 필터)+위치 변경 시 라이브영역 통지. code-reviewer **APPROVE(0 critical, 0 major)** — (a)**열 재번호 무충돌**(0인디케이터/1상태/2*제목/3위치/4모니터/5단축키, 모니터·단축키 정확히 +1), (b)**빈 문자열 접힘 정상**(Value="" 가 String.Empty 매칭·Auto 열이라 접히면 *제목이 공간 회수·무갭), (c)**자동화 이름 테스트 무파손**(유일 피어 테스트가 substring Contain·빈 위치 필터돼 2부분 유지)·곡+PPT 둘 다 같은 ApplyLiveSnapshot 싱크로 갱신·Stop=Off 면 위치 비고 Hidden/Black 은 with 로 보존(복귀 시 유지) 검증. 리뷰어 cosmetic(알약 패딩 8,3→8,4 단축키 알약과 통일) 반영. 신규 테스트 2(PositionLabel 기본/설정·LiveBar 라이브 위치 1/3→2/3 갱신). 1911 green(Release).

- (2026-06-02) ✅ 증분 143(상단 LiveBar 에 다음 예정 항목 "다음 ▸ X" 표시) — 142(위치)와 짝. 다음 항목 데이터는 이미 스냅샷 CurrentItemNextTitle(ComputeNextTitle — 큐의 다음 항목 제목·마지막이면 빈문자열)에 있었으나 **UI 어디에도 안 보이던 계산-후-미사용** 갭. LiveBarViewModel 에 NextItemTitle + ApplyLiveSnapshot 주입(같은 싱크). 중앙 제목 열을 2줄 StackPanel 로(현재 제목 16px + 다음 ▸ 12px 흐림, 빈 문자열이면 DataTrigger 로 줄 숨김). 운영자가 다음을 미리 준비. code-reviewer **APPROVE(0 critical, 0 major)** — (a)**세로 StackPanel 이라 현재 제목 생략(ellipsis) 유지**(* 열 폭을 자식에 전달·가로 StackPanel 의 무한폭 함정 회피, 위치 알약 침범 없음), (b)**빈 문자열 트리거가 부분 "다음 ▸ " 숨김**(DataTrigger 는 StringFormat 안 거친 raw NextItemTitle 을 ""와 비교·142 알약과 동일 검증된 관용구), (c)**다음 항목을 a11y 에서 제외 정당**(Assertive 라이브영역에 준비 정보 끼우면 소음·142 알약과 동일 패턴이라 객체탐색으로는 도달 가능·강제 발화만 억제), MinHeight 56 에 2줄 무클립·**절 이동해도 다음=큐 다음이라 안 바뀜**(ComputeNextTitle 은 Id 키, 절 인덱스 무관) 검증. ▸(U+25B8) UTF-8(E2 96 B8) 무손상 확인. 신규 테스트 3(NextItemTitle 기본/설정·LiveBar 다음 항목 표시·마지막이면 빈). 1913 green(Release).

- (2026-06-02) ✅ 증분 144(예배 순서 미저장 변경 표시 "● 수정됨") — 저장한 세션을 불러온 뒤 큐를 바꿔도 운영자가 알 길이 없던 갭. **저장/불러오기 이후 큐가 바뀌면 패널 제목 옆에 "● 수정됨"(Brush.Status.Warning) 표시**로 저장을 잊지 않게. WorshipListHasUnsavedChanges 플래그: Queue.CollectionChanged 한 곳에서 모든 변경(추가·삭제·이동·복제·서식편집 Replace)에 true, LoadQueue(전체교체=불러오기/시작) 끝·SaveWorshipListAsync 성공 시 false. 단일 초크포인트라 어떤 편집도 안 새어나감. code-reviewer **APPROVE(0 critical, 0 major)** — (a)**거짓 양성 없음**(GoLive/PublishSelectedItem 은 투영본만 보내고 큐 안 바꿈·선택 변경·절 이동도 Queue.CollectionChanged 안 탐 — 다 추적 확인), (b)**리셋 정확**(LoadQueue 마지막 Queue 영향 줄 뒤 false 라 Clear/Add 의 transient true 덮어씀·Save 는 성공 try 안에서만 리셋이라 실패 시 dirty 유지·LoadQueue 호출자는 LoadWorshipListAsync 뿐), (c)**시작 깨끗**(필드 기본 false·ctor 가 큐 시드 안 함·App/MainWindow 시작 LoadQueue 없음). 머지=APPEND 라 dirty 유지(정확)·ImportEsw 도 리셋 안 함(새 저장소에 미저장이라 dirty 가 옳음) 검증. 리뷰어 minor(주석 "빈 문자열"→"false") + suggestion(GoLive→dirty 안 됨 명시 테스트) 반영. 신규 테스트 3(편집/저장/불러오기 추적·선택은 dirty 아님·GoLive dirty 아님). 1916 green(Release).

- (2026-06-02) ✅ 증분 145(예배 순서 빠른 저장 Ctrl+S) — 144 미저장 표시(● 수정됨)의 짝. **현재 세션 이름(CurrentWorshipListName, 저장/불러오기 시 설정)으로 Ctrl+S 덮어 저장**. QuickSaveWorshipListAsync: 이름 있으면 SaveWorshipListAsync(이름)로 저장(성공 시 dirty 해제), 이름 없으면(한 번도 저장 안 함) 저장 안 하고 안내만("'순서' 관리 창에서 이름 정하세요"). 표준 저장 단축키. command-id+카탈로그(category "예배 순서", Ctrl+S)+레지스트리 fire-and-forget 바인딩+파일 메뉴 "예배 순서 저장"(Ctrl+S 힌트·Save24 아이콘). code-reviewer **APPROVE(0 critical, 0 major)** — (a)**Ctrl+S 무충돌**(카탈로그 유일·plain S 미디어 Stop 은 별 경로·XAML Ctrl+S 없음)·전역 가로채기 무해(MainWindow 텍스트박스는 Ctrl+S 기본 편집키 아님·곡/공지 편집기는 별 창·팔레트 열림 억제), (b)**이름 없음 가드**(IsNullOrWhiteSpace·CurrentWorshipListName 쓰는 곳 RecordRecentWorshipList 한 곳뿐이라 공백 불가·SaveWorshipListAsync 도 공백 거부 이중 방어), (c)**덮어쓰기=표준 Ctrl+S 의미**·원자적 temp-move 쓰기라 크래시 안전. 리뷰어 비차단 고려(머지 후 Ctrl+S 가 머지전 이름에 머지 내용 저장 — 정직한 "현재 세션" 모델 결과·dirty 표시가 단서) — **CurrentWorshipListName 은 세션 메모 키이기도 해 머지 시 비우면 메모 부작용이라 별도 분석 필요해 이번엔 미변경, 정직히 기록**. fire-and-forget/isDangerous false/한글 주석 일관 검증. 신규 테스트 4(이름 있으면 저장+dirty 해제·이름 없으면 안내+미저장 유지·카탈로그 Ctrl+S·바인딩 TryInvoke). 1919 green(Release).

- (2026-06-02) ✅ 증분 146(예배 순서 Delete 키로 항목 제거) — 목록 항목 삭제 표준 키 Delete 를 **컨트롤 레벨(QueueList)에서 맥락적으로** 처리해 검색창 등 다른 텍스트 입력의 Delete 는 안 가로챈다. 순수 `WorshipListKeyMap.IsRemoveSelectedItem`(Delete+수식없음만 true)가 판단, 제거는 검증된 RemoveSelectedItemCommand 에 위임. 툴바 Delete 버튼 툴팁에 "(Del)" 표기. code-reviewer **APPROVE(0 critical, 0 major)** — (a)**Delete 가 텍스트 입력서 안 도둑맞음 확인**(KeyDown 은 버블링이라 QueueList/그 항목에 포커스 있을 때만 발화·다른 컨트롤/창은 버블 경로 밖 — 구조적으로 보장, 전역 핸들러였으면 IsTextInputFocused 가드 필요했을 것), (b)**Window OnPreviewKeyDown 이 Delete 안 삼킴 확인**(verse-jump=null·media 아님·카탈로그에 Delete 단축키 없음→e.Handled false→터널링이 ListBox 버블 KeyDown 까지 도달), 무확인 제거=툴바 버튼과 동일·CanExecute 가드(선택 없으면 무동작·e.Handled false)·제거 후 이웃 선택 이동으로 연속 Delete 동작·dirty(144) 표시·순수+View 위임 분리 검증. 리뷰어 praise(컨트롤 레벨 버블 선택이 "텍스트 안 도둑맞음"을 if-가드 아닌 구조로 보장)·suggestion(버튼에 (Del) 표기) 반영. 신규 테스트 6(Delete/None→제거·수식키·Backspace·다른키 아님). 1925 green(Release).

- (2026-06-02) ✅ 증분 147(⌘K 팔레트 "결과 없음" 빈 상태) — 일치 명령 없는 검색 시 빈 목록만 보이던 것에 **"일치하는 명령이 없습니다." 안내** 추가(현대 팔레트 UX). CommandPaletteViewModel 에 `HasNoResults => Results.Count == 0`(검색어 비면 전체라 false, 매칭 0건일 때만 true) + RebuildResults 끝에서 통지. 팔레트 DockPanel 에 검색창과 결과 목록 사이 Dock=Top TextBlock(HasNoResults 면 표시, AutomationProperties.LiveSetting="Polite"). code-reviewer **APPROVE(0 critical, 0 major)** — (a)**의미 정확**(빈 검색어→전체→false·매칭 있으면 false·없을 때만 true·닫히면 오버레이가 IsOpen 게이트라 안 보임·Open 이 Query 리셋해 stale-flash 없음), (b)**통지 타이밍 정확**(RebuildResults 끝 Results 갱신 후 통지·통지가 새 값 싣는 테스트로 순서 회귀 방지), (c)**LiveSetting=Polite 유효 WPF**(.NET 4.5+·Assertive 보다 타이핑 안 끊는 적절한 선택) 확인 + DockPanel 무회귀(접히면 0크기)·결과 0건 시 SelectedCommand null→Enter/↑↓ 무NRE 검증. 리뷰어 optional(코드비하인드 RaiseAutomationEvent 로 즉시 발화 보장) 비차단 — 선언적 LiveSetting 이 정답이라 미적용. 신규 테스트 2(결과없음 의미·통지). 1927 green(Release).

- (2026-06-02) ✅ 증분 148(현재 송출 항목 선택) — 138 LIVE 배지 위에. **미리보기로 앞서 보거나 "송출 후 다음"으로 선택이 라이브를 앞질렀을 때, 라이브(LIVE 배지) 항목으로 선택을 되돌리는** 명령/버튼. SelectLiveItem: 큐에서 Id==_liveItemId 항목 찾아 SelectedItem 만 바꿈(라이브 출력·_liveItemId 안 건드림). CanSelectLiveItem=큐에 라이브 Id 있을 때만(공지 센티넬 "notice:live"는 큐에 없어 false). 반응성: SetLiveItemId 단일 통로(라이브 변경)+Queue.CollectionChanged(큐 변경) 양쪽서 통지. 툴바 Target24 버튼+팔레트(category "예배 순서")+BindGated. code-reviewer **APPROVE(0 critical, 0 major)** — (a)**반응성 완전**(GoLive→true·Stop/Clear→false·공지 센티넬→false·라이브 항목 제거→false 네 전이 다 통지, _liveItemId 쓰기 한 곳뿐이라 누수 없음·NotifyCommandStates 에 추가 불필요), (b)**선택만 바꿈 확인**(OnSelectedItemChanged 가 가사 페이지·미리보기만 갱신·재송출/_liveItemId 무변), (c)**공지 센티넬 충돌 없음**("notice:live" vs 큐 "text:guid" 네임스페이스 분리). FirstOrDefault+CanExecute 이중 가드·생성 순서 null 가드·Target24 유효 검증. 리뷰어 minor(테스트 이름이 remove 케이스 약속하나 미커버) 반영(GoLive→제거→비활성 단계 추가). 리뷰어 정보(LoadQueue 가 _liveItemId 리셋 안 함 — 이 증분 무영향이나 슬라이드 가드와 함께 후속 정리 후보) 정직 기록. 신규 테스트 3(라이브 항목 선택 복귀·비활성 전이·카탈로그). 1930 green(Release).

- (2026-06-02) ✅ 증분 149(예배 순서 항목 툴팁 "종류 — 제목") — 137 종류 아이콘이 무엇을 뜻하는지 글로도 설명 + 길어서 잘린(ellipsis) 제목 전체를 마우스 올리면 보여 준다. 순수 `WorshipItemIcon.KindLabel`(곡/성경/PowerPoint/미디어/공지, 폴백 "항목" — SymbolForKind 와 대칭) + 얇은 `KindToLabelConverter`(KindToSymbolConverter 선례). 항목 행 StackPanel.ToolTip = MultiBinding StringFormat "{}{0} — {1}" [KindToLabel(Kind), Title]. code-reviewer **APPROVE(0 critical, 0 major, "ship it")** — (a)**StringFormat 이스케이프 정확**("{}" 가 markup extension 회피→"{0} — {1}"→"곡 — 제목", em-dash U+2014 UTF-8 무손상·같은 파일 기존 ⚠ StringFormat 으로 검증), (b)**KindLabel ↔ SymbolForKind 완전 대칭**(같은 5종+같은 폴백·아이콘만/라벨만 있는 종류 없음·테스트도 1:1 미러), (c)**a11y 이중 발화 없음**(툴팁은 UIA HelpText 로 매핑돼 Name(제목)과 별개 필드·종류는 보태는 정보·137 Name 무변경 테스트 통과), property-element 배치가 .Single() 바인딩-표면 테스트 안 깨뜨림(StackPanel.ToolTip LocalName 다름·Binding 은 TextBlock 아님) 검증. PowerPoint 영문 유지가 상수와 일관. 신규 테스트 9(KindLabel 5종+폴백 4). 1939 green(Release).

- (2026-06-02) ✅ 증분 150(좌측 브라우저 탭 Fluent 아이콘 머리글) — 라이브러리/성경/검색 탭이 평문 Header 였던 것을 **아이콘+텍스트 머리글**로 업그레이드(directive "아이콘 현대화"): 라이브러리=Notebook24·성경=Book24·검색=Search24(EsIcons 도메인 상수). 각 TabItem 에 AutomationProperties.Name(한글 라벨) 추가해 스크린리더가 글리프 대신 탭 이름 읽게(137 패턴). code-reviewer **APPROVE(0 critical, 0 major, "No changes required")** — (a)**Tag 기반 자동 로드 무파손**(SelectionChanged 가 tab.Tag 로 분기·"Header 리터럴 의존 회피" 주석·Tag 불변이라 Library/Bible/Search 1회 로드 그대로), (b)**Header-텍스트 의존 없음 확인**(전 코드·테스트에 `.Header` 문자열 의존 0건), (c)**a11y Name 오버라이드 정확**(TabItem.Name 이 머리글 자식 파생 이름보다 우선→탭 이름만 발화·137 워십행 패턴과 동일·장식 SymbolIcon 은 부모 Name 에 흡수). EsIcons 상수 유효·StackPanel 머리글 XAML 유효·탭 스트립 클리핑 없음 검증. 중앙 탭(Preview/PPT/Media)은 자동 전환 미리보기라 평문 유지=수용 가능 스코프(후속 후보: EsIcons.PptPreview/PowerPointSlide/MediaFile). 리뷰어 praise(테스트가 아이콘·a11y·텍스트 보존 3중 검증). 신규 테스트 3(탭별 아이콘+접근성 이름+텍스트 보존). 1942 green(Release).

- (2026-06-02) ✅ 증분 151(중앙 미리보기 탭 Fluent 아이콘 머리글 — 150 후속, 탭 아이콘 통일 완성) — Preview/PowerPoint/Media 중앙 탭도 아이콘+텍스트 머리글로(좌측 탭과 일관). EsIcons.Preview=Eye24(눈/보기, 기존 EyeOff24=숨김의 짝) 신규 + PowerPointSlide(SlideLayout24)·MediaFile(Video24). 각 TabItem 에 AutomationProperties.Name(영문 라벨, 가시 텍스트와 일치) — Tag 없어 a11y 이름으로 식별. PowerPoint/Media 의 Visibility 바인딩 보존. code-reviewer **APPROVE(0 critical, 0 major, "Ship it")** — (a)**중앙 자동 전환 무파손**(TabControl.SelectedIndex={SelectedContentTabIndex}←UpdateContentTabForItem 가 항목 종류로 0/1/2 결정·Header 안 읽음·Tag 없어 좌측 탭 경로와 완전 분리), (b)**Visibility 보존**(IsPowerPointTabVisible/IsMediaTabVisible BoolToVis 그대로·속성 순서만 바뀜·접힌 탭은 머리글 미렌더), (c)**탭 수·순서 불변**(Preview=0/PowerPoint=1/Media=2 = 코드비하인드 매핑과 일치) 확인. Eye24 유효·a11y Name 오버라이드·150 과 구조 동일 검증. 리뷰어 suggestion(.Single() 이 a11y 이름 window-유일에 의존)·minor(특정 Symbol 미검증)는 비차단. 신규 테스트 3(중앙 탭별 아이콘+텍스트). 1945 green(Release). **좌측+중앙 6개 탭 아이콘 통일 완성.**

- (2026-06-03) ✅ 증분 152(재정렬/복제 도구막대 버튼 단축키 힌트) — 예배 순서 위로/아래로 이동·복제 버튼 툴팁(Ctrl+Shift+↑/↓, Ctrl+D)+a11y 이름(Ctrl+Shift+Up/Down, Ctrl+D)에 실제 단축키 노출(삭제 버튼 "(Del)" 증분146·메뉴·팔레트와 같은 발견성 패턴 완성). a11y 이름은 카탈로그 표시문자열을 그대로 담아 **드리프트 방지 테스트**(접근성 이름이 라이브 CommandCatalog.ShortcutHint 를 포함하는지)로 묶음 — 카탈로그 단축키가 바뀌면 깨진다. 툴팁은 사람이 읽기 쉬운 화살표 글리프 유지(테스트 비대상). code-reviewer **APPROVE(0 critical, 0 major)** — 힌트가 카탈로그와 정확 일치(Key.Up+Ctrl|Shift→"Ctrl+Shift+Up" 등)·.Single() 버튼 탐색 안전(각 Command 바인딩 유일)·기존 평문 a11y 이름 .Be() 검증 테스트 없어 무회귀 확인. 신규 테스트 3(버튼별 a11y↔카탈로그). 1948 green(Release).

- (2026-06-03) ✅ 증분 153(전 항목 서식 지우기 — 레거시 Clear All Formatting 전체판, §3.8 🔴 해소) — 예배 순서 우클릭 "전 항목 서식 지우기": 모든 곡·성경 항목의 항목별 FormatData 를 한 번에 비워 전역 기본으로 되돌림. PPT/미디어/공지·이미 서식 없는 항목은 건너뜀, 선택은 같은 자리로 유지, 라이브 항목은 세션 실제 절로 재송출(0절 안 튐). 단일 항목판 ClearSelectedItemFormatting(빈 SongFormatData→FormatData null)과 **동일 종단 상태**, ApplyCopiedFormatToAll 구조 미러(인덱스 루프 in-place 교체=Replace·열거자 무효화 없음). CanClearAllItemsFormatting=큐에 지울 서식 있는 곡·성경 유무 → CollectionChanged·SelectedItem 변경·NotifyCommandStates 3곳서 통지(지운 직후 비활성). code-reviewer **APPROVE(0 critical, 0 major)** — FormatData=null≡per-item clear 종단 동등(Encode 빈 문자열→null) 확인·UseIndividualFormatting 보존이 단일판과 일관·RepublishLiveSongForBodyChange 가 세션 CurrentLyricsPageIndex 사용해 라이브 안전·3곳 통지로 stale 없음·index-for 루프 안전 검증. minor(방어적 dead branch=의도)·suggestion(지운 뒤 비활성 잠금 테스트)은 반영. 신규 테스트 4(전 항목 지움+종류 건너뜀+활성 조건+선택 유지+라이브 종단). 1952 green(Release).

- (2026-06-03) ✅ 증분 154(검색 결과 빈 상태 안내 — 모던 빈 상태 UX) — 좌측 검색 탭 곡 검색에서 "검색을 실행했는데 결과 0건"일 때만 "검색 결과가 없습니다…" 안내를 결과 목록 셀에 겹쳐 표시(검색 전 첫 화면엔 안 뜸 — 빈 목록과 "검색 후 없음"을 구분). SearchUsageViewModel 에 `HasSearched`(검색 1회 실행 표시)+`HasNoSearchResults`(=HasSearched && SearchResults.Count==0) 추가, 두 변경원(OnHasSearchedChanged 부분메서드 + SearchResults.CollectionChanged) 모두서 통지. 안내 TextBlock 은 IsHitTestVisible=False 로 더블클릭 추가를 통과시키고, 마크업상 ListBox "다음"에 둬 테마가 목록 배경을 칠해도 항상 위에 보이게(z-order 안전 — 리뷰어 권고 반영). 팔레트 HasNoResults 패턴의 VM 미러(팔레트는 DockPanel 비겹침이라 z-order 무관·이건 진짜 오버레이라 순서가 중요). code-reviewer **APPROVE(0 critical, 0 major)** — (a)**z-order 검증**(WPF UI Fluent DefaultListBoxStyle 이 Background 미설정=투명·OverridesDefaultStyle 로 스톡 불투명 배경 미상속 확인→원래도 보였지만 "ListBox 다음" 배치로 테마 의존 제거), (b)**이중 통지 정확**(ReplaceWith=무조건 Clear()→Reset 발생·HasSearched=true 부분메서드, empty↔empty 재검색도 무stuck), (c)**오류 재검색 시 stale 무crash**(기존 VM 특성) 검증. minor(테마 의존 제거 권고·Grid.Row 고정 테스트·오류 stale)는 z-order 재배치+Grid.Row=3 양쪽 고정 테스트로 반영. 신규 테스트 4(VM 3: 검색 전/후 없음/후 있음 + XAML 1: 오버레이 바인딩·메시지·히트테스트·동일 셀). 1956 green(Release).

- (2026-06-03) ✅ 증분 155(제목 조회 빈 상태 안내 — 154 후속, 검색 빈 상태 일관 완성) — 좌측 검색 "제목" 서브탭(LookupTitlesAsync)도 곡 검색과 똑같이 "조회를 실행했는데 후보 0건"일 때만 "조회된 제목이 없습니다…" 안내를 후보 목록 셀(Row 2)에 겹쳐 표시(조회 전엔 안 뜸). SearchUsageViewModel 에 `HasLookedUp`+`HasNoLookupResults`(=HasLookedUp && LookupCandidates.Count==0) 추가 — 곡 검색 HasSearched/HasNoSearchResults 와 완전 대칭, 두 변경원(OnHasLookedUpChanged 부분메서드 + LookupCandidates.CollectionChanged) 통지. 안내 TextBlock 은 ListBox "다음"·IsHitTestVisible=False(154 z-order 안전 패턴 동일). SearchText 가 두 서브탭 공유지만 빈 상태는 각 행동 플래그(HasSearched/HasLookedUp)로만 게이트돼 교차 오발 없음. code-reviewer **APPROVE(0 critical, 0 major, 0 minor)** — copy-paste 오류 감사(속성/계산/부분메서드/CollectionChanged/플래그 set 5곳 모두 lookup 측 참조·HasSearched/SearchResults 오참조 0)·z-order ListBox 다음 배치·제목 Grid Row 2 공유·공유 SearchText stale 무발생(곡 검색 후 제목 탭 전환 시 HasLookedUp=false→안내 숨김) 검증. 리뷰어 praise(대칭 자기문서화 XML-doc·z-order 근거 로컬 주석). 신규 테스트 4(VM 3: 조회 전/후-없음/후-있음 + XAML 1: 오버레이 바인딩·메시지·히트테스트·동일 셀). 1960 green(Release). **곡·제목 두 검색 빈 상태 일관 완성.**

- (2026-06-03) ✅ 증분 156(검증 문제 클릭 → 항목 선택 + 모던 링크 버튼 스타일) — 예배 순서 검증 경고 목록이 "읽기 전용 텍스트"에서 "클릭 가능"으로: 깨진 PPT·미디어·DB 없는 곡 문제를 누르면 그 항목이 큐에서 선택돼(SelectWorshipProblemItemCommand) 운영자가 수동 스크롤 없이 바로 가 제거·교체한다. 검증 후 인스턴스가 교체돼도(서식 편집) **Id 매칭**으로 현재 인스턴스를 찾고, 이미 제거된 항목이면 "이미 제거됨" 안내(참조 stale 회피). 디자인 시스템에 **EsButton.Link**(보더·배경 없는 평평한 텍스트 버튼·호버 강조·왼쪽 정렬·MinHeight 0) 추가 — 목록 안 인라인 클릭 항목용 재사용 모던 컴포넌트(directive "디자인 현대화"). 각 문제는 Link 버튼으로 평소엔 경고처럼 보이되 누르면 강조되고 키보드 포커스도 받는다(a11y: AutomationProperties.Name=Message). code-reviewer **APPROVE(0 critical, 0 major)** — (a)**Id 매칭 정확**(LiveQueueItem positional record·with 가 Id 보존·테스트가 실제 인스턴스 교체 후 BeSameAs 로 참조 아닌 Id 매칭 증명), (b)**중복 Id 무위험**(복제는 dup:{Guid} 고유 Id), (c)**비게이트 커맨드 정당**(문제 목록은 HasWorshipListProblems 때만 표시·null/제거 항목 내부 처리·NotifyCanExecuteChanged 불필요), (d)**Link 템플릿 유효**(PART_Border 트리거 매칭·FocusVisual 참조 해결·투명 배경 히트테스트 가능·빈 영역 없음), (e)**a11y 이중발화 없음**(Button Name 오버라이드) 검증. minor(SelectLiveItem 침묵 비대칭=새 동작이 더 나음, 비차단). 신규 테스트 6(VM 4: 선택/Id매칭/제거됨안내/null + XAML 2: 클릭 버튼 바인딩·Link 스타일 존재). 1966 green(Release).
- (2026-06-03) ✅ 증분 157(라이브러리 곡 목록 빈 상태 안내 — 검색·제목 빈 상태(154/155)의 라이브러리판) — 좌측 "라이브러리" 곡 목록(LibrarySongList, Grid.Row 4)이 "**곡을 불러왔는데** 표시할 곡 0건"일 때만 안내를 목록 셀에 겹쳐 표시한다. 핵심 난점은 154/155보다 한 단계 더: 빈 목록이 "**아직 안 불러옴**(처음 화면·로딩 중)"인지 "**정말 비었음**"인지 구분해야 섣부른 안내를 막는다. LibraryViewModel 에 `HasLoadedSongs` 플래그 추가 — `LoadSongsForSelectedFolderCoreAsync` 양 분기 끝에서 true(불러오기 한 사이클 완료=빈 목록이 권위 있음), `ClearLibrary`(DB 경로 실패)에선 **Songs.Clear() 보다 먼저** false(경로 실패는 "빈 폴더"가 아니므로 오버레이 대신 상태바가 원인 안내·한 프레임도 깜빡 안 함). 빈 상태는 `IsBusy·HasLoadedSongs·Songs·SearchText` 4입력의 순수 함수 `LibraryEmptyStateMessage`(검색어 있으면 "'…'에 맞는 곡이 없습니다"·없으면 "이 폴더에 곡이 없습니다")+`HasLibraryEmptyState`로 모델링, **4입력이 각자 변경을 통지**(OnIsBusyChanged·OnHasLoadedSongsChanged·OnSearchTextChanged 부분메서드 + 생성자 Songs.CollectionChanged)하는 단순 규칙으로 stale·누락을 구조적 차단. 특히 로딩 중엔 IsBusy 가드로 숨고, RunBusyAsync finally의 IsBusy=false→OnIsBusyChanged 재평가가 모든 로드 경로(LoadAsync·폴더 전환·곡 이동)에서 "끝나는 시점" 확정. XAML 오버레이는 154 패턴 동일(ListBox "다음"·IsHitTestVisible=False·동일 Grid.Row 4·BoolToVis)이되 문구는 정적 텍스트가 아닌 LibraryEmptyStateMessage 동적 바인딩(빈 폴더/검색 0건 두 상황 분기). code-reviewer **APPROVE(0 critical, 0 major)** — (a)**통지 완전성**(4입력↔4통지 매핑 빠짐없음·로딩↔완료 타이밍 정확), (b)**경로 실패 비깜빡임**(HasLoadedSongs=false 를 Songs.Clear 앞에 둔 순서 의도대로), (c)**폴더 0개 분기 무오작동**(LoadAsync 인라인 분기가 HasLoadedSongs 미설정→오버레이 안 뜸·경로무효는 ClearLibrary 로 분리), (d)**z-order/히트테스트/Grid.Row 154 일관** 검증. nit 2(폴더 0개 시 문구 미세 어긋남=공개 API로 거의 도달 불가·테스트 불가 가지라 미적용 / ""vs string.Empty=저장소 일관이라 현행) 둘 다 현행 유지 승인. 신규 테스트 6(VM 5: 로드 전 숨김/빈 폴더 메시지/검색 0건 메시지/곡 있으면 숨김/경로실패 후 숨김 + XAML 1: 오버레이 바인딩·동적 메시지·히트테스트·동일 셀). 1972 green(Release).
- (2026-06-03) ✅ 증분 158(찬양집 색인 RTF 내보내기 — 레거시 "Listing of Selected Folder" 형식 파리티) — **조사 먼저**: 레거시 File▸"Listing of Selected Folder"(선택 폴더 곡 색인을 RTF 파일로)는 gap §3.8 에서 🔴 표기였으나 **이미 증분44 PraiseBook Index("찬양집 색인..." 창)로 포팅돼 있었음**(현재 선택 폴더 곡 → 머리글자 색인 → 파일 내보내기) — gap 문서가 stale. 유일한 실제 잔여 갭은 레거시가 내던 **RTF 출력**(WPF 는 HTML 만)이라, 그 RTF 파리티를 추가. `IPraiseBookIndexExporter.BuildRtf` 신설(기존 BuildHtml 미러·순수 함수) — RTF 특수문자 `\ { }` 이스케이프 + 비ASCII(한글)는 `\uN?`(부호 있는 16비트, '가'=U+AC00→-21504, \uc1 폴백) 유니코드 이스케이프, 번호 0/음수는 빈 칸. VM `BuildIndexRtf()`(BuildIndexHtml 미러). 창 내보내기 버튼을 HTML 전용 → "색인 내보내기..."로 일반화(`ExportIndex_Click`): SaveFileDialog 필터 HTML/RTF, **레거시처럼 고른 확장자로 형식 결정**(.rtf→RTF, 그 외→HTML). RTF 는 본문이 전부 \uN? ASCII 라 BOM 없는 UTF-8 로 저장(리더가 머리 BOM 에 안 걸리게·레거시 BOM 대비 개선). 기존 HTML 경로 무변경(회귀 0). code-reviewer **APPROVE(0 critical, 0 major)** — 실제 RTF 출력 덤프로 (a)**구조 유효**(헤더\rtf1\ansi\fonttbl\fcharset129\uc1·그룹 {} 균형·제어워드 공백 delimiter), (b)**한글 \uN? 스펙 적합**(부호 있는 16비트·음수 \u 워드 디코드·\uc1 1글자 폴백 — 레거시 UnicodeToAscii_RTF 는 부호없는 䐃2? 지만 둘 다 유효·동일 결과), (c)**이스케이프 완전**(\{} 외 RTF 필수 escape 없음·& 등 raw 정상·음수 번호 가드), (d)**BOM 회피 타당**, (e)**HTML 회귀 0**(버튼 리네임 XAML↔코드 일치·기본 .html) 검증. minor 2(음수번호 테스트·& raw 주석) 모두 반영. 신규 테스트 6(RTF: 헤더+ASCII·\{}이스케이프·한글\u-21504?·번호0 빈칸·음수번호 빈칸·빈제목 기본값 동등성). 1978 green(Release). **gap §3.8 Listing 🔴→✅ 정정.**
- (2026-06-03) ✅ 증분 159(모든 설정 초기화 — 레거시 "Clear Registry Settings and Exit" 포팅, gap §3.8 마지막 RED 해소) — 레거시 Tools "Clear EasiSlides Registry Settings and Exit"(`Gf.ClearRegistrySettings()` 성공 시 `SaveToRegistryOnClosing=false; Close()`)를 포팅. 설정이 꼬여 앱이 이상할 때 쓰는 복구 탈출구(설정 창을 안 거치고 한 번에 기본값으로). **조사: WPF 는 이미 `ISettingsService.RestoreDefaults()`(기본 스냅샷을 디스크에 동기 영속화)가 있고 설정 창에도 배선됨 — 잔여 갭은 "도구 메뉴 진입점 + 초기화 후 재시작"뿐**이라 그것만 추가. `MainViewModel.ResetAllSettingsToDefaults()`(=`_settings.RestoreDefaults().Succeeded`, 테스트 가능 seam·"성공 시에만 재시작" 계약), 도구 메뉴 Separator + "모든 설정 초기화..."(`EsIcons.Alert`), 코드비하인드 `ResetAllSettings_Click`: 확인 다이얼로그(OKCancel·Warning·**기본 Cancel**·되돌릴 수 없음/곡·성경 데이터 무영향 명시) → VM 초기화 → 실패 시 경고+중단 / 성공 시 `Process.Start(Environment.ProcessPath)`+`Application.Current.Shutdown()` 재시작. **핵심 난점=재시작 경쟁**: 닫을 때 `OnClosing` 이 창 위치를 다시 저장하면 방금 되돌린 기본값을 덮으므로, `_suppressSettingsSaveOnClose` 가드로 reset-restart 경로에서만 저장을 끔(레거시 `SaveToRegistryOnClosing=false` 와 동일). RestoreDefaults 동기 저장 → 그 후 Process.Start → 새 인스턴스가 항상 기본값을 읽음(단일 인스턴스 mutex 없음 확인·read-after-write 보장). code-reviewer **APPROVE(0 critical, 0 major)** — (a)**재시작 경쟁 제거**(동기 영속화→Start 순서·OnClosing 가드·App.OnExit 설정 미기록·ShutdownMode 기본이라 OnClosing 보장 검증), (b)**파괴적 안전장치**(경고문·기본 Cancel·실패 시 재시작 안 함), (c)**가드 부작용 없음**(reset 경로 한정·base.OnClosing 항상 호출), (d)**ProcessPath null 강등 합리적**, (e)**파일 기반 테스트가 실제 영속화까지 검증** 확인. nit 2(Process.Start 예외 방어·재시작 누락 안내) 반영. 신규 테스트 1(설정 바꾼 뒤 초기화→기본값 복원·true). 1979 green(Release). **gap §3.8 데이터 작업 메뉴 전부 포팅 완료(Clear Registry 🔴→✅).**
- (2026-06-03) ✅ 증분 160-A(별도 Preview 스테이지 모니터 — 상태머신, gap §3.1 🔴 착수) — 레거시 §5 "이중 모니터 Preview+Output 동시 송출"(회중용 출력과 별개로 워십 리더·밴드가 보는 스테이지/확인 모니터)을 여러 증분으로 포팅하는 **첫 단계**. 설계(사용자 합의): MVP=풀 스테이지 디스플레이(현재 가사+다음 항목+시계, blackout/clear 무시), 검증된 출력 스택(`IOutputWindowService`/`OutputWindowHost`/`IOutputSurface`/`OutputWindowViewModel`)을 **병렬 미러**(`docs/wpf-migration/preview-stage-monitor-plan.md`). 증분 분해 A(상태머신)→B(Host)→C(실제 창)→D(모니터 선택)→E(다음/시계). **160-A**: `PreviewWindowService`+`PreviewWindowState`/`PreviewWindowChangedEventArgs`/`IPreviewWindowService` — `OutputWindowService` 라인 1:1 미러(Open/MoveTo/Close·동일상태 no-op 가드·null 가드·상태 이벤트), 지오메트리 레코드(`OutputDisplay`/`OutputWindowPlacement`)·배치(`IWindowPlacementService.CreateOutputPlacement`)는 **공유**, 상태/이벤트는 **분리**(후속 blackout 무시 확장이 출력 상태를 오염 안 하게). 출력 경로 무변경(회귀 0). code-reviewer **APPROVE(0 critical, 0 major, 0 minor)** — 상태머신 6의미 라인단위 일치·공유/분리 경계 타당·풀스크린 배치 단언 실제 일치·record 값동등성 no-op 정확·타입충돌 0·빌드 0에러 검증. nit(settings-backed 배치 공유 리스크=출력 DisplayCustom* 가 Preview 도 끌어당김)는 계획문서 §4 리스크에 반영(C/D 착수 시 결정). 신규 테스트 6(시작Closed/Open풀스크린+이벤트/MoveTo/Close/동일상태no-op/null). 1985 green(Release).
